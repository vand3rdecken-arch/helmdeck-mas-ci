{
  "$schema": "../schema/settings.schema.json",
  "//": [
    "The board copilot's OWN settings layer. Passed as",
    "`--settings ops/harness/settings/copilot.json` with `--setting-sources \"\"`.",
    "",
    "WHY EMPTY SOURCES, unlike a card:",
    "  The copilot is spawned with cwd=daemon/, so until now it inherited whatever",
    "  the cwd happened to imply - the operator's personal ~/.claude PLUS the repo's",
    "  project layer. Both are wrong here, and the project one is actively harmful:",
    "  the repo's Stop hook runs ops/tools/loop_state.py --stop-hook, which BLOCKS a turn",
    "  that isn't at a resting build-loop state. That hook exists to stop a coding",
    "  agent from resting mid-loop; the board copilot writes no code and has no loop,",
    "  so it would be blocked for a build state it can neither cause nor fix.",
    "  So: no ambient layer at all, and everything it needs stated here.",
    "",
    "",
    "HANDS (state 2026-09-12 evening, owner decree 'mehr Rechte geben und ueber",
    "Hook einschraenken' + 'es soll offen bleiben, nicht ploetzlich",
    "eingeschraenkt'): Henry runs permission-mode AUTO (copilot.henry_pmode, one",
    "knob for chat + broker). Claude's own classifier approves the unremarkable",
    "and denies the risky, PC-wide, with no human at the prompt. HISTORY, so",
    "nobody re-adds it: under acceptEdits a headless -p could edit files but",
    "run NO command outside a literal allow rule, and the deny was silent -",
    "measured 2026-09-02: 'Freigabe fuer das Kommando fehlt' on a read-only",
    "check. The allow list below is therefore only a pre-approval of the",
    "board reader + read-only git in their exact form, NOT the fence.",
    "",
    "THE FENCE is code, not a list: ops/tools/card_tool_guard.py (the one rule",
    "Claude cannot know - never kill the daemon's process tree) plus the deny",
    "rules below, which hold ONLY the secrets and the shared memory dir (laws).",
    "Agent/Task were denied 2026-09-02 after one stray subagent; removed",
    "2026-09-12 with the openness decree - chat CHECKS, building goes through",
    "cards, and that is policy prose in the brief, not a tool ban."
  ],
  "env": {
    "HELMDECK_SURFACE": "copilot"
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Write|Edit|MultiEdit|NotebookEdit|Bash|PowerShell|WebFetch|WebSearch",
        "hooks": [
          {
            "type": "command",
            "command": "py -3.12 \"${HELMDECK_GUARD:-$CLAUDE_PROJECT_DIR/ops/tools/card_tool_guard.py}\"",
            "timeout": 30
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(py -3.12 ../ops/tools/board_state.py:*)",
      "Bash(py -3.12 ../ops/tools/loop_state.py:*)",
      "Bash(py -3.12 ../ops/tools/henry_memory_get.py:*)",
      "Bash(py -3.12 ../ops/tools/henry_brief_get.py:*)",
      "Bash(py -3.12 ../ops/tools/henry_inbox.py:*)",
      "Bash(git status:*)",
      "Bash(git log:*)",
      "Bash(git diff:*)",
      "Bash(git show:*)"
    ],
    "deny": [
      "Read(./daemon/settings.json)",
      "Read(./daemon/users.json)",
      "Read(./daemon/helmdeck.db)",
      "Read(./daemon/copilot_log.json)",
      "Read(./.env)",
      "Read(./**/.env)",
      "Write(~/.claude/projects/**)",
      "Edit(~/.claude/projects/**)"
    ]
  }
}
