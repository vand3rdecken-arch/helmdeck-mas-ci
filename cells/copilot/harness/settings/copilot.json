{
  "$schema": "../schema/settings.schema.json",

  "//": [
    "The board copilot's OWN settings layer. Passed as",
    "`--settings ops/harness/settings/copilot.json` with `--setting-sources \"\"`.",
    "",
    "WHY EMPTY SOURCES, unlike a card:",
    "  The copilot is spawned with cwd=daemon/, so until now it inherited whatever",
    "  the cwd happened to imply - the operator's personal ~/.claude PLUS the repo's",
    "  project layer. Both are wrong here, and the project one is actively harmful:",
    "  the repo's Stop hook runs ops/tools/loop_state.py --stop-hook, which BLOCKS a turn",
    "  that isn't at a resting build-loop state. That hook exists to stop a coding",
    "  agent from resting mid-loop; the board copilot writes no code and has no loop,",
    "  so it would be blocked for a build state it can neither cause nor fix.",
    "  So: no ambient layer at all, and everything it needs stated here.",
    "",
    "STALE NOTE, kept for history: 'runs --permission-mode plan, granted no",
    "tools' described the pre-2026-08-21 world. Owner decree since then: Henry",
    "runs a WORKING mode (henry_pmode, default acceptEdits). acceptEdits",
    "auto-approves EDITS but not Bash - and a headless -p session has no",
    "permission prompt (repo memory: no AskUserQuestion in -p), so every",
    "ungated Bash call just dies. Measured 2026-09-02 14:55: Henry, told to",
    "verify a dispatch instead of assuming it, tried the check and reported",
    "'Freigabe fuer das Kommando fehlt'. The allow list below grants exactly",
    "the read-only checks his job needs - the board reader",
    "(ops/tools/board_state.py, relative to his cwd=daemon/) and read-only",
    "git. Nothing that writes, ships, or leaves the repo.",
    "",
    "Agent/Task DENIED (2026-09-02 15:07, measured): on Henry's very first",
    "checking turn he also spawned a stray subagent by accident (Agent tool,",
    "prompt 'n/a - not used'), then stopped it - extra seconds, extra cost,",
    "and an English 'that was a mistake' narration the owner saw as a second",
    "answer. Chat CHECKS things; BUILDING goes through cards (```actions ->",
    "the daemon dispatches). A subagent from inside the chat process is",
    "neither - it bypasses the card rails entirely.",
    "",
    "The memory Write()/Edit() deny below is belt-and-suspenders for the same reason:",
    "plan mode already withholds the Write/Edit tools, so this only matters if that",
    "mode ever changes - but memory_paths.auto resolves to the SAME shared, non-git",
    "directory for this surface too (measured), so it costs nothing to say twice.",
    "See ops/harness/settings/card.json for the full story."
  ],

  "env": {
    "HELMDECK_SURFACE": "copilot"
  },

  "hooks": {
    "PreToolUse": [
      { "matcher": "Read|Write|Edit|MultiEdit|NotebookEdit|Bash|PowerShell|WebFetch|WebSearch",
        "hooks": [ { "type": "command",
                     "command": "py -3.12 \"${HELMDECK_GUARD:-$CLAUDE_PROJECT_DIR/ops/tools/card_tool_guard.py}\"",
                     "timeout": 30 } ] }
    ]
  },

  "permissions": {
    "allow": [
      "Bash(py -3.12 ../ops/tools/board_state.py:*)",
      "Bash(py -3.12 ../ops/tools/loop_state.py:*)",
      "Bash(py -3.12 ../ops/tools/henry_memory_get.py:*)",
      "Bash(git status:*)",
      "Bash(git log:*)",
      "Bash(git diff:*)",
      "Bash(git show:*)"
    ],
    "deny": [
      "Agent",
      "Task",
      "Read(./daemon/settings.json)",
      "Read(./daemon/users.json)",
      "Read(./daemon/helmdeck.db)",
      "Read(./daemon/copilot_log.json)",
      "Read(./.env)",
      "Read(./**/.env)",
      "Write(~/.claude/projects/**)",
      "Edit(~/.claude/projects/**)"
    ]
  }
}
