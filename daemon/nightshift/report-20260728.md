# Night shift report - 2026-07-28

## Unblock the build loop: design_lint false positive on QR black/white literals in settings.tsx
- repo: C:\Users\Tien Duy Vo\Downloads\swarmdeck
- card: 20260728-204631-night-unblock-the-build-lo (lane: working, status: needs_you)
- reply (tail): x-ok */` to the QR fg/bg line in `web/components/settings.tsx`, then run `python ops/tools/design_lint_selftest.py` (expect PASS, 4 cases) and `python ops/tools/loop_state.py` (the `hardcoded color #000000` red should be gone once the marker-enabled lint is present there).  All shippable work in this sandbox is done; I'm stopping here because both remaining actions require permissions or files outside it.
- reviewer: Review complete. The mechanism itself is sound — the `lint:hex-ok` marker logic is correct, equivalent to the old comment-skip where they overlap, and the selftest genuinely passes (I ran it: 4/4). But I found one significant task-level gap and a few smaller weaknesses. /  / ## Finding 1 (major): this branch does not actually unblock the build loop /  / The false positive lives in the **main checkout** (`Downloads\swarmdeck`), not this worktree: /  / - `web/components/settings.tsx:139` in the main checkout has the offending `color: { dark: "#000000", light: "#ffffff" }` — as uncommitted work. This worktree's `settings.tsx` has no QR code at all, so the branch couldn't annotate it. / - The main checkout still has the **old** `ops/tools/design_lint.py` (its `git status` shows no modification to it), and I verified `lint:hex-ok` appears nowhere in the main checkout — neither in the tool nor in settings.tsx. /  / So the loop that was blocked is still blocked: each checkout's hooks run its own copy of `design_lint.py`, and even after this branch merges, R4 will keep flagging until someone appends `// lint:hex-ok` to that line in the main checkout. The deliverable here is the escape hatch, not the unblock. **

## Add headless unit tests for drivers/nightshift/debt
- repo: C:\Users\Tien Duy Vo\Downloads\swarmdeck
- card: 20260728-210219-night-add-headless-unit-te (lane: working, status: needs_you)
- reply (tail): , nightshift dispatch (ranking/claiming/limit), driver meta extraction` — files: `ops/tests/unit/conftest.py`, `test_debt.py`, `test_nightshift.py`, `test_drivers.py`, `fixtures/claude_result_event.json`, plus `.loop/workorder.md`.  All 33 tests were desk-checked against `daemon/debt.py`, `daemon/processes.py`, and `daemon/drivers.py`; if any come back red, send me the pytest output and I'll fix them.
- reviewer: All finders and verification are done. Every reported finding below was confirmed directly against source or by repro (not just agent votes): I re-read `drivers.py`, `processes.py`, `events.py` (`emit(kind, track, **fields)` at line 122; shallow top-level settings merge at lines 79-88; `capacity` present in DEFAULTS), `ops/tools/loop_state.py:164-171`, grepped the daemon for `TimeoutExpired` handlers (none exist — `sessions.py:89` calls `drivers.run` bare), and reproduced the conftest-import break with `--import-mode=importlib`. /  / ## Review verdict /  / The suite is genuinely good: headless, fast (0.34s), order-independent (verified by running files reversed and in isolation), discovery works from repo root and `ops/tests/unit`, and it pins the measured-economics meta contract, cancel-turn semantics, dispatch ranking/floor/WIP-gating, and the debt register shape. All 31 tests pass — but note `py -3.12` (the interpreter CLAUDE.md documents) has no pytest installed; the suite only runs under the default `python`. The findings are mostly about what the fakes *fail to pin* and two latent production bugs the review surfaced that the tests should cover. /  / ```json / [ /   { /     "file": "ops/tests/unit/test_nig
