# -*- coding: utf-8 -*-
"""The structural debt register - the program's own list of load-bearing
shortcuts. Debt lives IN the code (reviewed like code, updated when paid),
is visible in the History view, known to the copilot, and convertible to a
fix card with one click. A shortcut that isn't written down here is either
not known yet or not a shortcut.

status: open | in_progress | paid  (paid items stay listed - they document
why the code looks the way it does)"""

DEBT = [
    {
        "id": "open-question-identity-is-lexical",
        "order": -10,
        "title": "two owner questions are judged 'the same' by word overlap, not by meaning",
        "status": "open",
        "what": "cells/pm/pm.py _same_question decides whether the verifier's "
                "must_ask duplicates a question the planner already asked. It "
                "does so LEXICALLY: content words (stoplist-filtered), then "
                "set containment or a Jaccard overlap >= _Q_SAME (0.55). That "
                "threshold is measured, not reasoned - on real PM question "
                "pairs the same ask reworded scores >= 0.571 and two different "
                "asks about the same object top out at 0.500 "
                "(ops/tests/test_pm_clarifications.py pins both sides) - but "
                "0.055 of daylight is a narrow gap, and the function has no "
                "understanding of what either question MEANS.",
        "why_it_bites": "open_questions is load-bearing: pm._state returns "
                        "'ASK' and holds EVERY dispatch while one is open. So "
                        "a false merge silently drops a question the owner "
                        "then never gets asked, and the PM plans on an "
                        "assumption instead. The blast radius is bounded "
                        "because this is the SECOND net - the real fix is that "
                        "_verify_plan now sees the owner's clarifications and "
                        "so stops re-deriving answered questions at the source "
                        "- and because the merge is conservative by "
                        "construction (small stoplist, >=3-word containment "
                        "rule), preferring a duplicate line over a lost "
                        "question.",
        "trigger": "Two genuinely DIFFERENT owner questions that share most of "
                   "their content words - most likely two decisions about the "
                   "same object ('Budget fuer den Closed Test?' vs 'Deadline "
                   "fuer den Closed Test?' already sits at 0.500, one word "
                   "away from being merged).",
        "fix": "Give a question an IDENTITY instead of a spelling: have the "
               "planner emit a stable key per open question (topic slug + "
               "decision kind) and dedup/resolve on that, so an answered "
               "question is closed by its key - the same 'derive it from the "
               "runtime's own signal, at one owner' shape as "
               "drivers.turn_active. Then _same_question degrades to a "
               "fallback for plans that predate the key.",
        "since": "2026-09-02",
    },
    {
        "id": "henry-card-session-uncompacted",
        "order": -9,
        "title": "only the BOARD Henry session is proactively compacted - card sessions are not",
        "status": "open",
        "what": "Henry conversations are keyed per (user, card) since 2026-09-02 "
                "(cells/copilot/copilot.py _skey): the board chat keeps the "
                "long-lived session, and each card-scoped chat gets its own. "
                "_maybe_compact still runs against the BOARD key only "
                "(sid = sess.get(_skey(user))), so a card session never gets "
                "the proactive /compact at ~80% context that the board one "
                "does.",
        "why_it_bites": "A card whose Henry chat runs long enough would hit "
                        "the model's context window with no proactive "
                        "compaction to save it - the same 'Prompt is too "
                        "long' failure the board session was given "
                        "_maybe_compact to avoid. Not observed yet: a card "
                        "session is bounded by the card's own lifetime, which "
                        "is why this ships as a known gap rather than a "
                        "blocker.",
        "trigger": "A single card with a very long Henry conversation - most "
                   "likely a long-lived process/epic card the owner keeps "
                   "returning to.",
        "fix": "Give _maybe_compact the same per-conversation key the rest of "
               "the path now uses: compact whichever session the turn belongs "
               "to, not only the board one. The mark/window logic is already "
               "per-session (it reads ctx_tokens from copilot_stats), so this "
               "is a keying change, not new machinery - but copilot_stats is "
               "itself still keyed per USER, so that has to be split first or "
               "a card turn will compact against the board's numbers.",
        "since": "2026-09-02",
    },
    {
        "id": "workers-die-with-the-daemon",
        "order": -8,
        "title": "a daemon restart can only be safe or instant, never both - workers are its children",
        "status": "open",
        "what": "Every worker is a CHILD process of the daemon, and the ONLY "
                "reader of a worker's stdout is a thread inside that same "
                "daemon (spine/agent/drivers.py:921 `for line in "
                "self.proc.stdout` -> _on_event; cells/copilot/copilot.py "
                "runs the same shape for chat). The singleton takeover then "
                "evicts a prior daemon with `taskkill /F /T` "
                "(spine/http/startup.py:144), whose /T cascades through that "
                "child tree. Two consequences, both load-bearing:\n"
                "(1) an agent that restarts the daemon from inside the tree "
                "kills ITSELF mid-turn - worked around by launching the "
                "restart under Task Scheduler's own parentage "
                "(restart_helmdeck.ps1, restart.cmd), not by fixing the "
                "lifecycle;\n"
                "(2) the eviction cannot simply DROP /T either: an orphaned "
                "worker keeps running with nothing reading its stdout, so "
                "its output is silently lost - strictly worse than killing "
                "it. So startup.py buys safety with a timed grace wait "
                "(HELMDECK_RESTART_GRACE, default 600s) instead.",
        "why_it_bites": "Every deploy is either slow (wait out the grace "
                        "while live turns finish) or destructive (grace "
                        "expires and running work is tree-killed - the owner "
                        "lost the same 15-minute machine turn to a deploy "
                        "twice in one evening, per startup.py's own "
                        "_running_turns docstring). It also makes 'restart "
                        "the daemon' a multi-step ritual an agent cannot "
                        "perform on itself.",
        "trigger": "Any daemon restart while a card or chat turn is live - "
                   "i.e. most deploys during a working session.",
        "fix": "Make a worker's output durable independently of the process "
               "that spawned it (worker writes its own event stream to "
               "run_dir, or a supervisor separate from the HTTP daemon owns "
               "the pump), THEN drop /T and let workers survive a daemon "
               "swap. surfaces/desktop/tray.py already supervises by HEALTH "
               "rather than PID, so the supervisor half of this shape "
               "exists; the worker-parentage half does not. Measure before "
               "shipping - do not drop /T on reasoning alone.",
        "since": "2026-09-02",
    },
    {
        "id": "chat-snapshot-skip-heuristic",
        "order": -7,
        "title": "board snapshot was skipped on a text heuristic, not a real relevance check",
        "status": "paid",
        "what": "cells/copilot/copilot.py chat() injected the whole BOARD "
                "SNAPSHOT/PM plan/memory block (83,488 chars / ~23k tokens, "
                "measured) as uncached input on EVERY turn. The first attempt "
                "skipped the block entirely when turnopts.is_trivial_chatter("
                "message) matched a short greeting/ack. That was a band-aid on "
                "the wrong layer and is now REMOVED (helper deleted too).",
        "why_it_bites": "PAID - kept for why the fix looks the way it does. "
                        "Two lessons. (1) The heuristic was English-only "
                        "(^hi|ok|thanks|yes|no...), so for a German-writing "
                        "owner 'danke'/'ja' never matched and the optimisation "
                        "almost never fired - a text-match gate inherits the "
                        "language of whoever wrote the regex, and the same "
                        "_EASY regex also drives model-tier routing. (2) It "
                        "optimised the CHEAP case (chatter) while every real "
                        "question still paid the full 23k - the saving was "
                        "where the cost wasn't.",
        "trigger": "was: a greeting-shaped message that nonetheless needed "
                   "fresh board state.",
        "fix": "Superseded, shipped in the same commit as this status flip: "
               "_snapshot() now takes full=False and a turn carries only the "
               "LIVE board (open cards, processes, policy, debt ids) - 17,249 "
               "chars / ~4.8k tokens, a 79% cut - while the history that made "
               "up 65% of the payload (121 finished + 31 archived cards, full "
               "debt prose) is fetched on demand by Henry running "
               "ops/tools/board_state.py --full. That is the Paseo shape "
               "(packages/server/.../claude/agent.ts:3386 sends ONLY the "
               "user's text; context lives in the cached system prompt + "
               "tools). No text heuristic remains in the path.",
        "since": "2026-09-02",
    },
    {
        "id": "wear-push-no-deep-link",
        "order": -6,
        "title": "a tapped notification opens the watch app at the BOARD, whatever the push was about",
        "status": "open",
        "what": "Shipped 2026-08-29 with the Henry reply push "
                "(spine/comms/notify.py chat_reply): a finished Henry turn now "
                "buzzes every paired device, and on the PHONE the tap routes by "
                "`kind` - kind='chat' lands in the Henry chat, a card push in "
                "that card's chat (surfaces/app/src/app/_layout.tsx). The WATCH "
                "reads the same sealed payload but PushService.kt's show() "
                "builds one fixed Intent to MainActivity and never looks at "
                "`kind` or `track`, so every notification - card question, card "
                "result, Henry answer - opens the app at BoardScreen.\n"
                "Deliberately not fixed in the same card: MainActivity holds "
                "its screen in Compose state with no intent-extra path, so this "
                "is real Kotlin plus an onNewIntent story, and it can only be "
                "judged on the wrist. A wear APK rebuild + install is not "
                "reachable from a card worktree, and navigation code shipped "
                "blind is worse than a known extra tap.",
        "why_it_bites": "The watch is the surface with the LEAST patience: the "
                        "owner feels the buzz, taps, and lands on a board he "
                        "did not ask about - then hunts for Henry (or the card) "
                        "by hand on a round screen. It also silently diverges "
                        "from the phone, so the two devices answer the same "
                        "notification differently.",
        "trigger": "Any Henry answer or card event tapped ON THE WATCH. Costs "
                   "one extra navigation every time; nothing breaks.",
        "fix": "Put `kind`/`track` into the PendingIntent as extras in "
               "PushService.show(), read them in MainActivity (the launch "
               "intent AND onNewIntent) and set the WearScreen accordingly - "
               "Henry for kind='chat', the card for a track that resolves. Then "
               "rebuild and install the wear APK and confirm on the watch, "
               "which is the only place this can be judged.",
    },
    {
        "id": "chat-optimistic-text-match-compat",
        "order": -5,
        "title": "the board chat still falls back to matching optimistic messages by TEXT when the daemon sends no client_msg_id",
        "status": "open",
        "what": "Cause fixed 2026-08-29 (owner: 'meine Nachricht steht ganz am "
                "Ende im chat obwohl ich zeitlich frueher kam'). "
                "surfaces/app/src/app/chat.tsx rendered "
                "[...server, ...pending] - every optimistic bubble appended "
                "AFTER the whole server list - and retired a pending turn only "
                "when the server reported the same TEXT more often than at "
                "queue time. Two defects followed: a PM message landing "
                "mid-turn rendered before a message sent earlier, and a copy "
                "whose stored text never matched stuck to the bottom forever. "
                "Now the daemon echoes the client's own `mid` (the replay "
                "token client.ts already minted and chat_dedupe.py already "
                "read) back as client_msg_id, and the app retires by that "
                "identity and INSERTS each bubble at the anchor position the "
                "server list had when it was queued.\n"
                "The remaining debt is the text-count fallback, tagged "
                "COMPAT(chatClientMsgId): it is what still clears a turn sent "
                "to a daemon older than this change. Paseo carries the same "
                "shim for the same reason (packages/app/src/timeline/"
                "session-stream-reducers.ts, matchesLocalUserMessageIdentity).",
        "why_it_bites": "Text-count matching is a heuristic where the mid is "
                        "an identity: send the same sentence twice against an "
                        "old daemon and the count can retire the WRONG copy "
                        "or none, and the bubble sticks to the bottom of the "
                        "chat - exactly the defect this change removed. "
                        "Deleting the shim today is no better: it would "
                        "strand every optimistic bubble on a surface still "
                        "talking to a pre-2026-08-29 daemon.",
        "trigger": "An app one OTA ahead of its daemon (the shim's reason to "
                   "exist), or repeated identical texts inside one turn "
                   "window (its failure mode). Revisit once every daemon in "
                   "use echoes client_msg_id - grep COMPAT(chatClientMsgId).",
        "fix": "Remove the COMPAT(chatClientMsgId) branch in chat.tsx's "
               "reconcile effect once every daemon in use echoes "
               "client_msg_id. One grep for COMPAT(chatClientMsgId) is the "
               "whole cleanup.",
    },
    {
        "id": "ship-aborted-loop-root-cause-open",
        "order": -4,
        "title": "rerun_deploy no longer silently no-ops on a repo_hooks mismatch; the ACTUAL cause of every ship-aborted recurrence today (wrong hook path, fixed) found and corrected; the daemon-restart trigger itself is still unconfirmed",
        "status": "open",
        "what": "Fixed (2026-08-27, found live during an unrelated RBAC "
                "session): cells/copilot/henry_broker.py's rerun_deploy "
                "handler used to spawn its deploy hook in a fire-and-forget "
                "thread and unconditionally return True, closing the "
                "escalation as 'decided' even when cells/engineer/"
                "lanemachine.py's _repo_hook() silently no-ops (settings."
                "repo_hooks has no EXACT string match for the resolved repo "
                "path) - the ship.lock's dead pid then never changes. Now it "
                "checks the hook command exists BEFORE spawning the thread "
                "and returns False + records a note if not, so a "
                "misconfiguration surfaces via the existing 2-attempt "
                "give-up + owner-notify path instead of silently repeating "
                "forever.\n"
                "Still genuinely open: WHY the daemon restarted TWICE before "
                "this fix (the same dead pid 28356 was reported by two real "
                "'ship-aborted' escalations at 08:48 and 09:50, ~62min apart "
                "- check_stale_ship_lock() only runs once per process from "
                "start_broker(), so this means the daemon process itself "
                "restarted, not just the escalation re-firing). "
                "surfaces/desktop/tray.py's supervisor restarts the daemon "
                "whenever :8140 stops answering its health check - plausible "
                "but NOT confirmed: box was idle (24% CPU, no gradle/java/"
                "node) when checked, arguing against 'a build is hogging the "
                "health check' as the whole story.\n"
                "RESOLVED, separately (initially misread as a THIRD/FOURTH "
                "recurrence of the same loop): two more baseline commits "
                "appeared at 11:03-11:06, only 29s apart with inverse diffs, "
                "and were first suspected to be more daemon-restart-driven "
                "escalations. Traced via the session transcript instead: no "
                "corresponding entries exist in escalations.jsonl for that "
                "window at all - the real cause was ops/tests/"
                "test_escalations.py being run (by the same session, "
                "debugging the rerun_deploy fix above) while it stubbed only "
                "hb._ask, not hb._hands_on_ask, and never redirected hb."
                "_HENRY_REPO_ROOT to a sandbox. Card-less escalations are "
                "'privileged' (_dispatcher_privileged(None)==True), so "
                "_decide() called the REAL _hands_on_ask() -> REAL "
                "_baseline_commit() against the ACTUAL live repo on every "
                "test run with a dirty tree. Fixed in the same commit as "
                "this entry: test_escalations.py now stubs both functions "
                "AND redirects _HENRY_REPO_ROOT to the test's tempdir (a "
                "non-git path, so _baseline_commit's own `except: return` on "
                "a failed `git status` makes it a safe no-op there) - the "
                "same defense-in-depth test_henry_privilege_gate.py already "
                "had. No daemon-side fix was needed for this half; it was "
                "never a daemon behavior.\n"
                "ACTUAL RECURRENCE CAUSE, found 2026-08-27 13:17 (same dead "
                "pid 28356 reported a THIRD time, ~3h20m after the 08:27-fix "
                "landed): daemon/settings.json's repo_hooks deploy command "
                "still pointed at 'deploy/ship.sh', a path that stopped "
                "existing when 298decc (the four-folder root refactor, "
                "2026-08-24) moved the script to 'ops/deploy/ship.sh'. That "
                "settings.json is git-ignored machine-local state, so the "
                "refactor commit never touched it. Every rerun_deploy since "
                "then spawned bash.exe against a nonexistent relative path - "
                "cmd was non-empty so the 08:27 exists-check passed, but the "
                "hook process failed instantly (before touching .loop/"
                "ship.lock at all), leaving the SAME dead pid to be "
                "re-reported on every subsequent daemon restart forever. "
                "Fixed by correcting the path in settings.json (not a code "
                "change - no commit carries this half). This was the real "
                "engine behind the loop; the daemon-restart trigger below is "
                "a separate, still-open question about why the daemon kept "
                "restarting at all.\n"
                "NEW OPEN ISSUE, found the same session (13:17-13:31): "
                "Henry's own two hands-on attempts at THIS exact escalation "
                "(ship-aborted-1787829465743) never closed it - both ended "
                "'ask failed: no JSON in reply' and it gave up after "
                "_MAX_ATTEMPTS, notifying the owner, EVEN THOUGH the "
                "underlying turn had done real, correct, valuable work (it is "
                "the turn that found and fixed the settings.json path bug "
                "documented above). Root cause: _ask()'s headless `claude -p` "
                "subprocess runs with cwd=_HENRY_REPO_ROOT - the SAME repo "
                "checkout this daemon's own session lives in - so it inherits "
                "THIS repo's project-level SessionStart/Stop hooks (the "
                "ALIGN/ANALYZE/EXECUTE/TEST/CLEAN/BUILD/COMMIT build loop, "
                "CLAUDE.md). Both attempts got redirected into filling in "
                ".loop/workorder.md as a normal build-loop session would, "
                "instead of replying with the bounded-verb JSON henry_broker."
                "py's own prompt demands - so _ask()'s regex-JSON parse found "
                "prose about the workorder instead. The stray, git-ignored "
                ".loop/workorder.md this left behind then made the NEXT "
                "interactive session's OWN loop_state.py read 'stuck at "
                "ANALYZE' - a real collision between two consumers of the "
                "same repo-root hooks, not just a wasted turn. Deleted by "
                "hand this session; nothing currently prevents it recurring "
                "on the next hands-on Henry turn.",
        "why_it_bites": "Every daemon restart while a ship.lock is stale "
                        "auto-commits the ENTIRE uncommitted working tree "
                        "via henry_broker.py's _baseline_commit() (a card-"
                        "less escalation is 'privileged' per "
                        "_dispatcher_privileged(None)==True, so it runs "
                        "through _hands_on_ask, which snapshots the tree "
                        "before giving Henry real hands) - any agent or "
                        "human with uncommitted work in this repo at the "
                        "wrong moment gets it swept into a commit without "
                        "warning. The same blast radius turned out to be "
                        "reachable from a test file too, not just a live "
                        "daemon - any future test that imports henry_broker "
                        "and drives _decide()/_hands_on_ask() without "
                        "sandboxing _HENRY_REPO_ROOT has this exact footgun.",
        "trigger": "Daemon restart trigger (CLOSED 2026-08-29, see "
                   "tray-health-probe-eviction-war): CONFIRMED as tray.py's "
                   "health check - it probed '/' (which 302-redirects to the "
                   "web UI on :3300, urllib follows it) and demanded a 200, "
                   "so it measured the WEB server's health and counted an "
                   "auth-gated reply as DOWN; every false 'down' spawned a "
                   "rival daemon whose SINGLETON taskkill /F /T evicted the "
                   "healthy one. "
                   "Test-sandboxing gap (closed): any ops/tests/*.py that "
                   "imports cells.copilot.broker.henry_broker and calls _decide() "
                   "or _hands_on_ask() on a privileged/card-less escalation "
                   "without first redirecting _HENRY_REPO_ROOT. Hook-hijack "
                   "gap (open): any privileged/card-less escalation at all - "
                   "every _ask() call inherits this repo's own build-loop "
                   "hooks via cwd, nothing scopes them to Henry's turn.",
        "fix": "Daemon-restart half is PAID (2026-08-29, see "
               "tray-health-probe-eviction-war). "
               "Test-sandboxing half is fixed (this commit). Separately "
               "still worth considering: _baseline_commit() firing on EVERY "
               "hands-on judgement turn (not just ones that edit code) may "
               "be too broad - a card-less, read-only-outcome escalation "
               "like a misconfigured deploy hook doesn't need a tree "
               "snapshot at all. Hook-hijack half needs: either point "
               "henry_broker._ask()'s subprocess at a settings scope that "
               "excludes THIS repo's own SessionStart/Stop hooks (the CLI "
               "supports --settings, though HARNESS.md warns it 'excludes "
               "nothing' by itself - worth re-reading before relying on it "
               "here), or make the build-loop's Stop hook recognize a "
               "workorder it did not itself create and refuse to gate on it.",
    },
    {
        "id": "tray-health-probe-eviction-war",
        "order": -4,
        "title": "Tray health probe measured the web UI, not the daemon - false 'down' spawned rival daemons that tree-killed the live one (the 2026-08-29 'HelmDeck dead' incident)",
        "status": "paid",
        "what": "surfaces/desktop/tray.py's _health() probed '/' with "
                "urllib demanding a 200. GET / 302-redirects to the web UI "
                "(:3300) and urllib FOLLOWS redirects - so the supervisor's "
                "'daemon health' was actually 'is something serving :3300', "
                "and an auth-gated direct reply (401) raised HTTPError and "
                "counted as DOWN. Every false negative made _supervise() "
                "spawn a fresh daemon, whose _take_singleton_lock() "
                "(spine/http/startup.py) taskkill /F /T'd the perfectly "
                "healthy incumbent. With several spawners on the box (tray, "
                "Electron main.js startDaemon, manual CLI starts, accept-"
                "path deploys) this became an eviction WAR with windows "
                "where NO daemon listened - measured live 2026-08-29 "
                "(pidfile churn 20236->20464->11044->3288->18640, owner-"
                "visible as 'HelmDeck dead' minutes after a verified-up "
                "restart). Fixed: _health() now probes /system/health "
                "(direct answer, no redirect) and treats ANY HTTP reply "
                "incl. HTTPError (401/404) as alive - byte-for-byte the "
                "semantics main.js daemonReachable() already had ('even "
                "401 means one is listening'). This closes the 'daemon "
                "restart trigger' half of ship-aborted-loop-root-cause-open.",
        "why_it_bites": "PAID - kept for why the probe looks the way it "
                        "does. The trap class: a supervisor health check "
                        "that transits ANY dependency (redirect, auth, "
                        "another port) will kill healthy processes when "
                        "that dependency blinks; probe the process itself "
                        "and count any answer as life.",
        "trigger": "was: web UI on :3300 not serving (Electron closed, Expo "
                   "dev server down) while tray.py supervises.",
        "fix": "shipped in the same commit as this entry.",
    },
    {
        "id": "rbac-audit-hardening-partial",
        "order": -3,
        "title": "Card 5 (rbac-gxp) audit hardening - PAID in full, including the two items deferred at first ship",
        "status": "paid",
        "what": "events.save_settings() emits an old->new diff event per "
                "write (changed keys only, secrets masked by key-name regex). "
                "events.query_audit() is the one shared filter GET /audit "
                "(routes_audit.py), Henry's chat 'audit_query' action "
                "(cells/copilot/copilot_actions.py), and Henry's autonomous "
                "escalation judgement (see below) all call. The app has a "
                "real screen (surfaces/app/src/app/audit.tsx, cap-gated "
                "audit.read). PAID LATER, SAME PASS: (1) device-token TTL - "
                "auth.issue_token(expires_days=...) (optional, default None = "
                "unchanged behaviour), resolve() refuses an expired token, "
                "_touch_token records last_used (throttled to once/day - "
                "resolve() runs on every request, an unthrottled write would "
                "rewrite users.json constantly), _token_stale() computes "
                "'>90 days unused' live (last_used wins over created) and "
                "GET /users surfaces it; the Users panel (settings.tsx) shows "
                "a stale badge. (2) henry_broker.py's autonomous escalation "
                "path now DOES pull audit context: new _audit_context(t) in "
                "_decide()'s prompt construction, gated on the card's own "
                "dispatched_by holding audit.read (never a standing "
                "capability Henry holds itself - it only ever sees what that "
                "account could see), capped to the card's own last 8 events, "
                "degrades to '' on any lookup failure rather than blocking "
                "the judgement turn.",
        "why_it_bites": "n/a (paid) - was: no access-review signal for stale "
                        "device tokens, and Henry's autonomous judgement on a "
                        "blocked card couldn't see the audit history that "
                        "would explain WHY the block exists.",
        "trigger": "n/a (paid).",
        "fix": "PAID. Tests: ops/tests/test_token_staleness.py (issuance, "
               "expiry, throttled last_used, staleness computation, real "
               "GET /users round-trip) and ops/tests/"
               "test_henry_audit_context.py (empty on no-dispatcher/no-"
               "audit.read/lookup-failure, populated and track-scoped when "
               "the dispatcher has audit.read) - both green.",
    },
    {
        "id": "rbac-permission-registry-partial",
        "order": -2,
        "title": "spine/auth/permissions.py's central capability guard covers a first tranche of routes, not all ~85 inline role checks",
        "status": "open",
        "what": "ops/docs/backlog/rbac-gxp card 2 built spine/auth/permissions.py "
                "(matrix/can/require/cap_for, seeded in "
                "policy_seed.json['policies']['permissions']) and wired it into "
                "spine/http/server.py's do_GET/do_POST right after the existing "
                "cell gate - additively: cap_for() returns None for anything not "
                "yet declared, so an unmigrated route's own inline check remains "
                "its only enforcement. Migrated so far (old inline checks REMOVED, "
                "central guard is now the only gate): routes_settings.py (all 4 "
                "routes), routes_runs.py (/runs, /live.jpg - NOT the per-card "
                "runs_item_get, which stays on auth.owns_card by design), "
                "routes_audit.py (/audit), routes_devices.py (all handlers, table "
                "+ path-param via PATTERNS), routes_misc.py (/voice/transcribe), "
                "routes_gxp.py (new in card 6, /gxp/state + /gxp/activate), "
                "routes_checkpoints.py (/checkpoints/<id>/diff + /restore, path-"
                "param, cap settings.read/write), routes_cells.py (/cells/<id>/"
                "source, path-param, new cap system.introspect), routes_projects.py "
                "(all 4 routes incl. 2 path-param, new caps projects.view/manage), "
                "cells/pm/routes_pm.py (all 6 routes, new caps pm.view/manage), and "
                "server.py's own /users block + PATTERNS entries for /users, "
                "/users/<name>/<action>. copilot_actions.py's chat_admin_roles() "
                "gate was ALSO unified (auth.chat_admin_roles() now derives from "
                "the matrix's cards.admin capability instead of reading "
                "settings.json's policy.chat_admin_roles independently - the "
                "deferral noted below is PAID). NOT yet migrated: cells/engineer/"
                "routes_tracks.py, routes_track_actions.py (the crown-jewel card-"
                "lifecycle routes - highest blast radius, deliberately last), "
                "cells/copilot/routes_copilot.py, cells/connectors/"
                "routes_connectors.py, routes_system.py, routes_info.py, and "
                "server.py's own biggest block: the client-blanket-POST allowlist "
                "(do_POST, 'clients can file and comment only') and the large "
                "/tracks/* path-param if-chain (~20-25 checks).",
        "why_it_bites": "Anyone reading only spine/auth/permissions.py could "
                        "believe every route is capability-gated centrally; it "
                        "is not. A route in an unmigrated module still relies "
                        "entirely on its own inline `if user[\"role\"] != ...` "
                        "check - correct today (nothing was removed there), but "
                        "it means the 'one central place that says who can do "
                        "what' promise is only true for the modules listed above. "
                        "A reviewer auditing authorization for compliance "
                        "purposes must still read the unmigrated modules by hand, "
                        "same as before card 2.",
        "trigger": "Adding a new route to an unmigrated module and assuming the "
                   "central guard covers it (it silently doesn't - cap_for() "
                   "returns None, so ONLY that route's own inline check, if any, "
                   "protects it). Or: an owner/auditor assuming the permission "
                   "matrix is a complete authorization map before Card 2's "
                   "migration finishes.",
        "fix": "Continue the migration sequencing already designed in card 2 "
               "(add *_CAPS/PATTERNS entries for one more module, add it to "
               "permissions._CAP_MODULES, verify against ops/tests/"
               "test_server_routes.py, THEN remove that module's old inline "
               "checks) until every remaining module above is covered, finishing "
               "with the client-blanket-POST block and the /tracks/* path-param "
               "chain last as planned. Extend ops/tests/test_permissions.py's "
               "coverage loop to assert 100% of GET_ROUTES/POST_ROUTES across "
               "every route module has a capability entry once that point is "
               "reached (currently it only asserts internal consistency for the "
               "modules in permissions._CAP_MODULES, not repo-wide completeness - "
               "a repo-wide assertion today would fail loudly, which is the "
               "honest state to be in until the migration is actually done).",
    },
    {
        "id": "make-icon-py-stale-design",
        "order": 0,
        "title": "ops/tools/make_icon.py still hardcodes the OLD logo - would revert the redesign if rerun",
        "status": "paid",
        "what": "The 2026-08-26 logo redesign (H with a swept diagonal crossbar, "
                "hand-traced from an approved AI concept) shipped by directly "
                "overwriting the asset files across surfaces/app/assets/images/, "
                "surfaces/app/assets/expo.icon/, and surfaces/desktop/assets/ - "
                "NOT by updating the repo's own canonical generator. "
                "ops/tools/make_icon.py is a deterministic Pillow script whose "
                "docstring lists it as the single source for every one of those "
                "same files (straight-post 'glass H' on an aurora-glow tile, a "
                "completely different design). It was never touched, so it was "
                "silently out of sync with what shipped: running "
                "`py -3.12 ops/tools/make_icon.py` would have regenerated every "
                "icon/splash/favicon back to the OLD design and overwritten the "
                "redesign with zero warning.",
        "why_it_bites": "PAID - a script that's the documented single source "
                        "for a brand asset but silently disagrees with what's "
                        "actually shipped means the next person who touches "
                        "icons (including a future agent turn) trusts the "
                        "generator, reruns it to make an unrelated tweak, and "
                        "reverts the whole redesign with a clean exit code and "
                        "no diff review catching it until someone notices the "
                        "app icon changed.",
        "trigger": "n/a (paid) - was: anyone running "
                   "`py -3.12 ops/tools/make_icon.py` for any reason.",
        "fix": "PAID (option b from the original two): make_icon.py's parametric "
               "h_mask()/glass_H()/make_tile() were retired. The generator now "
               "loads a fixed traced alpha mask (ops/tools/assets/logo_h_mask.png "
               "- the H shape thresholded from the approved concept image, since "
               "its organic curves have no clean parametric description) and "
               "composites it flat onto a 2-stop diagonal gradient matching the "
               "shipped colors exactly, deriving EVERY target from that one mask: "
               "desktop icon-1024/icon.ico/icon-mac-1024 (mac now also gets the "
               "proper inset+contact-shadow treatment the old script had but this "
               "session's ad hoc desktop fix skipped), the Expo unified icon + "
               "favicon, both Android adaptive layers, splash-icon.png, and the "
               "iOS Liquid Glass PNG layer + icon.json fill color. Re-running it "
               "now regenerates the CURRENT design byte-for-byte (verified: "
               "splash-icon.png and the iOS PNG came out identical; the gradient/"
               "rounded-corner pixels differ by only a few LSBs from sub-pixel "
               "rounding, sampled and confirmed negligible before overwriting "
               "anything live). To change the mark going forward: swap "
               "ops/tools/assets/logo_h_mask.png and rerun - one generator, one "
               "source of truth, no more silent divergence.",
    },
    {
        "id": "remote-worker-not-hardened",
        "title": "Remote device execution shipped as a reference implementation, not a service",
        "status": "paid",
        "what": "ops/docs/backlog/remote-device-execution: a team member's own "
                "PC can register as a device and execute cards locally, "
                "submitting its finished branch back via a git bundle - the "
                "daemon-side registry/dispatch/import path (spine/auth/"
                "devices.py, cells/engineer/dispatch.py's new_remote_task/"
                "claim_remote_task/submit_remote_result, spine/git/gitutil.py's "
                "_import_bundle) is proven end to end in ops/tests/"
                "test_remote_device.py against real git repos and bundles. "
                "PHASES A-C PAID 2026-08-25 (owner: 'mache a,b und c', see "
                "PLAN-hardening.md): claimed_at + sweep_stale_device_claims "
                "(boot one-shot + continuous poller) so an abandoned claim "
                "self-heals back to backlog for the SAME device instead of "
                "wedging a card in 'working' forever; idempotent "
                "submit_remote_result (a lost-response retry no longer hits "
                "_import_bundle's refuse-to-clobber as an error); a "
                "device-match check (a device may only submit against a card "
                "bound to itself); reassign_remote_task + POST /devices/"
                "reassign for owner/operator recovery onto a different device "
                "or back to a normal card; hd_worker.py's _api gained bounded "
                "exponential backoff + jitter on transient failures (never on "
                "a 4xx refusal) and the main loop distinguishes 'no work' from "
                "'daemon unreachable' pacing, plus a clean KeyboardInterrupt "
                "message. All pinned in ops/tests/test_remote_device.py (57 "
                "assertions) and the new ops/tests/test_hd_worker.py (9). Also "
                "added ahead of schedule: spine.auth.devices.register's "
                "billing_scope field ('external' default/'shared'), so a "
                "device's own Claude account is never assumed to feed the "
                "shared ai_billing/plan_calibration pool once real usage "
                "capture exists. "
                "PHASE D PARTIALLY PAID 2026-08-25 (owner: 'mache auch d'): "
                "usage/cost capture - hd_worker.py's _run_turn_locally now "
                "calls claude -p --output-format json (was plain text) and "
                "parses the SAME total_cost_usd/usage/modelUsage fields "
                "spine/agent/drivers.py itself parses, submitted as "
                "usage_meta and folded via spine/turn/econ.py's REAL "
                "_record_econ (found to already be a small function "
                "separate from the 700-line _ClaudeSession - no risky "
                "extraction needed, genuine reuse not a duplicate). "
                "billing_scope is now ENFORCED, not just recorded: "
                "_record_econ(external=True) tags the emitted turn event, "
                "and events.plan_calibration excludes external=True turns "
                "from its weekly-window sum - a real bug caught while "
                "implementing this, not hypothetical: that calibration "
                "divides tokens by the DAEMON's OWN account usage percent, "
                "so an external device's tokens would have silently "
                "inflated tokens_per_pct for every card on the real "
                "account. Revoke-signals-worker: hd_worker.py classifies a "
                "404 on its OWN /devices/<id>/queue as DeviceRevoked and "
                "exits (code 2) instead of retrying under Phase B's "
                "backoff forever - cheap (no new push channel, rides the "
                "existing ~20s poll), closes 'never notices', not 'stays "
                "running'. Config file: --config <path> (JSON) - argv wins "
                "per-field on overlap - the lighter alternative named in "
                "the original sketch, done instead of full packaging. "
                "Pinned: ops/tests/test_hd_worker.py grew to 25 assertions, "
                "new ops/tests/test_device_billing_scope.py (7, proves the "
                "plan_calibration exclusion with real token-sum before/"
                "after, not just a tag check), test_remote_device.py grew "
                "to 61. "
                "PHASE E PAID 2026-08-25 (owner: 'direkt mit e anfangen'): "
                "revoke/reassign now interrupts an IN-FLIGHT turn, not just "
                "the next poll. dispatch.device_card_status(device, tid) + "
                "GET /devices/<id>/card/<tid> is the cheap read-only 'still "
                "mine?' check; hd_worker._run_turn_locally runs claude via "
                "Popen with a still_mine watcher polled every ~5s and "
                "tree-kills the local subprocess (taskkill /T, mirroring "
                "drivers.py) + raises TurnInterrupted the moment the card is "
                "reassigned/reclaimed/revoked out from under it, so no "
                "orphaned result is submitted. A late submit from the "
                "reassigned-away device is rejected AND logged as a named "
                "outcome (remote_device action=stale_submit_rejected), not a "
                "bare 400. Pinned in test_remote_device.py (device_card_"
                "status across claim/reassign/unknown-card + the stale-"
                "submit event) and test_hd_worker.py (still_mine=False "
                "tree-kills + raises; still_mine=True runs to completion). "
                "PHASE F PAID 2026-08-25 (owner: 'F und g'): pip entry point "
                "(ops/tools/pyproject.toml, py-modules-scoped to just "
                "hd_worker so `pip install ops/tools/` exposes `hd-worker` "
                "without the daemon-side tools) + login autostart via an "
                "HKCU Run key (hd-worker --install-autostart --config <path>, "
                "same winreg mechanism as surfaces/desktop/tray.py, never "
                "shelling to powershell; token stays in the config file not "
                "the Run value; --uninstall-autostart removes it). Windows-"
                "first, no-ops with a clear message on mac/Linux (manual "
                "launchd/systemd recipe in the module docstring). Pinned in "
                "test_hd_worker.py (autostart cmd shape + a real HKCU Run-key "
                "register/read/remove round-trip against a throwaway value "
                "name). "
                "PHASE G CODE-COMPLETE 2026-08-25 (owner: 'F und g'): the "
                "board now shows a device card's exec_site as an 'on device'/"
                "'device offline?' chip (t.ai / t.danger, the latter driven "
                "by a new lifecycle._present_device read-side device_stale "
                "derivation - claimed_at older than the sweep TTL, a hint; "
                "the sweep stays authority), and settings has a Devices panel "
                "(GET /devices/mine + register/revoke, billing_scope + "
                "last_seen shown, and a reassign action for any stale device "
                "card via POST /devices/reassign). tsc --noEmit clean, i18n "
                "lint clean for the new keys (the 6 remaining i18n reds "
                "pre-date this and are the known base issue), present() "
                "device_stale derivation pinned in test_remote_device.py. "
                "PHASE H PAID 2026-08-25 (owner: 'ok, h'): a device turn now "
                "streams its live transcript to the board, identical to a "
                "local card. spine/agent/drivers.py's _fold_timeline body was "
                "EXTRACTED to a module-level fold_timeline_event(run_dir, ev) "
                "(the method now delegates - test_timeline_store.py, which "
                "drives _fold_timeline via _on_event, still green, proving the "
                "local path is byte-identical), so BOTH a local pump and the "
                "device path fold through the same function, no fork. Worker: "
                "_run_turn_locally's streaming path switched to --output-"
                "format stream-json, reads events in a reader thread (avoids "
                "the PIPE-fill deadlock), forwards each batch to on_stream "
                "WHILE checking still_mine every poll; process_one POSTs them "
                "to POST /devices/<id>/stream. Daemon: dispatch.record_remote_"
                "stream folds each event via drivers.fold_timeline_event into "
                "the card's timeline, guarded to this device's own working "
                "card, best-effort (a bad event skipped, never raised) - so "
                "the existing board SSE/transcript_store_version renders a "
                "device turn live with ZERO board-side change. Pinned in "
                "test_remote_device.py (fold shape incl. tool_result patching "
                "the same step, the moved-card guard, the route) and "
                "test_hd_worker.py (streaming path forwards events + parses "
                "usage from the stream-json result; interrupt still works). "
                "UI SCREENSHOT+JUDGE DONE 2026-08-25 (owner: 'ok teste'): "
                "seeded a device card (exec_site local:, device_stale) + two "
                "sample devices into data/demo.ts, built the web export, "
                "served it SPA-style and drove Playwright (demo flag via "
                "localStorage) at phone + wide widths. JUDGED: the board's "
                "red 'device offline?' chip sits cleanly in the card's chip "
                "row (correct t.danger, no collision); the settings Devices "
                "panel shows both devices with own-account/shared billing "
                "chips + revoke, the stale-card reassign row (red warn + "
                "accent action), and register button, consistent with the "
                "neighbouring Users/Pair panels, no overflow at either width. "
                "One nit found + fixed in the same pass: last_seen rendered a "
                "raw ISO timestamp - now formatted to the minute. "
                "LIVE-WIRE VERIFIED 2026-08-25 (owner asked directly 'warum "
                "nicht testen' after the UI pass above deliberately skipped "
                "the live-streaming case): ops/tests/test_device_live_stream_"
                "http.py runs the REAL spine.http.server.H handler bound to a "
                "real socket (self-sandboxed data dir, no serve() boot side "
                "effects) and drives the exact wire contract a real worker + "
                "the real board use - POST /auth/login, POST /devices/"
                "register, GET /devices/<id>/queue, then the board's own "
                "blocking long-poll GET /tracks/<id>/transcript/live?v=. "
                "Proved over real HTTP + real threads, not asserted: the "
                "long-poll is genuinely blocked server-side (still alive "
                "1.5s in with nothing streamed), a device's POST /devices/"
                "<id>/stream unblocks it in 1.78s carrying the just-streamed "
                "text, the version token advances, and a second batch lands "
                "incrementally (both texts present, in order) rather than "
                "popping in once at the end. This is what the UI screenshot "
                "pass above could not reach (the static demo seam has no "
                "real daemon round-trip) - now closed for real, all phases "
                "A-H shipped, tested end to end including the live path, and "
                "the UI visually judged - this debt is PAID.",
        "why_it_bites": "PAID - all A-H phases shipped + tested, the device "
                        "UI screenshot-judged at phone + wide widths. The "
                        "one deliberately-separate follow-up left is bundle "
                        "size caps / large-binary handling on submit (a "
                        "transport concern, its own small card).",
        "trigger": "n/a (paid) - bundle size caps stay a separate follow-up",
        "fix": "PAID. Remaining separate follow-up (not this debt): cap/"
               "chunk large bundle uploads on /devices/<id>/submit.",
        "order": 0,
    },
    {
        "id": "gxp-signature-not-independently-verifiable",
        "title": "GxP signatures are recorded, but only WE can vouch for them",
        "status": "paid",
        "what": "The signature record was built and enforced, but it lived in "
                "helmdeck.db and events.jsonl - storage HelmDeck owns. An "
                "auditor asking 'how do I know this wasn't edited' got 'the "
                "append-only log', a convention rather than a proof.",
        "why_it_bites": "'A human signed this' was true and defensible; 'here "
                        "is cryptographic proof you can check yourself' was "
                        "not available.",
        "trigger": "a supplier audit that asks to independently verify a "
                   "signature, or any dispute about a specific release",
        "fix": "PAID (spine/auth/signkeys.py): every APPROVED signature also "
               "lands as a GPG-signed tag gxp/approve/<card>-<seq> on the "
               "approved commit; an auditor verifies with stock `git "
               "verify-tag` + the exported public key, no HelmDeck code in the "
               "loop. Per-user Ed25519 key whose passphrase IS the user's "
               "password (owner's Option A) - the password is cryptographically "
               "required, not merely hash-checked; agent passphrase caching is "
               "disabled (default/max-cache-ttl 0), because with the default "
               "cache a signature succeeded with the WRONG passphrase for as "
               "long as the agent remembered the right one. Password changes "
               "rotate the key; every public key ever used is exported "
               "append-only so old tags verify forever. GPG rather than the "
               "design's gpg.format=ssh because this host's git is 2.27 and "
               "ssh signing landed in 2.34 - same auditor story, different key "
               "format. STRICT: an approval whose tag cannot be created is "
               "refused outright, never stored unanchored. Proven end to end "
               "in ops/tests/test_gxp_tag.py against real gpg and real git.",
        "order": 0,
    },
    {
        "id": "events-two-stores-unreconciled",
        "title": "events.jsonl and the events table can drift, with no way to tell",
        "status": "paid",
        "what": "events.emit() appends to events.jsonl and separately write-through "
                "inserts the same row into the db (events.py:175-191). The db write "
                "is best-effort inside a bare `except: pass`. Rows carried no id, so "
                "the two stores shared no key and a dropped db write stayed dropped "
                "and silent forever - re-scanning to heal it could only either miss "
                "real drops or duplicate everything (the bug A4 fixed).",
        "why_it_bites": "A failed write-through loses the event from every "
                        "dashboard, metric and audit query while it still sits in "
                        "the file - the two answers to 'what happened' disagree and "
                        "nothing detects it. For a GxP audit trail that is fatal: "
                        "the record has to be provably complete, not probably.",
        "trigger": "any db write failure during emit (disk full, lock timeout, "
                   "WAL trouble) - was silent, now healed on the next boot",
        "fix": "PAID (phase D3, ops/docs/gxp-plan.md): events.emit() stamps a stable "
               "secrets.token_hex id on every row; the events table got a UNIQUE "
               "index on it (ALTER TABLE migration for existing installs, NULLs "
               "allowed for pre-id-era rows so they never collide); "
               "db.event_insert uses INSERT OR IGNORE keyed on it. "
               "db._reconcile_events() runs on every boot, not just the first: it "
               "scans events.jsonl from a byte-offset checkpoint (only what was "
               "appended since the last boot, not the whole history) and folds in "
               "anything the write-through missed. Verified against a genuinely "
               "dropped write (ops/tests/test_events_reconcile.py) - healed on the "
               "next reconcile, a repeat reconcile does not duplicate it, and the "
               "checkpoint advances so a clean boot rescans nothing.",
        "order": 0,
    },
    {
        "id": "henry-direct-hands",
        "title": "Henry acts on the live tree with no gate/isolation",
        "status": "open",
        "what": "Owner decree 2026-08-21 ('normal mode, not planning - do stuff "
                "directly'): the exception broker's judgement turn now spawns in "
                "a working permission mode (default acceptEdits, settings "
                "henry_permission_mode) with cwd at the repo root, and may fix "
                "escalations itself (action 'did') instead of only steering "
                "workers. No worktree, no gate, no review lane for those edits. "
                "NARROWED 2026-08-25 (owner: 'das soll ok sein, solange user is "
                "owner oder hat genuegend rechte'): henry_broker._decide now "
                "resolves the escalating card's dispatcher (dispatched_by, "
                "resolved fresh against the user registry, never the `client` "
                "billing label) via the new _dispatcher_privileged() and drops "
                "Henry to --permission-mode plan (no edits possible) for any "
                "card a `client`-role account filed, with a defense-in-depth "
                "refusal if 'did' comes back anyway - pinned in ops/tests/"
                "test_henry_privilege_gate.py. BOUNDED FURTHER 2026-08-25 "
                "(owner: 'ich sehe 1+2 [lock+baseline] und dann erstmal "
                "erledigt'): the privileged path now goes through "
                "_hands_on_ask, which (1) serializes against a direct/"
                "machine card on the SAME repo via the existing "
                "spine.git.locks._direct_lock_for (same primitive/bounded-"
                "wait as turnrunner.py's DIRECT handling - Henry and a "
                "direct card on this repo now queue instead of racing "
                "blind over each other) and (2) auto-commits a labelled "
                "baseline (_baseline_commit, no-ops on a clean tree) BEFORE "
                "Henry's hands touch anything, so a wrong fix always has a "
                "clean rollback point. Both pinned in ops/tests/"
                "test_henry_privilege_gate.py (lock contention + baseline "
                "commit/no-op behavior, real git). Still OPEN, scope "
                "narrowed to exactly one item: Henry's 'did' diffs are not "
                "surfaced in the History view - an owner can still only "
                "find out what changed by reading the audit note text or "
                "running git log/diff by hand outside the app.",
        "why_it_bites": "Without diff visibility, reviewing what an "
                        "unattended 'did' fix actually changed means "
                        "leaving the app - the History view (built for "
                        "exactly this after-the-fact review) shows the "
                        "audit note text but not the diff itself.",
        "trigger": "an owner wanting to review what Henry's last 'did' "
                   "actually touched",
        "fix": "Capture git diff <baseline>..HEAD (or the working-tree diff "
               "if _baseline_commit had nothing to snapshot) right after a "
               "'did' turn, in _decide/_execute, and surface it as a real "
               "field the History view can render - not just the free-text "
               "audit note _audit() already writes.",
        "order": -1,
    },
    {
        "id": "expo-cutover-pipeline",
        "title": "Old frontends archived, but build/ops/deploy/loop still point at them",
        "status": "paid",
        "what": "web/ (Next.js) and apk/ (Kotlin) were moved to archive/ when the "
                "single Expo app in app/ took over as the frontend for phone, web "
                "and desktop. But ops/deploy/push_relay.sh still builds+ships the "
                "Kotlin APK, ops/tools/loop_state.py's TEST/BUILD states + artifact "
                "map reference web/ and apk/, ops/tools/design_lint*.py and "
                "gen_tokens.py read web/app/globals.css (now archive/web/...), and "
                "there is no EAS cloud-build / OTA wired.",
        "why_it_bites": "The deploy script errors (missing apk path), the build "
                        "loop mis-detects state, and the design gate reads a "
                        "moved token file - the harness thinks it can ship when it "
                        "cannot.",
        "trigger": "next relay push, next loop_state run, next design gate",
        "fix": "PAID in two passes. 3114f6d: loop_state.py ARTIFACT_SRC repointed "
               "to the signed Expo APK (native-only sources; JS ships via OTA "
               "ops/deploy/push_update.sh), BUILD action -> release.sh/push_update.sh. "
               "This commit: design_lint + selftest retargeted to app/ (theme "
               "tokens via useTheme(), webstyles.tsx carries the web-shell "
               "color-scheme rule); gen_tokens.py declared the CANONICAL palette "
               "source (globals.css is history in archive/); loop_state's tsc "
               "check + design-mode hint repointed web/ -> app/. push_relay.sh "
               "had already been rewritten for the Expo APK, app/eas.json and "
               "the OTA path (push_update.sh, release.sh ota) already existed. "
               "Still open elsewhere: delete archive/ only at confirmed "
               "production parity.",
        "order": 0,
    },
    {
        "id": "chat-dedupe-window-in-memory",
        "title": "POST /chat idempotency window lives in daemon memory, and the relay still abandons a long turn at 120s",
        "status": "open",
        "what": "cells/copilot/chat_dedupe.py keeps its 10-minute claim ledger "
                "in a module-level list, so a daemon restart forgets every "
                "in-flight and recently-settled message. Separately, the fix "
                "closes the DUPLICATE but not the thing that provokes one: "
                "surfaces/relay/relay.py's REPLY_TIMEOUT (120s) and "
                "spine/comms/relay_client.py's _local() urlopen (115s) both "
                "still give up on a chat turn that runs longer than that, so "
                "the phone still sees a failed send on a 3-minute turn - it "
                "just no longer gets a second turn out of it.",
        "why_it_bites": "Restart inside the window -> one message can still be "
                        "answered twice (the exact bug this module exists to "
                        "stop). And a turn longer than ~2 minutes still ends "
                        "in a transport error on the phone, which is a bad "
                        "surface even when it is now harmless.",
        "trigger": "a daemon restart within 10 minutes of a chat send; or any "
                   "chat turn over ~2 minutes on the relay path",
        "fix": "Ledger: persist claims next to copilot_log.json (or in "
               "helmdeck.db) - cheap, the write is once per message. Timeout: "
               "make POST /chat return a turn id IMMEDIATELY (202) and let the "
               "existing /chat/live + /chat/history polling deliver the "
               "answer, so no transport layer is ever asked to hold a "
               "connection open for a model turn. That is the real ack fix; "
               "this commit only made the replay harmless.",
        "order": 1,
    },
    {
        "id": "turn-locks",
        "title": "No per-track turn locks",
        "status": "paid",
        "what": "Steers/dispatches run in threads with nothing serializing "
                "turns per card.",
        "why_it_bites": "Two simultaneous turns on one card (user + chain, or "
                        "two users) run `claude --resume` against the same "
                        "session; session ids rotate, one continuation is "
                        "orphaned, last_reply can be overwritten.",
        "trigger": "second concurrent user, or heavy chain automation",
        "fix": "threading.Lock per track id around _turn(); queue + visible "
               "'agent busy' state. NOTE: the lock alone DEADLOCKED - it wrapped "
               "an unbounded stdout read, so a stalled turn held it forever and "
               "every later steer hung. Superseded by the persistent stream-json "
               "session port (drivers._ClaudeSession, Paseo's model): run_turn is "
               "now BOUNDED (tree-kill on timeout) so the lock always releases.",
        "order": 1,
    },
    {
        "id": "json-storage",
        "title": "JSON-file storage (tracks/events/users)",
        "status": "paid",
        "what": "Every save is read-modify-write of a whole file from "
                "concurrent threads; the event log is re-parsed fully on "
                "every dashboard request.",
        "why_it_bites": "Lost updates under concurrent writes; dashboard cost "
                        "grows linearly with history forever.",
        "trigger": "multi-user writes, or months of events.jsonl",
        "fix": "SQLite (stdlib): tracks/events/users tables, transactions, "
               "indexed metrics queries. Import script for existing JSON.",
        "order": 2,
    },
    {
        "id": "polling",
        "title": "5s polling instead of push (SSE)",
        "status": "paid",
        "what": "Every client refetches all board data every 5 seconds.",
        "why_it_bites": "Staleness between users, wasted requests, and the "
                        "class of re-render bugs that polling forces the UI "
                        "to defend against.",
        "trigger": "more than one concurrent user who expects live state",
        "fix": "Server-Sent Events endpoint (works in ThreadingHTTPServer, "
               "cookie auth as-is); store patches state from events instead "
               "of refetching.",
        "order": 3,
    },
    {
        "id": "single-secret-transport",
        "title": "Plain HTTP on the LAN",
        "status": "paid",
        "what": "Credentials and session cookies travel unencrypted on the "
                "local network.",
        "why_it_bites": "Any hostile device on the same network can read "
                        "them.",
        "trigger": "first remote/client access from outside a trusted LAN",
        "fix": "PAID. Remote paths were already TLS: relay behind nginx+"
               "certbot (E2EE-sealed frames on top), Cloudflare Tunnel script "
               "for the API. This card closed the LAN + enforcement gaps: "
               "(1) server.serve() grows a native https listener - cert/key "
               "via HELMDECK_TLS_CERT/KEY, settings.tls, or auto-detected "
               "daemon/certs/ (minted by ops/tools/make_tls_cert.py; trusted "
               "certs via `tailscale cert`) - and with TLS on, plain http "
               "binds LOOPBACK-ONLY (local tooling keeps working, nothing "
               "cleartext leaves the machine; a broken TLS config also stays "
               "loopback-only instead of downgrading). (2) Pairing links "
               "embed a live device token in a URL, so relay_client."
               "insecure_url() now refuses plain-http non-loopback relay "
               "URLs at BOTH seams: /settings save and pairing_payload(); "
               "an existing http config logs a one-time warning but keeps "
               "bridging (frames are E2EE regardless). Remaining relatives "
               "stay listed separately: [android-cleartext-lan], "
               "[pair-token-no-ttl].",
        "order": 4,
    },
    {
        "id": "vanilla-fallback-ui",
        "title": "Legacy single-file UI still served at :8140",
        "status": "paid",
        "what": "The pre-Next fallback UI shares no code with web/ and drifts.",
        "why_it_bites": "Confusion (features missing there look like bugs) - "
                        "already caused one 'I don't see it' incident.",
        "trigger": "anyone opening :8140 expecting the real UI",
        "fix": "PAID: server.py drops the BOARD/DASH/PAGE templates and "
               "daemon/ui/app.html; /, /classic, /recorder and /dashboard "
               "302 to the Next app (settings.web_url, default "
               "http://localhost:3300).",
        "order": 5,
    },
    {
        "id": "nightshift-limit-sniff",
        "title": "Night shift detects usage limits by string-matching replies",
        "status": "paid",
        "what": "pm._limit_hit() (moved from the removed nightshift.py) greps the card's last_reply for "
                "'usage limit'/'rate limit' instead of reading a structured "
                "error from the driver.",
        "why_it_bites": "A rephrased CLI error means the night shift keeps "
                        "starting cards into a dead quota; a false match in a "
                        "legitimate reply stops it early.",
        "trigger": "Claude CLI changing its limit wording, or a card whose "
                   "reply merely mentions rate limits",
        "fix": "PAID: drivers surfaces the stream-json result event's subtype/"
               "is_error/errors into meta; _record_turn stores last_subtype/"
               "last_error on the track; _limit_hit reads those STRUCTURED fields "
               "first (prose grep only for legacy turns). Remaining: match the "
               "exact usage-limit subtype string once observed from a real limit.",
        "order": 6,
    },
    {
        "id": "session-restart-on-optchange",
        "title": "Model/mode/tool change kills & resumes the session (no control plane)",
        "status": "paid",
        "what": "A steer with a different model/permission-mode/allowed-tools than "
                "the running process tree-killed it and respawned; Stop hard-killed.",
        "why_it_bites": "A respawn re-pays session init/context cost and briefly "
                        "drops streaming; rapid model-toggling churns processes.",
        "trigger": "owner flipping model/mode often mid-conversation on one card",
        "fix": "PAID: drivers._ClaudeSession speaks the CLI's stream-json control "
               "plane (verified against the real binary): model/mode changes apply "
               "LIVE via set_model / set_permission_mode; Stop sends a soft "
               "interrupt first (process stays alive, resumable) and only "
               "tree-kills as a fallback. Only an allowed-tools change (no live "
               "control subtype) or a failed control op still forces a respawn.",
        "order": 7,
    },
    {
        "id": "orphan-reap-pid-reuse",
        "title": "Startup orphan reap trusted a bare recorded PID list",
        "status": "paid",
        "what": "reap_orphans() tree-killed PIDs recorded by a prior daemon, "
                "guarded only by a tasklist image-name check (claude/node/cmd).",
        "why_it_bites": "A recycled PID that happens to be an unrelated node/cmd "
                        "process could be killed after a crash-restart.",
        "trigger": "daemon crash-restart on a busy machine with heavy PID churn",
        "fix": "PAID: driver_pids.json now records (pid -> spawn epoch); reap_orphans "
               "matches the OS-reported process start time (PowerShell Get-Process "
               "StartTime) against the recorded spawn within 6s - a recycled pid "
               "shows a later start and is skipped. Image-name guard remains only "
               "as a fallback when the start time can't be read.",
        "order": 8,
    },
    {
        "id": "accept-merge-base-branch",
        "title": "Accept merges into the checkout's CURRENT branch, ff-agnostic",
        "status": "open",
        "what": "TWO halves, one shared missing record. HALF A (base INTO the "
                "card) is now PAID: dispatch._record_base_branch folds "
                "base_branch onto the card at branch creation, and "
                "lanemachine._sync_base merges that base into the card's "
                "worktree between _autocommit and _gate - so a card is gated "
                "against the code it will land beside, not the code it forked "
                "from. That merge-base-INTO-the-card direction was the "
                "undocumented gap behind debt gate-base-lag. HALF B is still "
                "OPEN: move_lane('done') merges the card branch via "
                "_merge_to_main into whatever branch t['repo'] currently has "
                "checked out, NOT into the recorded base_branch. It also always "
                "--no-ff and does not push.",
        "why_it_bites": "If the owner leaves the main checkout on a different "
                        "branch, an accept would merge into the wrong target (the "
                        "guard only refuses detached HEAD or the card branch "
                        "itself). Worse now that half A exists: the gate would "
                        "have synced against the RECORDED base while the merge "
                        "lands into the CHECKED-OUT one, so green stops proving "
                        "mergeable. No push means 'deployed' still depends on a "
                        "deploy hook to publish.",
        "trigger": "a repo whose cards fork from a non-default base, or a checkout "
                   "parked on a feature branch at accept time",
        "fix": "PAID (half A): base_branch recorded at dispatch, verified (never "
               "trusted) by _base_branch, and merged in by _sync_base before the "
               "gate; a sync conflict bounces to Review with editable markers "
               "instead of a mystery-red gate. REMAINING (half B): merge into "
               "THAT recorded base (checking it out / using a dedicated "
               "integration worktree), offer ff-only vs --no-ff by policy, and "
               "push when the repo is remote.",
        "order": 9,
    },
    {
        "id": "android-cleartext-lan",
        "title": "Android APK enables global cleartext for the direct-LAN feature",
        "status": "paid",
        "what": "The app's 'direct LAN' option (More > http://<daemon>) could never "
                "work on a release build: targetSdk>=28 blocks cleartext HTTP by "
                "default, so every http:// daemon URL failed with 'Desktop nicht "
                "erreichbar'. Fixed by adding android:usesCleartextTraffic=\"true\" "
                "to surfaces/app/android/app/src/main/AndroidManifest.xml - but that file is "
                "gitignored/hand-managed (not driven by app.json), so the flag is "
                "invisible to git and a future `expo prebuild` would silently drop "
                "it (along with the hand-added expo-updates meta-data).",
        "why_it_bites": "usesCleartextTraffic=true permits plaintext HTTP to ANY "
                        "host app-wide, not just private LAN IPs - a mild security "
                        "downgrade (the relay path stays HTTPS/sealed). And the "
                        "whole native config can vanish on a prebuild.",
        "trigger": "next `expo prebuild`, or a security review of the APK",
        "fix": "PAID: app/plugins/withLanCleartext.js scopes cleartext via a "
               "network-security-config (base-config blocks cleartext; a "
               "domain-config allows it only for 10.0.2.2/localhost/127.0.0.1 "
               "plus the hosts listed in the app.json plugin entry - Android NSC "
               "cannot express RFC1918 ranges, so the scope is an explicit host "
               "list; a real phone's direct-LAN mode needs the PC's IP added "
               "there + an APK rebuild). One file drives BOTH build paths: expo "
               "config plugin for a future prebuild, and a bare-node CLI that "
               "ops/deploy/build_apk.sh runs against the hand-managed surfaces/app/android "
               "before every gradle build, replacing the invisible global flag. "
               "expo-updates is now an explicit app.json plugin, so its "
               "meta-data survives a prebuild too. "
               "Relates to [single-secret-transport].",
        "order": 10,
    },
    {
        "id": "pair-token-no-ttl",
        "title": "Pairing device-token outlives the 15-min pairing window",
        "status": "open",
        "what": "Each /relay/pair click mints a device bearer token with no "
                "expiry. The single-use PAIR_TTL window (relay_client._admit) "
                "gates the E2EE pin - the relay path of an unused code dies "
                "with the window - but the token inside the code stays a live "
                "credential.",
        "why_it_bites": "A leaked pairing code that was never used can still "
                        "authenticate over direct LAN mode ({b,t} style) even "
                        "after the window expired. Tokens are at least visible "
                        "and revocable per user in Settings -> users.",
        "trigger": "owner generates codes and abandons them; a code lands in "
                   "chat history/screenshots and someone on the LAN finds it",
        "fix": "Give pairing-issued tokens a TTL and auto-revoke unused ones "
               "when the window closes, or bind the token to the pinned "
               "device pub at admission time.",
        "order": 11,
    },
    {
        "id": "machine-task-blast-radius",
        "title": "Machine tasks run with bypassPermissions over the whole PC",
        "status": "open",
        "what": "sessions.new_machine_task dispatches a card whose workplace is a "
                "real folder on the owner's machine, with permission mode "
                "bypassPermissions (policy.machine.perm) and no path restriction "
                "by default (policy.machine.roots = []). Headless is why: an "
                "unanswerable permission prompt IS a block, and blocking was the "
                "bug being fixed. Guards in place: owner-only role gate "
                "(policy.machine.roles), the capability switch "
                "(policy.machine.enabled), full audit (machine/turn/done events + "
                "flight recorder), no merge path, and a brief that fences "
                "HelmDeck's own secrets.",
        "why_it_bites": "An agent turn that misreads its instruction can change or "
                        "delete anything the owner's account can reach, and unlike "
                        "a worktree card there is no branch to roll back - the "
                        "per-turn checkpoint is deliberately skipped for machine "
                        "cards. The prompt-level secret fence is guidance, not "
                        "enforcement.",
        "trigger": "the first machine task pointed at a broad folder (home, C:\\), "
                   "or a second non-owner account being granted machine roles",
        "fix": "Narrow by default: ship policy.machine.roots preset to the owner's "
               "usual work folders and require an explicit widening; add a "
               "recycle-bin-style undo (move-to-trash instead of delete) for "
               "machine file operations; enforce the secret fence in code (deny "
               "reads of settings.json/users.json/helmdeck.db via allowed_tools "
               "deny-rules) rather than in the brief; consider a dry-run turn that "
               "reports the plan before the acting turn for destructive verbs.",
        "order": 12,
    },
    {
        "id": "pm-loopstate-races",
        "title": "PM loopstate is read-modify-write from tick + resolve threads",
        "status": "open",
        "what": "pm.py's resolve threads (_bump_attempt/_give_up) serialize their "
                "own writes to pm/loop.json behind _resolving_lock, but the tick "
                "thread (_notify_deliveries, _dispatch_next, make_plan) still does "
                "unlocked read-modify-write of the same file with state read "
                "earlier in the tick.",
        "why_it_bites": "A tick save landing between a resolve thread's write and "
                        "the next read can revert an attempt counter or a "
                        "notified flag - worst case one duplicate owner ping or "
                        "one extra (harmless, rate-limited) delegation attempt.",
        "trigger": "a resolve thread finishing in the same second a tick saves "
                   "loopstate; more likely once several cards resolve in parallel",
        "fix": "Route ALL loopstate mutations through one locked helper that "
               "re-reads inside the lock (the _bump_attempt pattern), or move "
               "loopstate into the sqlite DB like tracks.",
        "order": 13,
    },
    {
        "id": "browser-attach-real-chrome",
        "title": "Standard agent browser attaches to a real, logged-in Chrome",
        "status": "open",
        "what": "browsercap.AgentBrowser now defaults to attach=True: it launches "
                "(or reuses) a persistent Chrome with a debug port + a dedicated "
                "HelmDeck user-data-dir and drives it over CDP. That profile holds "
                "real extensions and logged-in sessions (the whole point - so an "
                "agent can use Claude-for-Chrome etc.), and the agent gets a page "
                "handle in it. Guards in place: it is a SEPARATE profile (not the "
                "owner's daily user-data-dir), the browser is visible and fully "
                "screen-recorded (wincap screen.mp4 + live.jpg) beside the audited "
                "action timeline, close() never kills the owner's browser, and "
                "invocation stays behind the machine capability (policy.machine, "
                "owner role) like any other PC-touching work.",
        "why_it_bites": "An agent turn can act inside a browser carrying the "
                        "owner's cookies/OAuth sessions - it can read or send as "
                        "the owner on any site that profile is logged into, with no "
                        "per-site consent and no branch to roll back. The profile "
                        "separation is a convention, not a sandbox; nothing yet "
                        "restricts which origins the agent may drive.",
        "trigger": "the first card that opens the standard browser against a "
                   "sensitive logged-in site (mail, bank, cloud console), or the "
                   "debug port being reachable by another local process",
        "fix": "Gate origins: an allowlist of hosts the agent browser may navigate "
               "(deny the rest at goto()); bind the debug port to loopback with a "
               "per-run token; add a dry-run/confirm turn for state-changing "
               "actions on allowlisted-but-sensitive sites; surface the active "
               "origin in the live glance feed so the owner sees where it is.",
        "order": 14,
    },
    {
        "id": "ask-protocol-prompt-compliance",
        "title": "The question channel depends on prompt compliance + one repair turn",
        "status": "open",
        "what": "Phase 2.4 routes a worker's open question to real option buttons by "
                "TEACHING it a <helmdeck-ask> block (ask.BRIEF) and parsing that out "
                "of the reply. Measured against the shipped CLI (2.1.207), the system "
                "prompt alone is NOT reliably followed - the worker wrote prose and "
                "parked in both live probes - so sessions._repair_question spends one "
                "extra turn asking it to restate the question in protocol form, gated "
                "by the ask.looks_like_question heuristic. There is no hard channel "
                "(AskUserQuestion is not offered headless and no can_use_tool "
                "control_request is ever sent, so it cannot be intercepted).",
        "why_it_bites": "Two soft edges. (1) The heuristic decides when to spend a "
                        "turn: a false positive costs one short turn (the worker "
                        "answers NOQUESTION), a false negative silently falls back to "
                        "the old prose-and-park behaviour - so coverage is good but "
                        "not total. (2) If a future CLI or model drifts further from "
                        "the instruction, the repair turn is the only thing holding "
                        "the feature up, and its cost scales with every parked turn "
                        "that looks like a question.",
        "trigger": "a CLI/model upgrade changing instruction-following, or the repair "
                   "turn showing up as a noticeable share of spend on the turn events",
        "fix": "Re-run ops/tests/probe_cli_askuser.py against the new CLI: once it exposes "
               "AskUserQuestion (or sends control_request/can_use_tool to a "
               "stream-json client), replace the taught protocol with a real "
               "interception + park/respond, which needs no compliance and no repair "
               "turn. Until then, track the askrepair events to see how often the "
               "fallback carries the feature.",
        "order": 15,
    },
    {
        "id": "auto-compaction-disabled",
        "title": "Proactive context compaction was off - misdiagnosed, re-enabled in place",
        "status": "paid",
        "what": "sessions._maybe_compact injects a '/compact' turn at ~80% context "
                "to summarise the session in place. It was disabled 2026-08-10 "
                "(a0853d4) after the 'Fix AI-Kosten-Tracking' incident, blamed for "
                "corrupting the session tip so a later --resume silently started "
                "fresh, and for a shrink-check that read summed usage instead of "
                "compacted context.",
        "why_it_bites": "Turned out to be a misdiagnosis: both blamed causes were "
                        "ALREADY fixed by other commits before the disable landed. "
                        "The shrink-check bug was fixed by 66930bb (2026-08-08, TWO "
                        "DAYS earlier) when ctx_tokens moved to last-call-only usage "
                        "(meta.ctx_usage). The '--resume silently started fresh' "
                        "symptom was the claude.cmd shim eating the trailing "
                        "'--resume <sid>' arg under `cmd /s /c` - ANY --resume could "
                        "silently miss, not just a post-compact one - fixed by "
                        "ea09780 (2026-08-10, the SAME DAY, 5h after the disable). "
                        "Compaction was never actually the corruption source; it "
                        "just happened to be the session in front of the shim bug "
                        "when it was caught.",
        "trigger": "n/a - re-enabled 2026-08-14",
        "fix": "PAID: restored _maybe_compact's original self-verifying logic "
               "(probe /compact once, learn True/False from whether ctx_tokens "
               "actually shrank by >=25%) instead of building fork-based "
               "compaction - forking a session to compact it was solving a "
               "corruption problem that had already been fixed elsewhere. "
               "accept-and-rebind (_finish_turn, Weg B) stays as-is: it is a "
               "correct safety net for ANY unresumable session, not just a "
               "post-compact one, so it remains regardless of compaction. Watch "
               "the AUTO-COMPACT log line on the next card that crosses 160k to "
               "confirm this CLI still honors /compact in-place.",
        "order": 16,
    },
    {
        "id": "henry-memory-parallel-to-cli-automemory",
        "title": "Henry's memory is a SECOND surface beside the CLI's own auto-memory",
        "status": "open",
        "what": "Henry writes durable facts to daemon/henry_memory/ - a plain .md "
                "directory plus an index, injected each turn by copilot._memory_digest "
                "and instructed in cells/copilot/harness/agents/board-copilot.md. The claude CLI "
                "ALSO carries its own auto-memory directory under ~/.claude/projects/**. "
                "We deliberately did not build on that one: harness._memory_isolation "
                "records that the CLI derives its path from a project identity which "
                "measurably is NOT 'this cwd' (every card worktree measured shared ONE "
                "directory keyed off something else), and guessing that derivation is "
                "the unverified reconstruction CLAUDE.md forbids.",
        "why_it_bites": "Henry is a Claude Code process WITH HANDS (acceptEdits). "
                        "Nothing stops him from also using the CLI's native memory if "
                        "it is active for his project - and then two stores exist while "
                        "only ours rides in his turn, because the digest reads our index "
                        "alone. A fact written to the other one is invisible to him "
                        "afterwards, and nothing reconciles them. The failure is silent: "
                        "no error, just an assistant that forgot something it did write.",
        "trigger": "A `memory/` directory appearing under "
                   "~/.claude/projects/C--Users-*-swarmdeck-daemon/, any report of Henry "
                   "remembering inconsistently, or the CLI documenting/exposing how it "
                   "derives the auto-memory path.",
        "fix": "Consolidate onto ONE surface once the CLI's derivation can be MEASURED "
               "rather than guessed - either point the digest at the CLI's directory, or "
               "tell Henry in the brief never to use the native one. The second is cheap "
               "and could land today; it was left out on purpose, because writing a "
               "prohibition against a mechanism we have not yet observed him using would "
               "be the same guessing this entry exists to avoid.",
        "order": 51,
    },
    {
        "id": "autocompact-probe-process-global",
        "title": "The '/compact is supported' probe is ONE process-wide flag for the whole board",
        "status": "open",
        "what": "sessions._autocompact_supported is a module global: the FIRST "
                "card whose compaction fails to shrink turns auto-compaction off "
                "for EVERY card in the daemon, until a restart. It is never "
                "re-probed and it is not keyed by driver, model or CLI version, "
                "even though a board can run several drivers side by side.",
        "why_it_bites": "Measured 2026-08-30 (card 20260830-065545): the owner "
                        "typed while the harness was auto-compacting, steer()'s "
                        "interrupt-and-replace killed the maintenance turn, and "
                        "the unchanged context was read as 'this CLI doesn't "
                        "honor /compact' - latching the global False. One "
                        "interrupted turn on one card silently disabled "
                        "compaction board-wide, and the 92%-full session it was "
                        "trying to save was never compacted again.",
        "trigger": "A second driver type (codex/opencode/omp) in real use, or any "
                   "further report of auto-compaction 'just stopping' on cards "
                   "that never ran one themselves.",
        "fix": "Narrowed, not closed. The interrupt half IS fixed (an interrupted "
               "compaction now teaches nothing and re-queues onto the card via "
               "compact_pending, retried by the reconciler's "
               "sweep_pending_compaction), so the flag can only be set by a turn "
               "that actually RAN to completion - which is what the probe always "
               "meant to measure. Remaining: key the learned verdict by driver "
               "name + model instead of one process global, and let it decay so a "
               "CLI upgrade is re-probed rather than staying off until a restart.",
        "order": 16,
    },
    {
        "id": "ai-billing-workspace-global",
        "title": "AI billing mode (flat vs metered) is one workspace-wide switch",
        "status": "open",
        "what": "events.ai_billing() maps settings.pm.plan to a single display "
                "contract for the WHOLE board: 'max' -> flat (cost surfaces show "
                "tokens, margins skip the phantom $), anything else -> metered. "
                "plan='auto' (default) detects the mode from the ONE workspace "
                "login (usage.login_method: subscription OAuth -> flat, API key / "
                "Console login -> metered) and assumes Claude Code's precedence - "
                "a stored login always wins over an env ANTHROPIC_API_KEY, since "
                "the CLI only uses a key the owner explicitly approved "
                "(customApiKeyResponses), which we cannot see from outside. "
                "Every turn still gets priced (measured economics untouched); only "
                "the rendering and margin math read the switch. Per-card billing "
                "does not exist because every driver shells out to the same "
                "claude CLI login today.",
        "why_it_bites": "On a 'mixed' plan (some turns on the Max quota, some on "
                        "API keys) the switch falls back to metered for everything: "
                        "flat cards then show $-amounts again - the exact bug this "
                        "fixed, now only for the flat half of the fleet. There is "
                        "no per-turn record of WHICH plan billed it, so the split "
                        "cannot be reconstructed from events.jsonl later.",
        "trigger": "adding a driver with its own API key, setting "
                   "settings.pm.plan = 'mixed', or the owner approving an env "
                   "API key inside Claude Code while a subscription login "
                   "exists - auto would keep saying flat while turns bill cash",
        "fix": "Stamp the billing mode per TURN at record time (sessions._record_econ "
               "writes meta into the turn event; add billing='flat'|'metered' from "
               "the driver's auth source), roll it up per card in events.metrics(), "
               "and let the UI render each card by its own mode instead of the "
               "workspace switch.",
        "order": 17,
    },
    {
        "id": "plan-share-calibration",
        "title": "Plan share is calibrated from OUR tokens against the ACCOUNT's quota",
        "status": "open",
        "what": "events.plan_calibration() turns a card's tokens into '% of the "
                "subscription' by dividing the tokens this board burned inside the "
                "live weekly window by that window's utilization "
                "(tokens_per_pct = tokens_in_window / used_pct). Anthropic's usage "
                "endpoint publishes a percentage and never the absolute limit, so "
                "there is no exact allowance to divide by. "
                "settings.pm.plan_tokens_week overrides it when the owner knows the "
                "real number.",
        "why_it_bites": "used_pct covers the WHOLE Anthropic account - Claude Code in "
                        "the owner's own terminal, claude.ai, other machines - while "
                        "the numerator counts only turns this daemon recorded. Every "
                        "token spent outside HelmDeck shrinks the implied allowance "
                        "and inflates every card's percentage proportionally: burn "
                        "half the quota outside the board and cards read ~2x their "
                        "true share. It is an estimate, rendered with '~', never an "
                        "invoice.",
        "trigger": "owner reports card percentages that don't reconcile with the "
                   "usage panel, or heavy Claude use outside HelmDeck",
        "fix": "Use the absolute window limit if the usage endpoint ever exposes one; "
               "otherwise track the delta (account utilization minus our recorded "
               "tokens) as an explicit 'outside this board' slice, show it in the "
               "usage panel, and calibrate against the remainder.",
        "order": 18,
    },
    {
        "id": "legacy-outcome-on-read",
        "title": "Pre-outcome done cards get their outcome derived at snapshot READ time",
        "status": "paid",
        "what": "Cards accepted before the outcome field existed never ran "
                "sessions._record_outcome, so copilot._snapshot fell back to "
                "sessions.extract_outcome(last_reply) on every read for lane=done "
                "cards without a stored outcome. New accepts persist outcome at "
                "EVENT TIME (the accept mutators) - only the legacy tail was "
                "reconstructed on read.",
        "why_it_bites": "Read-time derivation is the reconstruction pattern the "
                        "Paseo law bans for load-bearing state: if extract_outcome's "
                        "heuristics change, a legacy card's remembered result "
                        "silently changes with them, and the PM plan reads it.",
        "trigger": "editing extract_outcome, or auditing why an old done card's "
                   "snapshot line differs from its actual final reply",
        "fix": "PAID (card chat-fix--sessions-extract-ou, 2026-08-12): "
               "sessions.backfill_outcomes() stamps outcome once at daemon start "
               "(key presence = migrated, so '' is a valid stamp); the read-time "
               "fallback in copilot._snapshot is gone. NOT adopted blindly: an "
               "agent reviewed all 44 legacy done cards against their full final "
               "replies - 19 heuristic misses (aside-first replies like chatfork, "
               "merge-meta, junk) carry hand-written values in "
               "sessions._OUTCOME_BACKFILL_REVIEWED.",
        "order": 19,
    },
    {
        "id": "gate-exit-code-vs-stdout-verdict",
        "title": "sessions._gate trusts run_gate.py's PRINTED verdict over a "
                 "contradicting OS-level returncode",
        "status": "open",
        "what": "Card 20260812-164257 (chat-fix--sessions-extract-ou) bounced with "
                "gate_report = 'gate FAILED:' wrapping a body of 34 straight 'ok' "
                "lines ending in ops/tools/run_gate.py's own 'gate: PASS (34 checks)' - "
                "i.e. r.returncode was nonzero even though the script ran every "
                "check green and reached its own sys.exit(0). Replaying the exact "
                "daemon invocation by hand (same HELMDECK_REPO, same cwd, same "
                "cmd.exe shell=True path) came back clean both times - not "
                "reliably reproducible on demand. No HKCU/HKLM Command Processor "
                "AutoRun hook is set (ruled out as a cause). _gate() now regexes "
                "stdout for run_gate.py's own success sentinel and trusts it over "
                "a disagreeing returncode, logging the mismatch to the daemon "
                "console instead of bouncing the card on it.",
        "why_it_bites": "A card can get bounced by the gate for NOTHING - its own "
                        "work is fully green - which reads as a random, "
                        "unexplained rejection and burns a review cycle. The fix "
                        "closes the symptom but is itself a text-match heuristic "
                        "over run_gate.py's print format (fragile if that format "
                        "changes without updating _GATE_PASS_RE) standing in for "
                        "an OS/shell-level root cause that was never pinned down.",
        "trigger": "another card bounces with a gate_report whose body is all "
                   "'ok' lines ending in 'gate: PASS (...)', or editing "
                   "run_gate.py's PASS/FAIL print strings without updating "
                   "sessions._GATE_PASS_RE alongside them",
        "fix": "Pin the real root cause (capture r.returncode AND stdout/stderr "
               "on every gate run, not just failures, so the next occurrence has "
               "full evidence instead of a single anecdote) - most likely "
               "candidates: a Windows shell=True/cmd.exe exit-code relay glitch "
               "under load, or a py-launcher subprocess-teardown quirk. Once "
               "pinned, fix at the source and drop the stdout-verdict override.",
        "order": 20,
    },
    {
        "id": "ios-submit-local-asc-key",
        "title": "eas submit reads the ASC key from a path only THIS box has",
        "status": "open",
        "what": "app/eas.json submit.production.ios pins ascApiKeyPath + "
                "ascApiKeyId + ascApiKeyIssuerId, and the path points at "
                "C:/hd/secrets/AuthKey_*.p8 on the owner's machine. Chosen "
                "because eas-cli's other route - storing the key on EAS' servers "
                "via SetUpAscApiKey - has no non-interactive mode "
                "(AscApiKeySource.js: 'App Store Connect API Keys cannot be set "
                "up in --non-interactive mode'), and a card cannot drive a TTY "
                "prompt. The .p8 itself is NOT committed and stays outside the "
                "repo; only its path and the two non-secret identifiers are, and "
                "those two are already published in DEPLOY.md 2b.",
        "why_it_bites": "eas submit works on the owner's box and nowhere else - "
                        "another machine, a fresh clone or a CI runner dies on a "
                        "missing .p8. It also quietly breaks the rule the rest of "
                        "iOS signing follows (DEPLOY.md 2b: the signing assets "
                        "live in EAS, not in the repo), so the next reader "
                        "reasonably assumes submit is portable when it is not.",
        "trigger": "any eas submit from a different machine or a clean clone, or "
                   "the day the .p8 is moved or rotated",
        "fix": "One interactive `npx eas-cli credentials -p ios` from cmd.exe / "
               "Windows Terminal (NOT Git Bash - MinTTY gives node no TTY, the "
               "trap already written down in DEPLOY.md 2b), uploading the .p8 as "
               "the SUBMISSION_SERVICE key. Then delete the three ascApiKey* "
               "fields from eas.json: getAscApiKeyFromCredentialsServiceAsync "
               "then resolves the key from EAS and every submit is unattended "
               "from anywhere - the same one-time-then-forever shape the "
               "distribution certificate already has.",
        "order": 21,
    },
    {
        "id": "mac-build-never-executed",
        "title": "The macOS build target is configured but has never actually run",
        "status": "paid",
        "what": "surfaces/desktop/electron-builder.yml now carries a full mac target "
                "(dmg + zip, arm64 + x64, hardened runtime, entitlements, gated "
                "notarization), surfaces/desktop/build-mac.sh drives it and "
                ".github/workflows/desktop-mac.yml runs it on macos-14. NONE of "
                "it has been executed on macOS. The evidence behind it is: the "
                "config validates against electron-builder's own scheme.json "
                "(both the committed shape and the notarize-object override), "
                "and electron-builder 25.1.8 loads the file and then stops at "
                "exactly one line - 'Build for macOS is supported only on "
                "macOS'. That is the strongest signal a Windows box can "
                "produce, and it is still not a build. Two reasons it could go "
                "no further: there is no macOS here, and at the time "
                "github.com/Tienduyvo/helmdeck held ONLY README.md + release "
                "assets - the source had never been pushed, so no runner had "
                "anything to check out. That half is now DECIDED (owner, "
                "2026-08-14: one repo - the source goes into the same public "
                "repo as the builds) and tooled: ops/deploy/publish_source.sh "
                "audits history for secrets and oversized blobs and pushes "
                "main. Still nobody has run it, and no runner has run.",
        "why_it_bites": "A green-looking config is not a green build. What a "
                        "schema cannot catch: whether `expo export` survives a "
                        "cold macOS runner, whether hdiutil produces both dmgs, "
                        "whether the PNG->icns conversion accepts our icon, "
                        "whether the entitlement set is the RIGHT one for "
                        "spawning python3/claude under the hardened runtime "
                        "(only a notarized run on real hardware proves that), "
                        "and whether Squirrel.Mac accepts the zip feed. Each is "
                        "a separate way the first real run can red, and none is "
                        "visible until someone runs it.",
        "trigger": "the first `bash ops/deploy/publish_source.sh`, or the first "
                   "`bash surfaces/desktop/build-mac.sh` on any Mac",
        "fix": "PAID 2026-08-15 - run 31877006863 on macos-14, 32m10s, ALL "
               "STEPS GREEN. Every question this item said only a real run "
               "could answer is now answered by that run's log: expo export "
               "survived a cold runner; hdiutil produced BOTH dmgs (imageinfo "
               "passed on each); the tracked PNG converted to .icns with no "
               "Pillow/iconutil; and the entitlement set IS the right one - "
               "build-mac.sh reported 'signing: Developer ID identity "
               "supplied' then 'notarization: ON', electron-builder logged "
               "'notarization successful' TWICE (once per arch), and the "
               "verify step closed it out: `codesign --verify --deep --strict` "
               "-> 'valid on disk' + 'satisfies its Designated Requirement', "
               "`spctl --assess --type execute` -> 'accepted' with "
               "'source=Notarized Developer ID'. Gatekeeper accepts the "
               "artifact. Full set produced: HelmDeck-0.2.0-{arm64,x64}.{dmg,"
               "zip} + blockmaps + latest-mac.yml (the Squirrel.Mac feed). "
               "Benign log noise NOT to 'fix': electron-builder prints "
               "'Please specify notarization Team ID in the APPLE_TEAM_ID env "
               "var instead of notarize.teamId'. The -c.mac.notarize.teamId "
               "override is still what TURNS NOTARIZATION ON (the committed "
               "config keeps notarize:false so an unsigned build can succeed); "
               "the warning is only about where the team id is read from, and "
               "notarization demonstrably worked. Getting there first needed: "
               "1) bash ops/deploy/publish_source.sh (audit + push over SSH - the "
               "gh token has no `workflow` scope, so HTTPS is rejected). "
               "2) The push to the remote DEFAULT branch auto-triggers the "
               "workflow (push: branches: [main]); otherwise Actions -> "
               "desktop-mac -> Run workflow. Flip to 'paid' only when a run "
               "produced all four artifacts + latest-mac.yml and the workflow's "
               "verify step (hdiutil imageinfo per dmg) passed, then confirm "
               "`spctl --assess` accepts the signed app. "
               "SIGNING AND NOTARIZATION ARE NO LONGER BLOCKERS (2026-08-15): "
               "all SIX secrets are live and VALIDATED on the repo - "
               "MAC_CSC_LINK + MAC_CSC_KEY_PASSWORD (cert read back with "
               "openssl: Developer ID Application, issuer G2, EKU Code Signing, "
               "to 2031-08-16, team 92WJZQ2WWH matching APPLE_TEAM_ID) plus "
               "ASC_API_KEY_P8 + ASC_KEY_ID + ASC_ISSUER_ID + APPLE_TEAM_ID, "
               "the ASC key proven to authenticate against Apple live via "
               "mac_credentials.py --check. See DEPLOY.md 1c. A run with NO "
               "secrets is still designed to pass unsigned, so that remains "
               "the fallback smoke test if the signed path reds. What is left "
               "is purely step 1, and it is now blocked on an OWNER DECISION "
               "rather than on tooling: publish_source.sh's new privacy check "
               "fails closed on five .attachments/ chat uploads (the owner's "
               "phone screenshots of his own board - unreleased card titles, "
               "due dates, distribution decisions) that a push would make "
               "public forever, since a push publishes history. Resolve with "
               "either HELMDECK_PUBLISH_ALLOW_PRIVATE=1 (publish them "
               "knowingly) or a git_filter_repo purge of the pushed lineage. "
               "Note also that the trunk is `expo-migration`, NOT `main` - "
               "`main` is a stale 2026-08-12 branch with no .github/ at all.",
        "order": 22,
    },
    {
        "id": "desktop-lock-heuristic",
        "title": "Desktop-control mutual exclusion is a substring match + in-process lock",
        "status": "open",
        "what": "Only one card may hold real Windows desktop control (mouse/"
                "keyboard/screen via windows-mcp) at a time - two such turns "
                "racing would fight over the same cursor. sessions._turn() "
                "guards this with a plain in-process threading.Lock "
                "(_desktop_lock), acquired non-blocking for the synchronous "
                "duration of any turn whose driver's allowed_tools grant a "
                "windows-mcp CONTROL tool (_uses_desktop_control: a wildcard "
                "grant or any tool not on the read-only screen allowlist "
                "{Screenshot,Snapshot,Scrape,DisplayInventory} - fail-safe so "
                "an unknown/new tool still locks; narrowed 2026-08-17 from the "
                "old raw 'windows-mcp' substring match that made a passive "
                "Screenshot card take the exclusive cursor lock). A second "
                "dispatch/steer that needs "
                "desktop control while the lock is held now QUEUES (bounded "
                "blocking acquire, desktop_lock_wait_s, default 960s - sized "
                "to outlive one healthy turn plus the wedge ceilings) and only "
                "bounces with a visible reason after the wait expires "
                "(2026-08-17; was refused outright).",
        "why_it_bites": "(1) The lock is process-local: it is correct only "
                        "because the daemon runs as a single evicting-"
                        "singleton process (server.serve's "
                        "_take_singleton_lock) - if that ever changes "
                        "(multi-worker, multiprocess), two desktop turns could "
                        "run concurrently again with nothing catching it. (2) "
                        "Detection is a string match on 'windows-mcp' in "
                        "allowed_tools, not a derived capability from an "
                        "authoritative registry - it is now capability-aware "
                        "for windows-mcp (read-only vs control) and fail-safe, "
                        "but a future driver granting equivalent desktop "
                        "control under a differently-named MCP server would "
                        "still silently bypass the guard. (3) [PAID in part "
                        "2026-08-17: contention queues instead of bouncing] "
                        "Fail-fast means a legitimate second desktop card just "
                        "bounces/parks; nothing tells the owner to retry once "
                        "the first one frees the lock.",
        "trigger": "the daemon is ever run with more than one process/worker; "
                   "a new desktop-capable driver is added whose tool patterns "
                   "don't contain the string 'windows-mcp'; a desktop turn "
                   "outlives desktop_lock_wait_s (the queued card then bounces "
                   "with the waited-and-gave-up note)",
        "fix": "If multi-process ever happens: move the lock to a file lock "
               "or DB row (same durable-state pattern as turn_active) instead "
               "of in-memory. Replace the capability check with an explicit "
               "per-driver 'desktop: true' flag in settings.json's drivers "
               "config, checked instead of parsing allowed_tools patterns.",
        "order": 23,
    },
    {
        "id": "site-deploy-outside-hook",
        "title": "helmdeck.de ships by hand - accepting a card never deploys it",
        "status": "open",
        "what": "The public site is a Cloudflare Worker (ops/deploy/waitlist/, "
                "worker 'helmdeck-waitlist', custom domain helmdeck.de). Every "
                "other shipping surface rides the repo deploy hook that "
                "sessions._repo_hook fires post-merge on accept; this one does "
                "not. Nothing in the accept path runs `wrangler deploy`, so a "
                "site commit is merged, gated, accepted and still not live. "
                "ops/deploy/push_site.sh now makes the step one canonical command "
                "that self-verifies against the origin, but RUNNING it is still "
                "a human remembering to.",
        "why_it_bites": "It already bit, silently, for two days. Card "
                        "proc-20260814-s7 merged the full landing page "
                        "(39bf69a, 2026-08-15) and helmdeck.de kept serving the "
                        "2026-08-13 waitlist-only build - the owner saw 'nur die "
                        "Waitlist' while the commit log and the card's own "
                        "outcome said shipped. The failure is invisible from "
                        "inside the repo: git log, gate and accept all read "
                        "green, and the card had verified against `wrangler "
                        "dev` instead of the origin. Worse, it is the marketing "
                        "surface - the one place where being stale costs "
                        "signups rather than developer time.",
        "trigger": "any future card that edits ops/deploy/waitlist/src/index.js and "
                   "is accepted without someone separately running "
                   "ops/deploy/push_site.sh",
        "fix": "Add `bash ops/deploy/push_site.sh` to repo_hooks.<repo>.deploy in "
               "settings.json so accept ships the site like it ships everything "
               "else (owner-side edit - settings.json is git-ignored, an agent "
               "cannot make it). Better still, derive it instead of hardcoding: "
               "have the deploy hook ship the site only when the merged diff "
               "touched ops/deploy/waitlist/, which is the Paseo-style 'observe the "
               "runtime signal' version of the same thing. Until then the "
               "origin check `bash ops/deploy/push_site.sh --check` is the backstop "
               "- it exits 1 when live != source and is cheap enough to run "
               "from the loop.",
        "order": 24,
    },
    {
        "id": "build-stale-tracked-sources-only",
        "title": "build_stale() only ever sees the native inputs git status can see",
        # PAID 2026-08-16. It was left OPEN by the step that fixed only the
        # false-POSITIVE half; the title describes the false-NEGATIVE half, and
        # closing the entry then would have retired a blind spot that was still
        # there. Both halves are closed now - see "fix" below.
        "status": "paid",
        "what": "ops/tools/loop_state.py's build_stale() gates on touches_native(touched), "
                "and `touched` comes from `git status --porcelain` (dirty_files()). "
                "surfaces/app/android/ is entirely git-ignored (app/.gitignore: `/android`), so "
                "a native source edit under surfaces/app/android/app/src/main can NEVER appear "
                "in `touched` - the reachable trigger set is really just app/app.json "
                "and surfaces/app/package.json (ARTIFACT_TRIGGERS), a narrower net than the "
                "ARTIFACT_SRC inputs ship.sh's own fingerprint hashes.",
        "why_it_bites": "A change made ONLY inside surfaces/app/android/ (a manual native tweak, "
                        "a Gradle edit) would not flag BUILD stale even though it really "
                        "did move the native fingerprint - the same class of blind spot "
                        "the fix here just closed for the false-positive direction "
                        "(every card was nagged to rebuild for an artifact it could never "
                        "possess). This is the false-negative shadow of that same gap: "
                        "git-visibility, not the fingerprint itself, decides whether the "
                        "nudge can fire at all.",
        "trigger": "a native-only edit made directly under the ignored surfaces/app/android/ tree "
                   "(outside app.json/package.json) on the box that actually builds the "
                   "APK, with nothing else touched",
        "fix": "PAID (ops/tools/loop_state.py, 2026-08-16) by REORDERING the two signals "
               "rather than by widening the heuristic. The 2026-08-15 step made "
               "build_stale() take `touched` and return early unless "
               "touches_native(touched) - which killed the false-positive nag on every "
               "quiet card, but left the git-visibility heuristic as a VETO in front of "
               "the authoritative check, which is what this entry's title names. "
               "build_stale() now asks the fingerprint FIRST: when ops/deploy/.native_fp "
               "exists and _native_fp() computes, it compares them and returns, full "
               "stop. That answer is derived from the real native inputs (it hashes the "
               "AndroidManifest under the git-ignored tree too), so git-visibility no "
               "longer decides whether the nudge can fire - the false negative is gone "
               "at its root, without the `git -C surfaces/app/android status` probe this entry "
               "once proposed (a second reconstructed signal was the wrong shape; the "
               "fingerprint was already the derived one). `touched` still gates the "
               "DEGRADED branches - no marker (never shipped from this checkout) or no "
               "git-bash to compute a fingerprint - which is the only place a guess "
               "belongs, and is exactly the branch a card worktree lands in, so the "
               "false-positive fix holds for a better reason than before: not 'cards "
               "are excluded' but 'we have no authoritative answer here, so do not "
               "invent one'. Covered by ops/tests/test_harness_layer.py (both directions).",
        "order": 25,
    },
    {
        "id": "card-shares-the-operators-auto-memory",
        "title": "a card worker reads and (until this entry) wrote the OPERATOR'S "
                 "personal auto-memory",
        # PAID 2026-08-16. Owner's first reaction to the read/write finding was
        # "isn't it tracked by git, what's the issue with writing this" - a
        # reasonable question, and worth checking rather than either assuming it
        # away or accepting the premise unchecked. It is FALSE: `git -C ~/.claude
        # status` is "not a git repository" and there is no .git anywhere under
        # ~/.claude. So the write really was unrecoverable, and the read-only fix
        # (option b from the original entry) shipped.
        "status": "paid",
        "what": "MEASURED 2026-08-16 against CLI 2.1.207, with the environment scrubbed "
                "of every CLAUDE* variable so the reading is not an artefact of the "
                "probe's own parent session: every surface's init event reports "
                "memory_paths.auto = ~/.claude/projects/C--Users-Tien-Duy-Vo-Downloads-"
                "swarmdeck/memory/ - the operator's personal cross-session memory "
                "directory, shared by every card, every machine task, the board copilot "
                "and the operator's own desktop sessions, unmoved by --setting-sources "
                "(identical under `project` and `\"\"`). Confirmed WRITABLE against the "
                "real spawn path (drivers.build_argv + the shipped card.json, a stream-"
                "json turn on stdin exactly like _ClaudeSession.run_turn) before this fix: "
                "the Write tool created a file in that directory with no permission "
                "prompt under --permission-mode acceptEdits. And confirmed NOT git-backed: "
                "`git -C ~/.claude status` and `git -C <the memory dir> status` both say "
                "'not a git repository (or any of the parent directories)' - there is no "
                ".git anywhere in the operator's ~/.claude tree, so a card's write there "
                "had no revert path.",
        "why_it_bites": "ops/harness/ exists to stop the operator's personal ~/.claude layer "
                        "reaching a sandboxed worker. It closed the SETTINGS half of that "
                        "leak (hooks, the model pin, skillOverrides) and this half was "
                        "never noticed, because nothing rendered it: /harness's provenance "
                        "view showed settings layers and the hook matrix, not memory_paths. "
                        "Two consequences, both real. (1) Personal context leaks INTO a "
                        "card - the memory dir holds the owner's notes, not the card's. "
                        "(2) Memory being writable meant a card could silently edit notes "
                        "every future session of every card and the operator's own desktop "
                        "would read as background context - shared mutable state outside "
                        "the worktree, the one thing worktree isolation exists to prevent, "
                        "and with no git history to recover from a bad write.",
        "trigger": "was: any card spawn - the steady state, not an edge case, and it bit "
                   "the moment a card wrote a memory file (the memory instructions ship in "
                   "the system prompt, inviting exactly that). Now: none - the write is "
                   "denied at the permission layer before it reaches disk.",
        "fix": "SHARE BUT READ-ONLY (option b of the three originally proposed), verified "
               "against the real CLI in both directions before shipping - the same "
               "'measured, not assumed' standard as probe_harness_settings.py. "
               "cells/engineer/harness/settings/card.json and cells/copilot/harness/settings/copilot.json now deny "
               "`Write(~/.claude/projects/**)` and `Edit(~/.claude/projects/**)`, the SAME "
               "Read/Write/Edit tool-pattern mechanism that already protects "
               "daemon/settings.json two lines above it - no new mechanism introduced. "
               "Read is left open, so a card still benefits from accumulated notes; only "
               "the write is closed. Measured before AND after: the real "
               "drivers.build_argv() + card.json spawn wrote the file with the deny "
               "absent, and the identical spawn got `<tool_use_error>File is in a "
               "directory that is denied by your permission settings.</tool_use_error>` "
               "with it present - for both the Write tool (new file) and the Edit tool "
               "(modifying an existing one). `~` in the pattern is honoured by the CLI, "
               "measured the same way. Also shipped: harness.preview()'s "
               "`_memory_isolation()` reads each surface's OWN settings file at preview "
               "time and reports whether its deny list actually covers this - so a future "
               "edit that removes the line is visible in /harness the same way a "
               "disappearing hook is, rendered in surfaces/app/src/ui/harness_section.tsx as a "
               "write-protected/WRITABLE row. Deliberately NOT computed: the exact value "
               "of memory_paths.auto (option c's literal ask). The CLI derives that slug "
               "from a project identity that measurably is not just \"this cwd\" - every "
               "card worktree probed resolved to the SAME directory despite different "
               "cwds - and guessing that derivation to render it live would be exactly the "
               "unverified reconstruction CLAUDE.md's NO MONKEY PATCHES rule forbids. What "
               "IS shown is honestly derivable from the file alone: does this surface's "
               "actual deny list cover it, right now.\n\n"
               "COMPLEMENTARY HALF - VERSIONING, now a system rather than a one-off. The "
               "owner asked whether the memory directory should simply be made recoverable "
               "instead of blocked. Both, not either: the deny above stops a card's write "
               "from being adopted as background context before anyone notices, which git "
               "history alone would not prevent (a bad write still lands and is read by the "
               "NEXT session before any revert). What git adds is what the deny "
               "structurally cannot - recoverability for the OPERATOR's own interactive "
               "sessions, which the deny never gated and which are now the only writers. "
               "Shipped as ops/tools/memory_autocommit.py, a Stop hook deployed to "
               "~/.claude/hooks/ by ops/tools/install_memory_hook.py and wired into "
               "~/.claude/settings.json. It sweeps EVERY ~/.claude/projects/*/memory/, "
               "git-inits any that holds notes without a repo, and commits what changed - "
               "so a project created next month is covered with no action taken (the owner's "
               "explicit ask: 'establish system so each project in the future gets proper "
               "tracking'). Commits carry the session_id from the hook payload, which is "
               "what makes a later revert decidable. Local only: it never adds a remote and "
               "never pushes, asserted in ops/tests/test_memory_autocommit.py by scanning its "
               "own source. Its ONE law is daemon/harness.py's law - it can never break a "
               "turn: a Stop hook exiting 2 BLOCKS the turn, so main() returns 0 "
               "unconditionally, every git call is timeout-bounded, and the test drives a "
               "missing root, a file where a directory belongs, a repo mid-merge, a missing "
               "git binary and a sweep() that raises, asserting exit 0 through all of them. "
               "Canonical copy stays in the repo with the deployed copy held byte-identical "
               "by `--check`, because fixing untracked state with an untracked script would "
               "have been a joke at its own expense. Verified live: 7 memory directories "
               "bootstrapped, audited for credential-shaped content (none) and for remotes "
               "(none).",
        "order": 26,
    },
    {
        "id": "null-result-test-races-under-load",
        "title": "ops/tests/test_null_result.py races on subprocess frames and can red "
                 "an innocent card when the box is busy",
        "status": "open",
        "what": "Observed 2026-08-16 during the /glance card: a full gate run "
                "failed ONE check - 'fresh spawn: guard scoped to resume only "
                "(out=%r)' - reporting out='real-answer' where the test pins "
                "out=''. The same test passed standalone twice immediately "
                "after, and the very next full gate run passed 55/55. The only "
                "changes in the working tree at that moment were Markdown docs, "
                "which cannot reach drivers.py. The distinguishing condition was "
                "LOAD: three subagents and a Playwright browser had just been "
                "running. The test spawns a real subprocess (fake_claude via a "
                ".cmd wrapper) and asserts which result FRAME wins; under load "
                "the null frame it expects to count appears to be overtaken by "
                "the later real one.",
        "why_it_bites": "This is the gate-bounces-an-innocent-card class again, "
                        "but from a different direction than "
                        "gate-exit-code-vs-stdout-verdict: here the gate is "
                        "reporting a REAL failing check, so no verdict-level "
                        "heuristic can catch it. A card that touched nothing "
                        "near the driver gets held on Review with a report about "
                        "null-result frames, which reads as an unexplained "
                        "rejection and burns a review cycle. It is also worse "
                        "than a plain flake because the gate is the harness's "
                        "one objective signal - a gate that is sometimes wrong "
                        "quietly teaches the owner to re-run instead of read.",
        "trigger": "any card whose gate reds ONLY on ops/tests/test_null_result.py, "
                   "especially while other work is running on the box; or the "
                   "same shape appearing in another test that spawns "
                   "fake_claude and asserts frame ordering",
        "fix": "Make the assertion deterministic instead of timing-dependent: "
               "have fake_claude emit the null and real frames with an explicit "
               "ordering barrier the test can wait on (or drive the frame pump "
               "directly rather than through a real subprocess), so 'which frame "
               "wins' is decided by the code under test and not by scheduler "
               "luck. Until then, do NOT paper over it by retrying the gate.",
        "order": 27,
    },
    {
        "id": "direct-build-no-gate",
        "title": "DIRECT build cards edit the live tree with no gate and no isolation",
        "status": "open",
        "what": "sessions.new_direct_task (owner-requested, Paseo semantics) "
                "files a card whose workplace is the repo's LIVE working tree: "
                "machine=True rides the no-worktree dispatch/accept path, so "
                "there is no branch, no merge, and NO gate-before-review - the "
                "agent's edits land in the tree the owner is looking at, "
                "immediately. Two direct cards on the same tree are serialized "
                "in _turn (bounded queue per normalized tree path, the desktop-"
                "lock pattern), so they cannot edit blind over each other.",
        "why_it_bites": "(1) A direct card can break the tree and nothing red "
                        "stops it - the gate law is deliberately bypassed for "
                        "this card class; the repo's own hooks/loop-state are "
                        "the only guard rail. (2) A direct card and the OWNER "
                        "editing the same files at the same time still race - "
                        "the per-tree lock serializes cards, not humans. (3) "
                        "Uncommitted owner work in the tree is exposed to the "
                        "agent's edits; there is no snapshot to roll back to "
                        "unless the agent (or owner) commits first.",
        "trigger": "a direct card is dispatched onto a tree with uncommitted "
                   "owner changes; a direct turn goes wrong and there is no "
                   "gate to bounce it; the owner edits while a direct turn runs",
        "fix": "Make the dispatch snapshot the tree first (a lightweight "
               "baseline commit or stash-ref, same nothing-lost rule as "
               "worktree reclaim) so every direct turn has a rollback point; "
               "surface 'direct card active on this tree' in the app while a "
               "turn runs; consider an optional post-turn gate run (advisory, "
               "non-blocking) so red at least becomes visible.",
        "order": 28,
    },
    {
        "id": "fast-track-no-gate",
        "title": "Fast-Track cards ride the no-worktree direct path - no isolation, no gate, auto-deploy",
        "status": "open",
        "what": "Owner-decreed 2026-08-20: worktree isolation was supposed to be "
                "regenerable, disposable state, but it left an open Fast-Track "
                "card exposed to base drift for its ENTIRE open lifetime - a "
                "56-file base-only test-wiring fix reddened a card's gate for "
                "code it never touched (debt gate-base-lag, hit live on card "
                "proc-20260816-s2), and the daemon has no sync-loop to fix that "
                "drift while a card sits open. Rather than build the sync loop, "
                "the owner chose to drop worktree isolation for Fast-Track "
                "entirely: dispatch._start_inner marks a fast_track (non-machine) "
                "card machine=True + direct=True + worktree=repo at first "
                "dispatch (same no-worktree rails as new_direct_task) - no "
                "branch, ever. sessions._maybe_fast_track_ship_direct replaces "
                "the normal gate+merge ship with autocommit-only (still refuses "
                "open conflict markers - data hygiene, not a gate) + the deploy "
                "hook, fired automatically after every finished turn. Extended "
                "2026-08-20: flipping fast_track ON for a card that STARTED "
                "worktree-isolated now CONVERTS it onto these rails "
                "(sessions._convert_fast_track_live: idle-turn-only, branch "
                "landed via the accept-path merge WITHOUT a gate run, worktree "
                "reclaimed, session dropped) - the conversion merge rides this "
                "same gate skip. Before, such a card stayed gated for life, "
                "which made fast-track useless as an escape hatch when the "
                "gate itself was the broken part (the base-sync card).",
        "why_it_bites": "Everything direct-build-no-gate already bites, PLUS: "
                        "(1) Fast-Track is MORE automated than a direct card - "
                        "direct_task is owner-requested solo building, Fast-"
                        "Track ships every turn UNATTENDED, so a broken change "
                        "can deploy with nobody watching, repeatedly, before "
                        "anyone notices. (2) _accept_machine's Done path now "
                        "also deploys for ANY direct card (fixed alongside this "
                        "change, since something DID land in a repo) - a plain "
                        "new_direct_task card that the owner accepts by hand "
                        "now deploys too, where it silently didn't before.",
        "trigger": "a Fast-Track card's turn breaks the tree; two humans/agents "
                   "touch the same Fast-Track tree at once; a Fast-Track card "
                   "runs a long time unattended and ships something bad",
        "fix": "Same fix direct-build-no-gate already names (snapshot-before-"
               "turn rollback point, advisory post-turn gate, 'card active on "
               "this tree' surfaced in the app) - Fast-Track needs it more "
               "urgently than a solo direct card because it is unattended by "
               "design.",
        "order": 29,
    },
    {
        "id": "plugin-kernel-dual-nav",
        "title": "Plugin kernel boots alongside expo-router (two navigation systems)",
        "status": "open",
        "progress": "ec7e307: the PRODUCTION tab set is now registry-driven — root "
                    "_layout boots the 'app' profile + provides the kernel; "
                    "(tabs)/_layout renders <Tabs.Screen> from the nav.tabs surface "
                    "plugin with a 1:1 hard-coded fallback (never bricks). "
                    "Screenshot-JUDGED at both breakpoints (desktop sidebar + phone "
                    "bottom bar) identical to before. REMAINING: the Sidebar's own "
                    "NAV array is still hard-coded; per-screen COMPONENTS are not "
                    "yet surface plugins (only their nav metadata is); kernel-demo "
                    "still exists as the dev route.\n"
                    "2026-08-24 (two-mains phase 3, owner 'direkt hier'): the five "
                    "cell Surface plugins now physically live in their cells "
                    "(cells/<id>/ui/surface.tsx), wired back via metro.config.js "
                    "(watchFolders + @cells extraNodeModules + nodeModulesPaths) "
                    "and tsconfig @cells paths; the bare-module tsc fallback is "
                    "QUARANTINED in tsconfig.typecheck.json because Metro reads "
                    "tsconfig.json paths at runtime (resolving react to "
                    "@types/react bricks the bundle - measured). kernel-demo dev "
                    "route deleted (zero references). Sidebar registry-driven since "
                    "earlier. REMAINING for this debt: per-screen COMPONENTS - the "
                    "(tabs)/ route files still render their own imports, not the "
                    "registry's Surface components.\n"
                    "2026-08-18 (cell-registry-daemon-plugin-kernel, order 33): the "
                    "'app' profile (surfaces/app/profiles/app.json) now ALSO loads the 5 "
                    "cell Surface plugins (surfaces.board/pm/processes/connectors/"
                    "chat, built during the Cell sweep) alongside nav.tabs. This is "
                    "a real, narrow step: those Surface objects are now actually "
                    "LOADED into KEYS.SURFACES in PRODUCTION for the first time "
                    "(previously only the dev kernel-demo route's 'phase2' profile "
                    "loaded any of them) - so /modules' generic SURFACES section "
                    "genuinely lists all 5 as present, real, swappable modules, "
                    "not just nav.tabs' route metadata. Deliberately did NOT swap "
                    "(tabs) screen rendering to consume Surface.component instead "
                    "of the plain expo-router files - each Surface has no `route` "
                    "field on purpose (verified: grepped all 5 files, none set "
                    "it), so nav.tabs' hard-coded route entries still drive what "
                    "renders, unchanged. Verified: tsc --noEmit clean; confirmed "
                    "no Registry key collision (surfaces.board vs tab.board are "
                    "distinct ids) and no inject-ordering issue (KEYS.SURFACES is "
                    "provided by a CORE_PLUGIN loaded before any profile plugin, "
                    "so JSON array order in app.json doesn't matter) by reading "
                    "boot/core.ts + boot/index.ts directly. NOT runtime-booted-and-"
                    "observed this pass (no dev server started, no screenshot tool "
                    "available this session) - code-level verification only, "
                    "stated plainly rather than claimed as judged.\n"
                    "2026-08-19: the actual CUTOVER, corrected against a stale "
                    "premise found while executing it - the plan assumed 4 "
                    "tab-bearing cell surfaces (board/processes/connectors/"
                    "copilot); reading surfaces/app/src/plugins/surfaces/tabs.ts directly "
                    "showed copilot has NO tab entry at all (its entry point is a "
                    "floating chat FAB + overlay, matching PM's precedent of no "
                    "tab) - only 3 cells are genuinely tab-bearing. Also found: "
                    "(tabs)/_layout.tsx's Sidebar ALREADY read from useSurfaces() "
                    "with the same fromRegistry-shaped computation as the bottom "
                    "bar (not hard-coded, contrary to this entry's own earlier "
                    "'REMAINING' note) - that half of the debt had already been "
                    "paid by a prior change this entry never recorded. board.tsx/"
                    "processes.tsx/connectors.tsx each already had a `nav` object "
                    "but no `route` field, so fromRegistry's `s.route && s.nav` "
                    "filter silently excluded them and nav.tabs' separate "
                    "tab.board/tab.processes/tab.connectors entries kept driving "
                    "production - the actual gap was exactly that one missing "
                    "field per surface, not a bigger rewrite. Fixed: route + the "
                    "real nav values (copied from nav.tabs' matching entries, "
                    "same icon/order/labelKey/sectionKey/desktopOnly/teamOnly - "
                    "zero visual change) merged onto all 3 Surface objects; the 3 "
                    "now-redundant tab.* entries removed from tabs.ts (the "
                    "remaining 9 - dashboard/needs/recordings/sessions/history/"
                    "automation/settings/modules/more - stay nav-only, genuinely "
                    "no cell owns them). Confirmed the (tabs)/{board,processes,"
                    "connectors}.tsx route files do NOT need to become "
                    "'Surface.component shims' as originally planned - reading "
                    "them showed the wrapping direction was already correct "
                    "(surfaces/board.tsx imports FROM (tabs)/board.tsx, re-"
                    "exporting the SAME component as `.component`; there was "
                    "never a second copy of the screen logic to reconcile, only "
                    "the nav metadata was duplicated). kernel-demo.tsx NOT "
                    "deleted yet - left as the safety net until a real dev-"
                    "server screenshot pass confirms the 3 real tabs render "
                    "identically (no browser/dev-server tooling available this "
                    "session either). Verified: tsc --noEmit clean; grepped for "
                    "remaining tab.board/tab.processes/tab.connectors references "
                    "(none outside this change's own comments); "
                    "isSurfaceCellDisabled's dual id/route lookup confirmed still "
                    "correct for the merged surfaces (their own `.id` is "
                    "'surfaces.<x>', `.route` is the bare name, matching what "
                    "useDisabledCellSurfaces indexes on both keys for).",
        "what": "surfaces/app/src/kernel/ (Phase 1) + surfaces/app/src/boot/ (Phase 2) introduce a "
                "plugin-first composition: profiles pick swappable plugins, "
                "surfaces register into KEYS.SURFACES, a registry-driven host "
                "renders them. But production navigation still runs through the "
                "hard-coded expo-router surfaces/app/src/app/(tabs) list. The only surface "
                "actually wired through the kernel is a dev route "
                "(surfaces/app/src/app/kernel-demo.tsx) rendering the board from the "
                "registry; the real (tabs) screens are unchanged. Engines are "
                "half-migrated too: engines.claude wraps the daemon api behind the "
                "Engine contract, but the ~11 scattered `import copilot` / "
                "`import claude_sessions` branches in daemon/server.py + "
                "sessions.py still select backends inline.",
        "why_it_bites": "Two nav systems mean a screen can drift between the (tabs) "
                        "route and its surface plugin; a profile (store/owner/demo) "
                        "does NOT yet govern what production ships, so the "
                        "pre-config story is only true for the demo route. Adding a "
                        "new screen needs doing twice until the cutover lands.",
        "trigger": "adding/removing a screen; shipping a store build expecting the "
                   "store.json profile to exclude machine-control (it doesn't gate "
                   "production nav yet); adding the deepseek engine",
        "fix": "Phase 2 cutover card: make surfaces/app/src/app/(tabs)/_layout render tabs "
               "from useSurfaces() (KEYS.SURFACES) instead of the static list, "
               "migrate each (tabs) screen into a surfaces/* plugin, and delete "
               "the hard-coded list in the same commit that adds its plugin. "
               "Phase 2b: replace the inline daemon backend-select branches with "
               "an engine registry keyed by a stored `engine` field. Boot the "
               "real profile (owner on desktop, store on stores) at app entry so "
               "pre-config governs production, then retire kernel-demo.tsx.",
        "order": 29,
    },
    {
        "id": "full-dynamism-decree",
        "title": "Charter reframed: fixed harness -> seeded+swappable modules, "
                 "universal tracking is the only floor",
        "status": "open",
        "what": "Owner decree (2026-08-17): move to the DeepSeek 'everything is a "
                "plugin' model, but (1) everything must be TRACKABLE and (2) the "
                "old rules are not deleted, they are SEEDED as defaults. The app "
                "kernel (surfaces/app/src/kernel) implements this: nothing is unswappable "
                "(unload/swap allowed on seed governance too), but every "
                "load/unload/swap appends a TrackEntry {op, pluginId, actor "
                "(seed|profile|user|agent|system), replaced?, note} to an "
                "append-only journal; swap() returns a rollback(); the charter "
                "(gate-before-review, append-only audit, worktree isolation, "
                "auth-required, measured economics, WIP limit) is seeded as data "
                "in boot/policies.ts CHARTER_DEFAULTS under KEYS.POLICIES. This "
                "CONTRADICTS the CLAUDE.md law 'never weaken the fixed harness' "
                "and the 'core refuses unload' enforcement (now removed).",
        "why_it_bites": "The daemon-side enforcement is still MOSTLY hard-wired "
                        "code that owns its own on/off. PROGRESS: policy.py is the "
                        "control plane (swap() mirrors a 'reconfig' event into the "
                        "append-only events sink); GET /policy + POST /policy/swap "
                        "expose it; the app hydrates the seeded modules from it via "
                        "a tracked swap; ONE real consumer now reads it "
                        "(events.metrics wip_limit -> policy.get_policies, seeded "
                        "from settings so non-divergent). REMAINING: gate / "
                        "auth / economics / worktree enforcement still don't read "
                        "the PolicySet; the APP kernel journal() is not yet "
                        "mirrored into events (only daemon-side swaps are); and "
                        "charter.py (the CONNECTOR SANDBOX, distinct from the "
                        "instruction charter) stays human-only by design.",
        "trigger": "wiring the super-agent's reconfiguration authority; letting a "
                   "UI toggle a policy; any claim that governance is swappable",
        "fix": "Daemon-side: (1) mirror the app journal into the existing "
               "append-only events.py sink so reconfiguration is audited with the "
               "same guarantees; (2) have charter.py/gate/economics READ the "
               "seeded PolicySet instead of hard-coding, so a tracked swap "
               "actually changes enforcement; (3) expose an engine/policy control "
               "plane the user (UI) and super-agent call, every call recorded, "
               "every swap reversible; (4) CLAUDE.md/charter is itself a SEED "
               "MODULE now (surfaces/app/src/boot/charter.ts, seed.charter, KEYS.CHARTER) "
               "- changing it is a tracked, reversible swap of seed.charter that "
               "materializes as an edit to the CLAUDE.md file, NOT an out-of-band "
               "approval gate (that framing was the retired fixed-harness reflex; "
               "the sign-off WAS the decree). agentMaySwap defaults false so an "
               "agent-initiated charter swap still wants a human confirm until "
               "seeded true. Daemon-side: point loop_state/charter.py at the "
               "seeded CharterDoc so on-disk CLAUDE.md and the module stay one "
               "source; mirror charter swaps into events.py.",
        "order": 30,
    },
    {
        "id": "daemon-god-files",
        "title": "Daemon god-file breakup — SUBSTANTIALLY RESOLVED: 23 service/"
                 "route modules extracted, server.py's ENTIRE route surface "
                 "is now dispatch-table-driven (2109 -> 478 lines); pm.py's "
                 "notice cluster investigated and DECLINED (confirmed genuinely "
                 "coupled, not just assumed)",
        "status": "open",
        "what": "The full-dynamism decree made the APP a plugin composition; this "
                "extends the same idea to the daemon by extracting SERVICES "
                "bottom-up (trackstore/locks/turn-execution first, so higher "
                "modules become thin dependents instead of reaching into a "
                "monolith - the DeepSeek kernel/plugin model applied to Python). "
                "Every extraction below is a strangler move verified against the "
                "FULL 11-file test suite (0 failures) plus an identity check "
                "(orig.fn IS new_module.fn) - re-export means every existing "
                "caller, internal and external, is unchanged.\n"
                "\n"
                "sessions.py 3927 -> 1046 (73% cut), 17 modules: gitutil.py (git+"
                "worktree primitives, extended twice with _worktree_for/_base_ref/"
                "_worktree_of_branch), econ.py (turn economics), blockers.py "
                "('what needs the owner'), outcomes.py (outcome extraction), "
                "worktrees.py (reclaim/sweep), trackstore.py (the data-layer "
                "SERVICE - _load/_save/_mutate; the monkeypatched tests were "
                "updated to fake trackstore._db instead of sessions._db, the "
                "technique proving patched clusters ARE extractable), locks.py "
                "(turn/surfaces/desktop/direct locks + steer epoch), turnrunner.py + "
                "devport.py (turn execution: _turn/_finish_turn/_settle_reply), "
                "lanemachine.py (the gate/merge crown jewel: move_lane/_gate/"
                "_merge_to_main/_repo_hook - only 4 lazy back-refs, needed NO test "
                "changes because the test-exercised entry point stayed in "
                "sessions.py), dispatch.py (new_track/machine-task/direct-task - "
                "the SAME cluster that failed with 19 back-refs on the first "
                "attempt now extracted with ZERO, because every dependency was "
                "already a service by the time it was reached), cardadmin.py "
                "(archive/update/fork/history/attachments), lifecycle.py "
                "(present()/sweep_zombies - lifecycle observation).\n"
                "\n"
                "drivers.py 1490 -> 990 (NO LONGER a god-file), 3 modules: "
                "agentcli.py (argv/MCP-config), proctable.py (PID/process table - "
                "MONKEYPATCHED, needed no test change since patched fns' callers "
                "stay in drivers), spawnenv.py (_env/_card_env).\n"
                "\n"
                "copilot.py 1136 -> 1012, 2 modules: copilot_stats.py (PM-session "
                "economics), copilot_actions.py (reply/action parsing).\n"
                "\n"
                "pm.py 2192 -> 1992, 1 module: pm_budget.py (budget/quota math + "
                "the pure quota-notice text builders _fmt_when/_usage_flag_text/"
                "_goal_budget_text + the two hard gates _quota_floor/"
                "_triage_green - surgically separated from the STATE-COUPLED "
                "notice functions _usage_checkin/_plan_gate_notice that stayed, "
                "since they call pm's mutable loopstate).\n"
                "\n"
                "server.py 2109 -> 1073 (49% cut), 8 module-level-helper modules "
                "(glances.py/apimeta.py/startup.py, as below) PLUS the H "
                "handler's do_GET/do_POST if-chain now has a real DISPATCH-TABLE "
                "seam with THIRTEEN route groups converted (~40 routes total): "
                "routes_auth.py (5: auth/state/setup/register/login/logout), "
                "routes_policy.py (3: /policy, /policy/swap, /reconfig/track), "
                "routes_settings.py (5: /settings GET+POST, /nightshift, /usage, "
                "/automation), routes_glance.py (4: /glance, /glance/voice/<id>."
                "mp3 [first PREFIX route - GET_PREFIX_ROUTES, an ordered list "
                "checked before the exact-match dicts], /glance/talk, /glance/"
                "answer), routes_info.py (6 read-only: /debt, /charter, /loop/"
                "map, /models, /harness, /ops/harness/schema - also exposed + "
                "removed a pre-existing DEAD duplicate /harness block further "
                "down do_GET, unreachable since the dispatch check runs first), "
                "routes_pm.py (6: /pm/economics, /pm/plan, /pm/config, /pm/"
                "consolidate, /pm/report, /pm/reconcile), routes_misc.py (3: "
                "/processes, /me, /processes/new), routes_control.py (5: "
                "/control/state, /control/teach/start,stop, /control/distill, "
                "/control/demo), routes_relay.py (2: /relay/pair,unpair), "
                "routes_connectors.py (3: /connectors list + /connectors/<name>/"
                "rollback,run - the rollback/run guard is path-param [parts[0]/"
                "parts[2]] and stays inline in server.py, only the body moved), "
                "routes_checkpoints.py (3: /checkpoints list + /checkpoints/"
                "<id>/diff,restore - same path-param-guard-stays-inline "
                "technique, diff/restore route bodies verified with a REAL "
                "create/list/diff/restore round-trip against a sandboxed CPDIR, "
                "not just role-gate assertions), routes_projects.py (4: /"
                "projects GET+POST, /projects/<id>/update,delete - path-param "
                "guard inline; caught along the way that projects.new_project "
                "requires fixed_price for billing='fixed', a real validation "
                "rule the test now exercises rather than assumes), routes_"
                "copilot.py (4: /chat/history, /chat/live, /chat/cancel, /chat "
                "- the real copilot.chat() model turn is NEVER invoked by the "
                "test [cost/network/non-determinism]; caught along the way that "
                "copilot.history() returns {messages,session_id,stats}, not a "
                "bare list - the test asserted the real shape after a first "
                "wrong assumption failed loudly, not silently). ~40 routes "
                "converted; ~26 of the original ~66 remain inline.\n"
                "\n"
                "14th SLICE (2026-08-18), THE CROWN JEWEL: the tracks/dispatch "
                "cluster - the routes that sit directly on sessions.py's gate/"
                "merge state machine (lanemachine.py's move_lane -> _gate -> "
                "_merge_to_main). Split into TWO modules by risk, per the task "
                "brief's own suggestion: routes_tracks.py (14 routes - CRUD/"
                "reads: GET /tracks, /tracks/<id>/live,turns,history,transcript"
                "[+/live],checkpoints,attachments,attachment/<name>; POST /"
                "tracks/reorder,new,<id>/archive,fork,fork-chat,delete,update,"
                "rewind,attach[+/remove]) and routes_track_actions.py (5 routes "
                "- the gate/dispatch-critical half kept ISOLATED for extra "
                "scrutiny: GET /tracks/<id>/stream [live transcript SSE], POST "
                "/tracks/<id>/steer,answer,cancel,lane). Every route body moved "
                "VERBATIM (byte-identical, grep-verified no body left "
                "duplicated in both server.py and the new modules); path-param "
                "routes keep their guard inline in server.py per the "
                "routes_checkpoints.py precedent, only the body moved as a "
                "function taking the extra path segment(s). server.py: 1073 -> "
                "740 lines.\n"
                "\n"
                "test_server_routes.py grew a dedicated tracks-cluster section "
                "driving the REAL state machine end-to-end (not just role-gate "
                "assertions): files a card against a real temp git repo, lists "
                "it, updates/archives/attaches[+detaches] it, reads its history/"
                "transcript/checkpoints, then exercises BOTH gate outcomes for "
                "real - GATE BLOCKED (never dispatched -> lanemachine._gate's "
                "own \"never dispatched\" problem, card bounces) and GATE "
                "PASSING (dispatched via the real ->working path, a clean "
                "worktree with no helmdeck.gate file advances past the gate) - "
                "plus reorder, fork/fork-chat/rewind against an unknown id (400, "
                "RuntimeError caught not a 500), and a real owner-gated delete. "
                "drivers.run is stubbed (same seam test_direct_task.py/"
                "test_desktop_lock_wait.py already use) so every dispatched/"
                "steered turn is instant and network-free while the REAL "
                "worktree/lane/gate machinery still runs.\n"
                "\n"
                "THREE real bugs found and fixed along the way (none caused by "
                "the route move itself - the route bodies are byte-identical; "
                "all three were latent in the sessions.py extraction and only "
                "surfaced because this was the first test to dispatch/gate a "
                "REAL card end-to-end): (1) cardadmin.py was missing `import "
                "subprocess` and `import db as _db` - delete_track's worktree/"
                "branch cleanup NameError'd, turning DELETE /tracks/<id> into a "
                "500; (2) dispatch.py was missing `from devport import "
                "_alloc_dev_port` - a NameError on first dispatch; (3) runs.REC "
                "(a card's run_dir root) is a THIRD independent __file__-"
                "derived global (same class of bug as connectors.CDIR/"
                "checkpoints.CPDIR, see the orphan-root-paths entry below) - "
                "dispatch.py/cardadmin.py/sessions.py each hold their OWN bound "
                "copy from `from runs import REC`, so patching runs.REC alone "
                "doesn't reach them. MEASURED THE HARD WAY: an early draft of "
                "this test filed real cards straight into the live daemon/"
                "recordings/ folder before the guard existed (git-ignored, no "
                "tracked data harmed, but a real sandboxing violation the test's "
                "own rules forbid) - now patches runs.REC + dispatch.REC + "
                "cardadmin.REC + sessions.REC together, before any card is "
                "filed. NOTE for the owner: a handful of stray test-created "
                "directories from before this fix landed (daemon/recordings/"
                "*req-x*, *req-gate-smoke-card*) are still sitting in the real, "
                "git-ignored recordings/ folder - harmless (never tracked, no "
                "real card data) but worth a manual `rm -rf` cleanup.\n"
                "\n"
                "PREREQUISITE BUILT FIRST (test_server_routes.py): server.py had "
                "almost no test coverage on its live HTTP surface. This spins up "
                "server.H on a REAL ThreadingHTTPServer bound to 127.0.0.1:0 "
                "(ephemeral port, never :8140 - cannot collide with or evict a "
                "live daemon; serve() itself is NEVER called since it takes the "
                "singleton port lock) and drives 10 real HTTP requests across "
                "auth/policy/tracks/dashboard/loop-map. TRAP HIT WHILE BUILDING "
                "THIS (documented in the test file): db.init() reads/migrates "
                "ROOT/tracks.json and ROOT/events.jsonl using the db.ROOT global, "
                "NOT db.DBPATH - a first draft that sandboxed only DBPATH actually "
                "renamed the REAL daemon/events.jsonl to .imported (30 real "
                "events). Caught immediately, recovered (pure rename, renamed "
                "back, live daemon on :8140 confirmed healthy throughout) - fixed "
                "by sandboxing db.ROOT before any db call. Lesson: db.py's file-"
                "import side effects are keyed off a DIFFERENT global than the "
                "one that looks like 'the' sandboxing point - verify the FULL set "
                "of module-level path globals before assuming a test is isolated.\n"
                "\n"
                "PATTERN (repeatable, proven 13 TIMES RUNNING): each route body "
                "moves VERBATIM into a routes_<concern>.py free function taking "
                "(self, user[, body]) - self._send/_sid/_send_cookie/rfile/"
                "headers all keep working since self is passed through unchanged. "
                "do_GET/do_POST gain one dispatch-table check each, BEFORE the "
                "remaining inline if-chain, so every unconverted route is byte-"
                "identical. EXTENSION for path-param routes (parts[0]==X and "
                "parts[2]==Y, e.g. /connectors/<name>/rollback or /checkpoints/"
                "<id>/diff): the guard condition itself stays inline in server.py "
                "(there is no generic PARAM_ROUTES table yet - only the route "
                "BODY moves, as a function taking the extra path segment as a "
                "plain argument, e.g. connectors_rollback_post(self, user, name)). "
                "Verify with test_server_routes.py + the full suite + a "
                "processes.json/events.jsonl/settings.json/users.json/helmdeck.db "
                "md5sum integrity check after EVERY group.\n"
                "\n"
                "PM.PY: pm_state.py extracted (touch()/loopstate/_in_window/"
                "_board_idle - the presence + daily-loop-gate SERVICE). The "
                "remaining notice/watch functions (_usage_checkin, "
                "_plan_gate_notice, _triangle_watch, _cost_watch, review_burn "
                "cluster) deliberately STAYED in pm.py: they call _ask/_say/"
                "_escalate to compose text and talk to the user - that IS pm's "
                "own domain logic (the PM's voice), not trapped infrastructure. "
                "Moving them would relocate the coupling, not remove it.\n"
                "\n"
                "METHOD LEARNED (do not repeat the mistake): always run a full "
                "dependency scan (module-DEFINEs vs external-refs, filtering "
                "comment/docstring mentions from real calls) BEFORE cutting a line "
                "range - a naive contiguous slice sweeps in unrelated consts that "
                "happen to sit nearby (BLOCKER_REASONS/LANES/LANE_FLOW/MODES were "
                "each caught this way and deliberately left behind). flow()/"
                "LANE_FLOW were deliberately NEVER moved: flow() does "
                "`_decl_lines(path=os.path.abspath(__file__), rel=\"daemon/"
                "sessions.py\")` - a self-referential source-line lookup that "
                "would silently misattribute if the file moved.",
        "why_it_bites": "server.py is now 478 lines (from 2109) with 16 route "
                        "groups extracted (routes_auth/policy/settings/glance/"
                        "info/pm/misc/control/surfaces/relay/connectors/checkpoints/"
                        "projects/copilot/tracks/track_actions/runs/system.py). "
                        "The 2026-08-18 session's second pass converted the "
                        "final residual grab-bag: routes_runs.py (/runs list, "
                        "/live.jpg, and the /runs/<id>/{timeline,playbook,video,"
                        "videochunk} path-param sub-router) + routes_system.py "
                        "(/presence GET+POST, /push/register, /sessions/claude, "
                        "/history, /harness[+/ops/harness/version/<kind>/<name>], "
                        "/debt/<id>/fix, /import/jira,url, /nightshift/plan, and "
                        "the /processes/<id>/step path-param sub-router). Every "
                        "path-param route kept its parts[]-guard inline in "
                        "server.py per the routes_checkpoints.py precedent - only "
                        "the route bodies moved, verbatim. Verified with real HTTP "
                        "assertions added to test_server_routes.py for EVERY "
                        "newly-moved route (including the processes/step "
                        "sub-router's non-'step' 404 fallthrough and runs_item_get's "
                        "match/no-match signal), full 16-file suite green, "
                        "processes.json/settings.json/users.json/helmdeck.db "
                        "byte-identical (md5) before/after. There is no longer any "
                        "route body left inline in do_GET/do_POST except the "
                        "auth/role branching and the parts[]-guards the pattern "
                        "itself calls for - server.py's route-dispatch breakup is "
                        "DONE, not just the crown jewel.\n"
                        "\n"
                        "pm.py's notice cluster (_usage_checkin/_plan_gate_notice/"
                        "_triangle_watch/_cost_watch/_needs_from_owner/the "
                        "review_burn pair/_goal_process/_goal_process_status) was "
                        "READ IN FULL and compared line-by-line against pm_budget.py "
                        "(the one extraction from this cluster's neighborhood that "
                        "DID succeed cleanly). The difference is real, not assumed: "
                        "pm_budget.py's functions are PURE - they take an econ dict/"
                        "est_turns/pace as parameters and do their own lazy "
                        "`import usage` for live data, with zero references back "
                        "into pm.py. The notice functions are not that shape: each "
                        "one reads/writes the shared `st` (loopstate) dict AND "
                        "calls straight back into pm.py's own module-level surface "
                        "- _save_loopstate(st), get_goal(), latest_plan(), "
                        "_triage_green(plan), _pace()/_days(), _i18n.t(...), and "
                        "_say()/_escalate() to actually speak to the owner - "
                        "typically 4-8 distinct back-references per function, not "
                        "the 1-2 a clean extraction needs. A thin State-carrier "
                        "class (as this debt entry used to suggest) does not "
                        "remove that coupling, it only renames it: the notice "
                        "functions would still need pm.py's plan/goal/triage state "
                        "and its _say/_escalate voice, so pm_notices.py would end "
                        "up doing `import pm` and calling back through it anyway - "
                        "moving the code without moving the coupling, exactly the "
                        "outcome this entry already warned against for the prior "
                        "review_burn cluster. DECLINED, now on read evidence "
                        "instead of a guess.",
        "trigger": "adding a new HTTP route (server.py's if-chain, now small); "
                   "any claim the daemon's API surface is not fully modular",
        "fix": "server.py: DONE. Every route group has a routes_*.py home; what "
               "remains inline (auth/role gates, LEGACY_UI redirect, the SSE/"
               "long-poll routes, the parts[]-guards for path-param routes) is "
               "either genuinely server.H's own job or a deliberate precedent "
               "(routes_checkpoints.py/routes_tracks.py) to keep dispatch guards "
               "next to the if-chain that reads them. No further server.py "
               "extraction is planned - if do_GET/do_POST grow again, repeat the "
               "same proven pattern for the new group only.\n"
               "pm.py: DECLINED for the notice cluster, confirmed by reading the "
               "code (not by re-guessing the earlier note) - see why_it_bites. "
               "Revisit only if pm.py's size becomes the active bottleneck AND a "
               "restructure of pm's plan/goal/voice state into an explicit object "
               "is independently worth doing (a real behavior change, not a pure "
               "code move) - at that point the notice functions extract for free "
               "as a side effect, which is a different, larger task than this one.",
        "order": 31,
    },
    {
        "id": "orphan-root-paths-events-jsonl-incident",
        "title": "Modules with their own hardcoded ROOT path silently bypass "
                 "db.py sandboxing in tests — one real incident, partially fixed",
        "status": "open",
        "what": "While route-testing /processes (test_server_routes.py, part of "
                "the daemon-god-files work): processes.py had its OWN flat-file "
                "storage (processes.json, hardcoded ROOT/STORE), independent of "
                "the db.ROOT/db.DBPATH sandbox every other test relied on. "
                "Testing /processes/new wrote 3 fake entries into the REAL "
                "daemon/processes.json - caught immediately, removed by exact-id "
                "match, real data (6 legitimate processes incl. the HelmDeck-"
                "Glasses-Layer one) verified intact by direct read. FIXED for "
                "processes.py (commit with this entry): it now delegates _load()/"
                "_save() to new db.processes_all()/db.processes_replace() "
                "functions (same id/data-blob table pattern as tracks/projects); "
                "db.init() migrates processes.json into the table once, renamed "
                "to *.imported (same safeguard as tracks.json/events.jsonl). "
                "Zero call-site changes needed in processes.py (all ~15 sites "
                "were already read-all/mutate-by-id/write-all). Verified with a "
                "dedicated sandboxed migration test (test_processes_db_migration."
                "py, 10/10) using SYNTHETIC data, never the real file.\n"
                "\n"
                "SECOND, related gap found while verifying the fix: events.emit() "
                "(the append-only audit sink) writes through events.py's OWN "
                "independent ROOT/EV globals - never covered by db.ROOT or even "
                "events.SET. processes.create()/sync() call it directly, so the "
                "FIRST test run against the new /processes routes appended 11 "
                "real lines to the production events.jsonl even with db.py fully "
                "sandboxed (6 clearly test-tagged actor=\"routetest-owner\"/"
                "\"test\"; 4 misleading-but-harmless \"completed\" log lines for "
                "two real processes whose actual STORED STATE was never touched "
                "- confirmed by reading their status/steps directly: both still "
                "show status=\"done\" with sensible step data, not sandbox-"
                "mutated garbage). events.EV is now sandboxed in both test files "
                "(test_server_routes.py, test_processes_db_migration.py). "
                "Re-verified with md5sum before/after (byte-identical) rather "
                "than the earlier file-size/mtime \"looks unchanged\" check, "
                "which is NOT sufficient to catch a pure append.\n"
                "\n"
                "OWNER DECISION (2026-08-18): the 11 already-appended events."
                "jsonl lines are NOT deleted. CLAUDE.md's append-only-audit law "
                "('never weaken... append-only audit/events') applies to test "
                "pollution the same as any other entry - the fix is to prevent "
                "recurrence, not to edit history. They remain as a small, "
                "honestly-labeled, permanent artifact of this session "
                "(timestamps 2026-08-18 11:30:56-11:41:32, kind=\"process\", "
                "greppable by actor=\"routetest-owner\"/\"test\").",
        "why_it_bites": "This is a CLASS of bug, not a one-off: any module that "
                        "computes its own `ROOT = os.path.dirname(os.path.abspath"
                        "(__file__))` and derives a storage path from it (instead "
                        "of going through db.py) is invisible to the db.ROOT "
                        "sandbox pattern every daemon test now relies on. Found "
                        "twice in one session (processes.py's STORE, events.py's "
                        "EV) purely by accident (a failing test assertion led to "
                        "manual debugging that happened to surface it) - there is "
                        "no automated check that a new test's sandbox is "
                        "complete, so a THIRD module with the same shape (grep "
                        "candidates: connectors.py, checkpoints.py, teach.py, "
                        "wincap.py, voice.py - anything with its own `ROOT =` "
                        "line) could silently repeat this the next time a route "
                        "touching it gets smoke-tested. CONFIRMED by grep (not "
                        "guessed): connectors.py (CDIR), checkpoints.py (CPDIR + "
                        "a direct settings.json/users.json path dict), and "
                        "voice.py (CACHE) each have their own `ROOT =` and a "
                        "derived storage directory, none covered by the current "
                        "sandbox. teach.py/wincap.py/presence.py were checked and "
                        "do NOT have this pattern.\n"
                        "\n"
                        "FOURTH instance found 2026-08-18, NOT by grep this time - "
                        "by a real test failure surfacing it: the routes_tracks.py "
                        "extraction's new end-to-end test hit GET /chat/history "
                        "returning real production messages on a 'fresh sandboxed "
                        "DB' assertion, because copilot.py has its own "
                        "`ROOT = os.path.dirname(...)` with CHATLOG/SESS (plus "
                        "copilot_runs/ and .copilot_attachments/ directories) none "
                        "of which test_server_routes.py sandboxed. READ-only leak "
                        "(GET /chat/history), not a write - no real data was "
                        "corrupted, only read past the sandbox boundary. Fixed: "
                        "copilot.ROOT/SESS/CHATLOG now patched in "
                        "test_server_routes.py's setup, same as connectors/"
                        "checkpoints/runs.REC above. Confirms the grep sweep from "
                        "the original incident was incomplete - a `ROOT =` grep "
                        "finds candidates but doesn't guarantee every derived "
                        "directory (copilot_runs/, .copilot_attachments/) is "
                        "covered by the same audit; the shared sandbox-helper idea "
                        "in fix (3)/(4) below would prevent this shape of miss "
                        "structurally instead of per-module.",
        "trigger": "writing a NEW daemon test that imports any module with its "
                   "own `os.path.join(ROOT, ...)` storage path; adding a new "
                   "route-dispatch group whose routes call into an unaudited "
                   "module",
        "fix": "(1) DONE: processes.py migrated into db.py (removes its ROOT/"
               "STORE entirely - the class of bug can't recur for this module "
               "because there is no longer a second path to bypass). (2) DONE: "
               "events.EV sandboxed in the two test files that currently exist. "
               "(3) INVESTIGATED (2026-08-18), then ACTED ON per-module rather "
               "than blanket-declined - connectors.py, checkpoints.py, voice.py "
               "each read in full to check whether the processes.py pattern "
               "(JSON blob -> db table) applies:\n"
               "  - connectors.py: DONE. CDIR holds the actual EXECUTABLE .py "
               "files (loaded via importlib.util.spec_from_file_location, run "
               "in a separate sandboxed subprocess - the file must exist on "
               "disk to import/exec) plus a _versions/ rollback archive; those "
               "stay on disk (not a db.py fit - would break the isolation "
               "model). Its small connectors/_state.json (last-run timestamps, "
               "a plain dict) WAS the processes.json shape - migrated: db.py "
               "gained a `connector_state` table + connector_state_get()/"
               "connector_state_put() (single-row dict, not one row per id - "
               "there is only ever one state dict), a _migrate() block that "
               "imports connectors/_state.json once and renames it .imported, "
               "and connectors._state()/_save_state() now delegate to db.py. "
               "Verified with test_connectors_db_migration.py (sandboxes "
               "db.ROOT AND connectors.CDIR/VDIR - CDIR is a second, "
               "independent global the db.ROOT patch alone would NOT catch).\n"
               "  - checkpoints.py: DECLINED, same conclusion as before but "
               "now test-covered rather than just noted. CPDIR holds "
               "directory-tree SNAPSHOTS (shutil.copytree of settings.json + "
               "the whole connectors/ dir, restored via shutil.copy2) - the "
               "value IS the file tree, not a value you'd unpack into a row; "
               "forcing it into db.py would be a real architecture change "
               "(tree-vs-blob semantics), not a mechanical extraction. Instead "
               "added test_checkpoints_db_migration.py which sandboxes CPDIR "
               "(+ROOT, since _snapshot_targets() derives settings.json/"
               "connectors from it) to a temp dir and exercises create/list/"
               "diff/restore end to end against synthetic data - the actual "
               "risk this debt item is about (a test writing into the real "
               "checkpoints/ dir or the real settings.json) is now closed by "
               "a green test, not just an assertion in this file.\n"
               "  - voice.py: DECLINED, same conclusion, same treatment. "
               "CACHE holds binary mp3 files served by URL and by raw-bytes "
               "base64 inlining - forcing these into db.py's id/data-TEXT "
               "table would mean base64-encoding audio into SQLite text for "
               "no functional gain. Added test_voice_db_migration.py, "
               "sandboxing CACHE alone and exercising path_for()/stats()/"
               "_prune() (traversal guard, missing-dir tolerance, MAX_FILES "
               "eviction) against synthetic files - render()/render_b64() "
               "need edge_tts + network so are intentionally left untested "
               "here (that is voice.available()'s fails-soft path, not this "
               "debt item's concern).\n"
               "All three new test files were run against a REAL freshly-"
               "created tempfile.mkdtemp() sandbox, never daemon/'s real "
               "connectors/, checkpoints/, voice_cache/, processes.json, "
               "events.jsonl, settings.json, users.json, or helmdeck.db - "
               "confirmed by md5sum before/after (byte-identical) for every "
               "real file, plus a directory-entry-count check for connectors/, "
               "checkpoints/, voice_cache/, both before and after the full "
               "daemon test suite ran clean (all 16 test_*.py files, "
               "including the 3 new ones, zero regressions). "
               "(4) OPEN: a SHARED test-sandbox helper (a single "
               "`sandbox_daemon_storage(tmp)` function patching db.ROOT/"
               "db.DBPATH, events.EV/events.SET, auth.USERS/auth.SESS, "
               "connectors.CDIR/VDIR, checkpoints.CPDIR, voice.CACHE all in "
               "one place) would still be worth adding so a future test "
               "author doesn't have to re-derive this list from scratch - each "
               "of the three new tests above hand-rolls its own subset today. "
               "(5) DONE going forward: this session's migration/sandbox work "
               "verified with md5sum/checksum, not file-size+mtime, per the "
               "earlier lesson that mtime cannot distinguish 'untouched' from "
               "'grew by a plausible amount'. "
               "(6) DONE 2026-08-19, a DIFFERENT sub-risk than (4) - don't "
               "conflate them: the physical Cell-folder reorg (cells/"
               "<id>/ + spine/) needed every module's independent "
               "`ROOT = dirname(abspath(__file__))` (~26 copies) consolidated "
               "into one shared daemon/_subpaths.py, since a file moving one "
               "folder deeper would otherwise have silently broken its own "
               "path math with no error. This closes the ROOT-DUPLICATION "
               "half of the class of bug (every module's storage-root "
               "constant is now correct regardless of the file's physical "
               "location), but does NOT close item (4) - each module's "
               "OWN derived directory (events.EV, connectors.CDIR/VDIR, "
               "checkpoints.CPDIR, voice.CACHE, copilot.SESS/CHATLOG, "
               "copilot_runs/, .copilot_attachments/) still needs per-module "
               "sandboxing in new tests; a shared sandbox helper remains "
               "worth adding.",
        "order": 32,
    },
    {
        "id": "cell-registry-daemon-plugin-kernel",
        "title": "Cells: agentic systems as pluggable units - ALL FIVE cells "
                 "conformed (Phase 0-3 complete)",
        "status": "paid",
        "what": "Owner decree: the unit of modularity is an agentic SYSTEM (a "
                "'Cell' - a role like PM or Engineer), each bundling {logic, "
                "storage, ops/harness/role, API routes, UI surface, lifecycle, "
                "enable-flag}, plugged into a shared spine, addable/removable/"
                "swappable without editing the spine. This is the daemon-side "
                "realization the full-dynamism-decree (order 30) deferred ('no "
                "daemon-side plugin registry exists'). Phase 0 SHIPPED: "
                "cells.py (pure-data Cell registry, 5 cells - engineer/pm/"
                "process/connectors/copilot); server.py do_GET/do_POST gate a "
                "path owned by a disabled cell (cells.path_disabled, ONE derived "
                "check after auth, no per-route edits); boot's flat start_*() "
                "calls -> cells.start_enabled() loop; 5 <cell>Enabled policy "
                "flags (all true) toggled through the EXISTING tracked "
                "policy.swap; GET /cells manifest; keys.ts PolicySet mirror. Zero "
                "behavior change while all enabled (verified: 16-file suite green, "
                "real files md5-identical). Phase 1 SHIPPED (Connectors, the "
                "reference cell, conformed end-to-end): "
                "surfaces/app/src/plugins/surfaces/connectors.tsx (Surface+Plugin pair, id "
                "'surfaces.connectors', strangler-wraps the existing (tabs)/"
                "connectors.tsx screen with zero logic moved) registered in "
                "AVAILABLE_PLUGINS (surfaces/app/src/boot/index.ts), filling the dead "
                "owner.json reference; api.cells() added to surfaces/app/src/data/client.ts "
                "(typed CellInfo[], mirrors GET /cells); (tabs)/_layout.tsx gates "
                "both the bottom-bar and desktop-sidebar tab lists generically off "
                "the /cells manifest (useDisabledCellSurfaces + "
                "isSurfaceCellDisabled, keyed by surface id / route suffix, not "
                "hardcoded to 'connectors' - cells 2-5 pick it up automatically "
                "once they register a Surface with nav+route); modules.tsx got a "
                "CELLS section (id/role/surface/modes + a Switch through the "
                "existing setPolicy -> POST /policy/swap path). tsc --noEmit "
                "clean; /cells fetch failures fall back to an empty list "
                "everywhere (never crashes Modules, never blocks nav). "
                "Phase 2 SHIPPED (PM, Process, Copilot conformed, same "
                "template): surfaces/app/src/plugins/surfaces/{pm,processes,copilot}.tsx "
                "(Surface+Plugin pairs, ids 'surfaces.pm'/'surfaces.processes'/"
                "'surfaces.chat' matching cells.py's Cell.surface and the "
                "owner.json/store.json profile references exactly) registered in "
                "AVAILABLE_PLUGINS; pm.tsx composes the two existing PM pieces "
                "(ui/pm_panel.tsx's PMStatusPanel + app/loopmap.tsx's "
                "LoopMapScreen) into one dashboard component, processes.tsx/"
                "copilot.tsx strangler-wrap (tabs)/processes.tsx and app/chat.tsx "
                "unmodified, same as Connectors. (tabs)/_layout.tsx's nav-gate "
                "filter (useDisabledCellSurfaces/isSurfaceCellDisabled) needed NO "
                "further edits - re-verified generic (keys off any surface's "
                "`surface` field against the live /cells manifest, not "
                "hardcoded to a specific id). Cross-cell-hook guards added, ONE "
                "owner per hook (NO-MONKEY-PATCH): pm.on_card_done and pm._tick "
                "both gain a `cells.enabled_id(\"pm\")` early-return (the _tick "
                "one is ADDITIONAL to the existing loop_enabled check - a "
                "DIFFERENT flag, the owner's proactive on/off, not the whole-cell "
                "kill switch); processes.clear_step_stamps gains the same guard "
                "for \"process\". processes.sync/_priority_dispatch/_autopilot "
                "got NO extra guard: they only run via start_chain_poller, which "
                "cells.start_enabled() never launches when processEnabled is "
                "false at boot, and their only route entry (GET /processes -> "
                "routes_misc.processes_get -> sync()) is already 404'd by "
                "cells.path_disabled before the handler runs. copilot.py got NO "
                "guard at all, deliberately: copilot.say() is called BY pm.py/"
                "lanemachine.py (cross-cell PM->copilot voice, e.g. "
                "lanemachine.py:462, pm.py:1814) rather than the other way "
                "round, so gating it would silently swallow a legitimate "
                "cross-cell message from a cell that IS enabled; the copilot "
                "cell has no `start` lifecycle in cells.py (no poller to guard) "
                "and its own surface is exactly its /chat* routes, already "
                "gated by server.py's cells.path_disabled check from Phase 0. "
                "Tests: test_server_routes.py's cell-gate section extended with "
                "the same disable/spine-stays-up/re-enable round trip for "
                "process (/processes) and copilot (/chat/history), plus a "
                "direct-call assertion that processes.clear_step_stamps no-ops "
                "on a seeded stamp while processEnabled=false and clears it for "
                "real once re-enabled (proves the guard, not a broken test). "
                "16-file daemon suite green, real files (processes.json/"
                "events.jsonl/settings.json/users.json/helmdeck.db) md5-"
                "identical before/after. tsc --noEmit clean. "
                "Phase 3 SHIPPED (Engineer, the crown jewel, conformed last): "
                "surfaces/app/src/plugins/surfaces/board.tsx VERIFIED against the "
                "Connectors template (id 'surfaces.board' matches "
                "cells.py's Cell.surface exactly, already registered in "
                "AVAILABLE_PLUGINS since Phase 0, no code changes needed) and "
                "the (tabs)/_layout.tsx nav-gate filter traced by hand: "
                "nav.tabs' tab.board entry (route \"board\") matches the "
                "disabled-set suffix derived from \"surfaces.board\", so "
                "disabling engineerEnabled hides the tab exactly like "
                "Connectors - wiring was already correct, no change needed. "
                "Lifecycle DECISION: sessions.start_engineer_lifecycle() (new, "
                "wraps start_zombie_reconciler + start_background_watcher) is "
                "now registered as cells.py's engineer Cell.start and "
                "launched via cells.start_enabled(); the two flat calls in "
                "server.py's serve() were removed. Reasoning: both pollers act "
                "directly on track/session state (status field, session "
                "liveness, background-task completion) - genuinely Engineer's "
                "own lifecycle, not spine-adjacent housekeeping - so they "
                "belong behind engineerEnabled like every other cell's "
                "poller. sessions.sweep_worktrees() stays a FLAT one-shot boot "
                "call (a startup backstop pass, not a continuous poller - "
                "nothing to stop on disable), alongside "
                "apply_board_directives/backfill_outcomes. Cross-cell guard "
                "DECISION: sessions.new_track / lanemachine.move_lane got NO "
                "disable guard, deliberately - the route-level gate "
                "(cells.path_disabled, already live from Phase 0) already "
                "covers the only path an external actor (UI/API) can reach "
                "Engineer through; other cells calling sessions.*/"
                "lanemachine.* directly (process.py, connectors.py, "
                "copilot._run_action) is the daemon acting on itself, not an "
                "external actor bypassing the disable - same reasoning that "
                "left copilot.py unguarded in Phase 2. Also fixed a Phase 0 "
                "typo-risk: cells.py's engineer prefixes dropped \"/track/\" "
                "(singular) - grepped server.py + routes_tracks.py, no route "
                "ever used that prefix, only \"/tracks/\" is real. Tests: "
                "test_server_routes.py's tracks-cluster section now opens "
                "with a disable/enable cycle for engineer (GET /tracks and "
                "POST /tracks/new both 404 'cell disabled', /me spine path "
                "stays up, /pm/plan - a DIFFERENT cell - keeps working "
                "proving the gate is per-cell, /cells reflects the toggle), "
                "then re-enables and lets the EXISTING real dispatch/"
                "gate-pass/gate-blocked round trip run unmodified, proving "
                "the cycle didn't corrupt real track state. 15-file daemon "
                "suite green (test_server_routes.py's entire tracks-cluster "
                "section, including the pre-existing gate round trips, still "
                "passes). Real files md5-identical before/after. tsc "
                "--noEmit clean.\n"
                "\n"
                "CORRECTION found + fixed while attempting the UI screenshot "
                "verification pass (owner restarted the live daemon to pick "
                "up this code - see the cardadmin.py json-import fix nearby "
                "in this same session, a real boot-crashing bug the restart "
                "surfaced): the Phase 1-3 claim that 'the app hides a "
                "disabled cell's nav surface' for all five cells OVERSTATED "
                "what was actually wired. Checked nav.tabs's real entry list "
                "(surfaces/app/src/plugins/surfaces/tabs.ts): only board/processes/"
                "connectors have a tab.* entry, so only THOSE THREE cells' "
                "nav-hiding genuinely worked. PM and Copilot have NO tab to "
                "hide - PM's UI is embedded inline (PMStatusPanel in the "
                "owner dashboard, PMControls in Settings), Copilot's entry "
                "point is a floating chat button + overlay, not a route. "
                "Toggling pmEnabled/copilotEnabled off hid nothing in "
                "production; the routes 404'd but the UI stayed fully "
                "visible and clickable. FIXED: surfaces/app/src/data/cells.ts "
                "(useCellEnabled(id), a small shared hook, same 'cells' "
                "query key as _layout.tsx's local one so react-query dedupes "
                "the fetch) wired directly at the three render points - "
                "index.tsx's PMStatusPanel + copilot FAB/overlay, board.tsx's "
                "copilot FAB/overlay (NOT the adjacent Engineer '+new card' "
                "button), settings.tsx's PMControls panel. tsc clean; daemon "
                "suite re-verified green + real-files-identical (app-only "
                "change). Also surfaced a SEPARATE, larger pre-existing gap "
                "while tracing this (debt order 29, plugin-kernel-dual-nav, "
                "already open, NOT re-litigated here): production boots the "
                "'app' profile (surfaces/app/profiles/app.json), which loads nav.tabs "
                "for tab METADATA but does NOT load the surfaces.board/pm/"
                "processes/connectors/copilot Surface PLUGINS built in "
                "Phase 1-3 - those are only loaded by the 'owner'/'store' "
                "profiles (not what boots by default) and by the dev-only "
                "kernel-demo.tsx route. Production screens still render from "
                "the plain expo-router files, not through Surface.component - "
                "the Cell's 'own UI surface, wired via the kernel' framing is "
                "therefore true in the sense that the Plugin exists and "
                "compiles, NOT in the sense that it drives production "
                "rendering. That is exactly order 29's pre-existing scope; "
                "this entry's job (per-cell enable/disable working correctly) "
                "does not require fixing it, since the nav-gate and the new "
                "direct-render guards both operate on what's ACTUALLY "
                "rendered (nav.tabs entries + the real screen files), not on "
                "the unused Surface.component path.\n"
                "\n"
                "KNOWN REMAINING GAP, not chased (diminishing returns - "
                "secondary/informational, not a primary entry point): "
                "more.tsx's static LINKS list still shows a 'loopmap' link "
                "(PM content) regardless of pmEnabled.\n"
                "\n"
                "THE SWEEP IS COMPLETE for what this entry set out to do: all "
                "five cells (engineer/pm/process/connectors/copilot) conform "
                "on the daemon side (registry + route gate + lifecycle) and, "
                "on the app side, ACTUALLY hide their real UI entry point "
                "when disabled (tab-hide for 3, direct component-gate for 2) "
                "- with every judgment call, including this correction, "
                "reasoned and recorded above. Nothing further is deferred within this"
                "item.",
        "why_it_bites": "RESOLVED. Phase 0-3 cover the registry, daemon "
                        "gating, all five cells' surfaces, their cross-cell-"
                        "hook guards (or reasoned absence thereof), and "
                        "Engineer's lifecycle + stress-tested route gate. "
                        "Kept as a paid, listed item per the debt-register "
                        "law (register once, never silently delete) so the "
                        "phase history and every judgment call stay "
                        "discoverable.",
        "trigger": "adding a new agentic system; a UI toggle for a whole system; "
                   "any claim the daemon is fully cell-modular",
        "fix": "PAID. Phase 0-3 all shipped (see 'what' above). "
               "DESIGN DECISION (evidence-backed, not an omission): machine-"
               "control and direct-task are NOT peer cells - they are MODES of "
               "the Engineer cell (a card variant with machine=True, forking only "
               "at dispatch.py:160 dispatch, _accept_machine no-merge accept, and "
               "drivers._agent_for role selection; sharing the same track store, "
               "gate, worktree, and merge state machine). Registering them as "
               "peers would duplicate the gate/merge machinery. They stay gated "
               "by the existing policy.machine, with their own role file "
               "(machine-worker.md) and the recordings surface.",
        "order": 33,
    },
    {
        "id": "expo-cutover-login-regression",
        "title": "The Expo cutover never ported the old web app's login "
                 "screen - fixed 2026-08-18",
        "status": "paid",
        "what": "The owner got locked out of their own app: browser session "
                "held a non-owner token, no logout control anywhere, and no "
                "way to log back in with a username/password. Root cause, "
                "confirmed by reading git history (NOT assumed) - the OLD "
                "Next.js web app (archived at the Expo cutover, commit "
                "6625edc, 'moved not deleted') had a real, working AuthGate "
                "component (web/components/auth.tsx, retrieved from history: "
                "sign-in / create-account / first-run-setup tabs, calling "
                "POST /auth/login, /auth/register, /auth/setup). It worked "
                "there because that app was same-origin (Next.js proxied "
                "/backend/* -> :8140), so a plain cookie session was enough. "
                "This component was simply never ported to Expo during the "
                "cutover - the expo-cutover-pipeline debt (order unknown, "
                "already paid) was marked complete without this gap being "
                "caught, because the Expo app's ENTIRE auth model became "
                "pairing-only (config.ts's own comment: 'pairing IS the "
                "app's login' - a QR code or #cfg= link sets baseUrl+token "
                "directly). Once a token went bad, there was no way back in "
                "short of a fresh pairing link from another device.",
        "why_it_bites": "A real, user-facing lockout with no in-app recovery "
                        "path - exactly what happened today. The daemon's "
                        "auth.py/routes_auth.py always had full username/"
                        "password support; the gap was 100% missing frontend "
                        "UI plus one real mismatch: /auth/login only ever "
                        "returned a COOKIE session (sd_session), while the "
                        "Expo app's actual request layer (data/client.ts) "
                        "authenticates every call with a BEARER TOKEN from "
                        "useConfig().token - a cookie-only login would not "
                        "have actually authenticated the app's own fetches "
                        "even with a login screen bolted on (different "
                        "mechanism entirely, and cross-origin in dev besides).",
        "trigger": "a token expires, gets revoked, or belongs to the wrong "
                   "role/account, and no other already-authenticated device "
                   "is available to generate a fresh pairing link",
        "fix": "PAID. (1) daemon/routes_auth.py: auth_login/auth_setup/"
               "auth_register all now ALSO mint a real device token via the "
               "EXISTING auth.issue_token() (same primitive the owner-only "
               "/users/<name>/tokens route and /relay/pair's QR flow already "
               "use - no new auth mechanism) and return it in the response "
               "body, alongside the unchanged cookie (backward compatible). "
               "(2) surfaces/app/src/data/authgate.ts (new): a small zustand store "
               "(same shape as data/health.ts's useHealth) - needsLogin, "
               "flipped by client.ts's existing 401 detection (the ONE place "
               "that already classifies AuthRequired) and by Settings' new "
               "logout button. Demo mode is exempt at the source (client.ts "
               "checks useDemo.getState().active before reporting). (3) app/"
               "src/ui/login_screen.tsx (new): restores the OLD AuthGate's "
               "three modes (setup/register/sign-in) against the SAME "
               "daemon routes, styled like onboard.tsx. client.ts's req() "
               "gained a skipAuthGate param so /auth/login's OWN 401 (wrong "
               "password) surfaces as a real ApiError with the daemon's "
               "actual message, not a message-less AuthRequired. (4) app/"
               "src/app/_layout.tsx: the needsLogin gate sits next to the "
               "existing showOnboard gate, same full-screen-blocking shape. "
               "(5) Settings gained an always-visible 'Abmelden' button (not "
               "gated behind the owner-only settings block - logout must "
               "work even when /settings itself can't load).\n"
               "Verified: py_compile clean; test_server_routes.py gained 6 "
               "new assertions (a real HTTP /auth/login round trip, "
               "confirming the token alone - NO cookie - authenticates GET "
               "/me, plus the wrong-password 401 case); full 16-file daemon "
               "suite green aside from a pre-existing, already-documented "
               "system-load timing flake unrelated to this change (a "
               "different route each time, confirmed by re-running "
               "standalone). tsc --noEmit clean.",
        "order": 34,
    },
    {
        "id": "cell-6-buildloop-self-governing",
        "title": "The build loop registered as Cell #6 - real enable flag, "
                 "not cosmetic - fixed 2026-08-18",
        "status": "paid",
        "what": "Owner pushback (correct): the build loop (ops/tools/loop_state.py "
                "- ALIGN>ANALYZE>EXECUTE>TEST>CLEAN>BUILD>COMMIT, enforced by "
                "the Stop/SessionStart hooks in .claude/settings.json) has the "
                "same shape as every other Cell - its own harness (CLAUDE.md), "
                "its own states/gates, a UI presence (/loop/map, loopmap.tsx) "
                "- structurally the same as Copilot/Henry. It was left out of "
                "the original Cell sweep (see cell-registry-daemon-plugin-"
                "kernel, order 32) purely because it isn't daemon-hosted. "
                "STRUCTURAL DIFFERENCE, stated plainly rather than papered "
                "over: the Stop hook is enforced entirely OUTSIDE the daemon "
                "- .claude/settings.json calls `py -3.12 ops/tools/loop_state.py "
                "--stop-hook` as a local script, no HTTP round-trip. The "
                "daemon's /loop/map (routes_info.py's _loop_machine) is a "
                "READ-ONLY MIRROR of the same module for display, not the "
                "actual enforcement path. Registering this as Cell #6 only "
                "means something if the enable flag is REAL: "
                "ops/tools/loop_state.py gained _build_loop_enabled(), which "
                "reads daemon/policy_live.json directly (falling back to "
                "policy_seed.json, mirroring policy.py's own seed-then-live "
                "semantics) with NO daemon round-trip (the tool must work "
                "even when the daemon isn't running) - fails OPEN (True) on "
                "any missing key or read error, since a missing/corrupt "
                "policy file must never silently disable the safety net. "
                "stop_hook() checks it first (same early-return shape as the "
                "existing stop_hook_active check) - when disabled, prints "
                "nothing and returns, no block. session_start() prints one "
                "extra NOTE line when disabled (the safety net going quiet "
                "must never be silent about being off - stop_hook has no "
                "such equivalent by design, so session_start is the one "
                "place a disabled build loop is visibly announced). "
                "cells.py: Cell gained a repo_files=() field (files at "
                "repo ROOT, not under daemon/ or app/ - ops/tools/loop_state.py "
                "is repo-root); allowed_files()/where()/manifest() extended "
                "to fold repo_files into the same 'logicFiles' the app "
                "already renders. New buildloop Cell registered: enabled_key "
                "'buildLoopEnabled', repo_files=('ops/tools/loop_state.py',), "
                "harness_file='CLAUDE.md', ui_files=('src/app/loopmap.tsx',) "
                "shared with engineer (noted in role, not double-claimed), "
                "route_modules deliberately EMPTY - it has no HTTP surface, "
                "self-governing not delegated. daemon/policy_seed.json + "
                "surfaces/app/src/kernel/keys.ts PolicySet + surfaces/app/src/boot/policies.ts "
                "CHARTER_DEFAULTS all gained buildLoopEnabled:true, matching "
                "every other cell flag's seeding pattern exactly. SAFETY: "
                "this touches the exact mechanism governing the current "
                "agent's own session, so every test used a sandboxed temp "
                "policy file via loop_state._POLICY_ROOT (never the real "
                "daemon/policy_live.json) - the real file's md5 "
                "(7181f5e5d61836a4e8d315204d2dbdea) was confirmed "
                "byte-identical before/after every test run in this session, "
                "including the full 17-file daemon suite. The real "
                "policy_live.json predates all 6 cell flags (only has the "
                "original 7 keys) and was never touched to add them - "
                "buildLoopEnabled resolves True today purely via the "
                "fail-open default, exactly like every other cell flag on a "
                "live daemon that hasn't been restarted since the Cell sweep. "
                "Because buildLoopEnabled lives in the same policies section "
                "as every other flag, it already inherits the existing "
                "agentMaySwap=false protection (an agent cannot flip it "
                "without a human confirm) - no new safety mechanism needed.",
        "fix": "PAID. ops/tests/test_loop_state_policy.py (new, sandboxed): "
               "8 assertions - fail-open on no file, seed-only True, "
               "live-missing-key defaults True, live=false really returns "
               "False, corrupt-live falls through to seed, stop_hook() "
               "prints nothing when disabled, real repo resolves True "
               "untouched, real policy_live.json md5-identical before/after "
               "(the one check in this session that is not optional). "
               "test_server_routes.py's /cells manifest assertion extended "
               "from five to six agentic systems, plus buildloop-specific "
               "checks (enabled by default, real logicFiles, harnessFile, "
               "empty routes). Full 17-file daemon suite green; real state "
               "files (processes.json/events.jsonl/settings.json/"
               "users.json/helmdeck.db) mutate normally from the "
               "integration tests that exercise them (unrelated to this "
               "change) while policy_live.json stays byte-identical. "
               "tsc --noEmit clean. NOT done live: the toggle was not "
               "flipped against the real daemon (deliberately, per the "
               "plan) - buildloop's correctness is proven by the sandboxed "
               "test, not by live-disabling the mechanism currently "
               "governing this agent's own session.",
        "why_it_bites": "Without a real (not cosmetic) enable flag, an owner "
                        "toggling buildloop off in /modules would silently do "
                        "nothing - the Stop hook would keep blocking regardless, "
                        "since it lived entirely outside the daemon and never "
                        "consulted policy at all before this fix. A control "
                        "that visibly exists but doesn't work is worse than no "
                        "control, and erodes trust in every other cell's toggle "
                        "too.",
        "trigger": "an owner disables buildLoopEnabled via /modules expecting "
                   "the Stop hook to stop blocking, or restarts the daemon "
                   "after this change and checks /cells for the sixth entry",
        "order": 35,
    },
    {
        "id": "sys-path-trick-to-real-package-imports",
        "title": "daemon/ became a real Python package (was a sys.path-"
                 "augmentation trick) - closes the owner's 'monkey patch' "
                 "pushback on the Cell-folder physical move",
        "status": "paid",
        "what": "Owner pushback on the just-shipped Cell-folder reorg "
                "(cells/<id>/ + spine/): daemon/_subpaths.py's "
                "sys.path augmentation (ensure_cell_paths(), called by every "
                "entrypoint before any other daemon-local import) made "
                "physical relocation safe with zero import-statement "
                "rewrites, but read as a workaround rather than real package "
                "structure - correctly, since Python's normal import graph "
                "couldn't see the real dependency between e.g. pm.py and "
                "sessions.py, only sys.path resolution could. Fixed: "
                "daemon/__init__.py + spine/__init__.py + "
                "spine/routes/__init__.py + cells/<id>/"
                "__init__.py for all 5 cells; _subpaths.py -> paths.py "
                "(keeps REPO_ROOT/DAEMON_ROOT, drops the sys.path mutation "
                "entirely); every flat `import X`/`from X import Y` across "
                "~90 files (spine/cells/routes/ops/tests/tools) rewritten to "
                "absolute `from spine import X` / "
                "`from cells.<id> import X` (mechanical, via a "
                "one-off script, not hand-edited file by file). Entry point "
                "invocation changed: `cd daemon && py -3.12 swarm.py serve` "
                "-> `py -3.12 -m daemon.swarm serve` from the REPO ROOT "
                "(swarm.py/mint_token.py themselves stay physically flat in "
                "daemon/ - they're spawned by surfaces/desktop/tray.py/surfaces/desktop/"
                "main.js via a relative script path, changing THAT is a "
                "separate, larger blast radius not worth taking here).\n"
                "\n"
                "Two real bugs found and fixed while converting, both worth "
                "recording since they're a CLASS, not one-offs: (1) a bare "
                "`import mod` (binding the whole module object, used as "
                "`mod.attr`) needs `from <owning-package> import mod`, NOT "
                "`from <owning-package>.mod import mod` - the latter tries "
                "to import an attribute literally named `mod` from inside "
                "mod.py itself, which doesn't exist except for the one "
                "genuine exception (distill.py defines a function called "
                "`distill`, so `from spine.distill import distill` "
                "IS correct there - confirmed by checking which module "
                "names self-define a top-level symbol matching their own "
                "filename, only one match repo-wide). An early heuristic "
                "fix ('skip if the package path already ends in .modname') "
                "was ITSELF wrong for cells/pm/pm.py-shaped cases (the "
                "folder is also named pm, so `cells.pm` legitimately "
                "ends in `.pm` even in the CORRECT form) - replaced with a "
                "ground-truth pass that compares against the actual module "
                "map instead of pattern-matching the string shape. (2) "
                "spine/cells.py's `start=`/`route_modules=` tuples "
                "store BARE module names (e.g. 'routes_pm') because they "
                "ALSO double as the read_source() security allowlist "
                "(Cell.allowed_files()) - changing them to dotted paths "
                "would have broken the allowlist matching, so instead a new "
                "_import_by_bare_name() helper translates bare-name-plus-"
                "owner-cell-id to the real dotted import at the two call "
                "sites that actually need to import (start_enabled(), "
                "_cell_routes()), trying the owning cell's own package "
                "first then both spine locations - the allowlist tuples "
                "themselves are untouched. Cell.daemon_roots() (added "
                "during the earlier physical-move phase) had the same class "
                "of latent gap - only searched cells/<id>/ and flat "
                "daemon/, never spine/ or spine/routes/, so "
                "read_source() for a cross-cell-owned route module like "
                "process's routes_misc.py would have silently 404'd; fixed "
                "by adding both spine locations to its search order.\n"
                "\n"
                "INCIDENT surfaced by this same conversion: test_policy.py's "
                "module stub (`sys.modules[\"events\"] = _fake`) stopped "
                "intercepting policy.py's events calls once policy.py's "
                "import became `from spine import events` (a "
                "different sys.modules key). Result: the test's real "
                "policy.swap() call (actor=\"user\", note=\"raise WIP\", "
                "wipLimit 10->5) had its EVENT emission leak to the real "
                "daemon/events.jsonl (one line, timestamp 2026-08-19 "
                "20:03:27) while its STATE writes stayed correctly "
                "sandboxed (policy.LIVE is a direct module-attribute patch, "
                "unaffected by import style - confirmed the real "
                "policy_live.json's mtime predates this session entirely, "
                "2026-08-18, so it was never touched). Per the append-only "
                "audit law (same precedent as the earlier processes.json/"
                "events.jsonl incident), that one line is NOT deleted - it "
                "remains as a small, honestly-labeled artifact. Fixed: the "
                "stub now targets `sys.modules[\"spine.events\"]` "
                "and also stubs `.settings()` (policy.load()'s first-run "
                "wipLimit seed reads it, previously silently hitting the "
                "real settings.json too).",
        "why_it_bites": "Any future daemon test or tool script that stubs a "
                        "module via `sys.modules[\"bare_name\"] = fake` will "
                        "silently stop working the same way if the code "
                        "under test uses the (now-standard) package-"
                        "qualified import - the stub installs under the "
                        "wrong key and the real module gets used instead, "
                        "with NO error, just quietly-wrong sandboxing. Grep "
                        "for `sys.modules[\"` (bare name, no `daemon.` "
                        "prefix) in any new test as a smell.",
        "trigger": "writing a new daemon test that stubs a module via raw "
                   "sys.modules assignment instead of monkeypatching the "
                   "already-imported module object's attributes directly",
        "fix": "PAID. Verified: full 17-file daemon test suite green "
               "(test_policy.py re-run standalone confirms real "
               "events.jsonl/policy_live.json byte-identical before/after); "
               "spine.cells manifest/_cell_routes()/read_source() "
               "all confirmed working for every cell including cross-cell-"
               "owned route modules (process's routes_misc.py); "
               "cells.engineer.cards.sessions (the crown jewel, heaviest "
               "cross-module fan-in) imports and its flow()/lane-graph "
               "file:line citations resolve correctly. ops/tools/loop_state.py's "
               "checks_red() (daemon-wiring subprocess check) and "
               "hygiene_problems() (debt register import) both updated to "
               "the real package form and verified against the real repo. "
               "RECURRED once more (2026-08-19, Phase 1 of the same reorg - "
               "spine/ sub-grouped into spine/{http,storage,auth,agent,git,"
               "media,comms,registry,turn,ops}/): policy.py itself moved to "
               "spine.auth.policy and its internal events import "
               "target moved to spine.storage.events, so "
               "test_policy.py's sys.modules stub key went stale AGAIN - "
               "one more leaked event line, same harmless shape (a policy-"
               "swap test artifact), not deleted. Root cause confirmed "
               "structural, not a one-off: policy.py's events import is "
               "genuinely lazy (events.py ALSO lazily imports policy.py for "
               "its own wip_limit fallback - a real two-way cycle, not "
               "historical caution), so `sys.modules[\"<dotted-path>\"] = "
               "fake` is the only viable stub technique here - it will "
               "stay fragile to any future move of policy.py OR events.py "
               "specifically. Accepted as a known, bounded cost rather than "
               "restructured away.",
        "order": 36,
    },
    {
        "id": "semicolon-combo-imports-survived-the-rewrite",
        "title": "7 UNGUARDED semicolon-combined bare imports (import X; X.fn())"
                 " survived the Phase 0 import-rewrite - found + fixed 2026-08-20",
        "status": "paid",
        "what": "The Phase 0 import-rewrite script (sys-path-trick-to-real-"
                "package-imports, order 36) explicitly skipped any line "
                "containing a semicolon (`if ';' in rest: return None`) - a "
                "deliberate guard for the ONE known semicolon-combo pattern "
                "at the time (`import _subpaths; _subpaths.ensure_cell_"
                "paths()`), but the guard was blanket, not pattern-specific, "
                "so it silently skipped every OTHER `import X; X.fn(...)` "
                "line too. Found while reading pm.py's own remaining tick "
                "logic for a further extraction pass: `start_loop()`'s error "
                "handler still had a bare `import events; events.log(...)`. "
                "A targeted repo-wide grep for the same shape then found 7 "
                "more, all in cells/engineer/{dispatch,lanemachine}.py "
                "- the gate/merge/accept 'crown jewel' flow: `import notify; "
                "notify.card_event(t, \"done\"|\"bounced\")` (6 call sites, "
                "NONE wrapped in try/except) and `import pm; pm.on_card_done"
                "(...)` (2 call sites, both already try/except-wrapped, so "
                "those two were merely silently no-op'ing rather than "
                "crashing).",
        "why_it_bites": "The 6 unguarded notify.card_event call sites are "
                        "hard AttributeError/ModuleNotFoundError crashes with "
                        "NO try/except around them, sitting directly in "
                        "lanemachine.py's bounce path (gate-red, merge-"
                        "conflict, dirty-checkout - 3 of the 4 real bounce "
                        "kinds) and its accept/merge path, plus dispatch.py's "
                        "machine-task accept path. Any card actually bouncing "
                        "or landing through these paths after the Phase 0 "
                        "commit (c00ac2c onward) would have crashed mid-"
                        "mutation - the card's status/lane update already "
                        "happened (via _mutate) but the notification and "
                        "everything after it in the same function never ran, "
                        "an inconsistent half-finished state.",
        "trigger": "a card actually bounces (gate red / merge conflict / dirty "
                   "checkout) or completes via lanemachine.py's accept path, "
                   "or a machine-task card completes via dispatch.py's "
                   "accept path, while the daemon is running code from the "
                   "affected commit range",
        "fix": "PAID. Checked the real daemon/events.jsonl DB table for any "
               "'done'/'lane'/'touch' event after the Phase 3 live restart "
               "(2026-08-19 ~20:0x, the first restart to run the affected "
               "code) - zero rows. No card activity happened in that window, "
               "so this was a real, live-armed bug that was never actually "
               "triggered, not an incident with real impact. Fixed: all 7 "
               "sites converted to the same two-line "
               "`from daemon.X.Y import mod` + `mod.fn(...)` form used "
               "everywhere else post-Phase-0 (notify -> spine.comms, "
               "pm -> cells.pm). Also grepped the entire daemon tree "
               "for the same `import \\w+;` shape with zero remaining hits - "
               "this was exhaustively swept, not spot-fixed. Verified: "
               "compiles clean, full 17-file daemon test suite green, real "
               "state files byte-identical before/after. The live daemon "
               "still needs a restart to pick up this fix (not done as part "
               "of this fix - the owner restarts deliberately per this "
               "session's established practice).",
        "order": 37,
    },
    {
        "id": "gate-light",
        "title": "Per-card gate is CODE+FUNCTION only - the full suite is a base monitor, not a blocker",
        "status": "open",
        "what": "Owner-decreed 2026-08-21 after a day that made the cost "
                "measurable: the old gate ran every test_*.py per card - 66 "
                "files, ~8min, +3-7 files/day, 504s under load, red on base "
                "breakage a card never touched (i18n_lint), red on box load "
                "(a Gradle build starved test_driver_session's timing "
                "assertion), and a starved worker double-started it. "
                "ops/tools/run_gate.py is now the LIGHT gate: py_compile over all "
                "daemon packages + design/i18n lints (code check) + `import "
                "daemon.swarm` (function check - the daemon wires up), "
                "seconds total. The full suite moved verbatim to "
                "ops/tools/run_suite.py as a BASE health monitor: run it against "
                "the trunk on a schedule or after a batch; red = fix the "
                "trunk, never bounce a card. This is the Paseo lesson "
                "adopted deliberately: behavior verification is the owner "
                "testing the deploy (fast-track), machine judgment guards "
                "hygiene only.",
        "why_it_bites": "A card can now pass the gate with a real behavioral "
                        "regression the suite would have caught - it lands on "
                        "base and deploys (fast-track) before run_suite.py "
                        "notices, hours later at best. Night-shift batches "
                        "compound this: several merges can stack before the "
                        "monitor runs, and bisecting which one broke the "
                        "suite is manual. The suite itself now has no "
                        "enforced runner - if nothing schedules it, it rots "
                        "into the same nobody-runs-it state ops/tests/test_*.py "
                        "was rescued from.",
        "trigger": "a merged card breaks behavior an old pin covered; a build "
                   "ships without any assembled-system verification",
        "fix": "Decree v2 (owner, 2026-08-21): NO recurring unit-suite run at "
               "all - a fix's test runs ONCE at the fix's own verification, "
               "then retires as replayable documentation (run_suite.py stays "
               "manual, for when someone deliberately reworks a subsystem). "
               "The only recurring verification is E2E at the integration "
               "points: wire the e2e_* tests + Playwright smoke (live daemon "
               ":8140 + web :3300) as the pre-build/pre-ship step in "
               "build_apk.sh/ship.sh. Real-system checks before real "
               "releases; never re-fatten the gate and never schedule the "
               "unit suite.",
        "order": 38,
    },
    {
        "id": "voice-native-stt",
        "title": "Voice mode HEARS on web/desktop only - native has no speech-recognition module",
        "status": "paid",
        "what": "Voice mode (surfaces/app/src/ui/voice_mode.tsx) ships both halves of a "
                "spoken turn, but only web/desktop can currently do both. "
                "SPEAKING works everywhere the daemon reaches: the reply is "
                "rendered server-side by voice.py and played via expo-audio "
                "(native) or HTMLAudioElement (web). HEARING is platform work, "
                "and the intended package - jamsch/expo-speech-recognition - "
                "HAS NO SDK 57 BUILD: npm `latest` is 56.0.1, the repo's main "
                "branch still pins expo ~56.0.12, and there is no sdk-57 "
                "dist-tag, release note or tracking issue (checked "
                "2026-08-21). Its peerDependencies are wildcards (expo:*, "
                "react-native:*), so it would INSTALL silently into an SDK 57 "
                "app and only fail at native build time - which is why it was "
                "deliberately NOT added as a dependency. On native, "
                "data/voice.ts caps() therefore reports hear:false and the UI "
                "opens in speak-only mode. "
                "[CORRECTION, see fix] That last inference was wrong, and the "
                "wrongness is the lesson: 'fails at native build time' was "
                "REASONED from the missing release, never measured. It was "
                "then measured, and it builds.",
        "why_it_bites": "The owner asked for ChatGPT/Gemini-parity voice mode, "
                        "and on the phone - the surface that matters most for "
                        "hands-busy use - half of it is a read-only "
                        "experience until this resolves. Worse, the gap is "
                        "invisible from the code: the adapter is written, "
                        "typed and wired, so a reader sees a complete "
                        "implementation and only the missing package makes it "
                        "inert. The capability probe is what keeps that "
                        "honest at runtime instead of shipping a dead "
                        "microphone button.",
        "trigger": "expo-speech-recognition publishes a 57.x; OR the owner "
                   "asks why the phone will not listen when the desktop does",
        "fix": "PAID - by measuring the premise instead of trusting it. "
               "'No SDK 57 build' turned out to be a statement about upstream "
               "TESTING, not about compatibility: expo autolinking compiles "
               "community modules FROM SOURCE against the app's own "
               "expo-modules-core, so there is no prebuilt ABI that could "
               "mismatch. Proof: `:expo-speech-recognition:"
               "compileReleaseKotlin` against this app's real SDK 57 / "
               "RN 0.86 / Kotlin 2.1.20 tree is BUILD SUCCESSFUL, zero "
               "warnings. So (1) 56.0.1 is a dependency, pinned EXACTLY to "
               "the version that was compiled - no caret, because a future "
               "upstream release is exactly the thing that has not been "
               "verified; (2) the Android <queries> package-visibility entry "
               "the recogniser needs went into app/plugins/withGlassVoice.js "
               "rather than by registering the vendor's own config plugin - "
               "that plugin has no hand-managed-android half (DEPLOY.md), so "
               "the two build paths would have disagreed, and it would have "
               "overwritten the German iOS permission strings with English "
               "Apple boilerplate; (3) caps() now asks "
               "isRecognitionAvailable() rather than settling for 'the module "
               "loaded', because on a Play-less device the module loads fine "
               "and every start() fails; (4) voiceUsable() now requires an "
               "EAR, since speak-only voice mode was a screen with no way in. "
               "Residual risk moved to [voice-stt-sdk-lag].",
        "order": 39,
    },
    {
        "id": "voice-stt-sdk-lag",
        "title": "Speech recognition runs on an SDK-56 package pinned into an SDK-57 app",
        "status": "open",
        "what": "surfaces/app/package.json pins expo-speech-recognition to EXACTLY "
                "56.0.1 - upstream's newest release, published for Expo SDK "
                "56. Its Android half was compile-verified against this app's "
                "SDK 57 toolchain before adoption (see [voice-native-stt]) "
                "and the whole APK links and runs. The iOS half was NOT "
                "compile-verified here, because that needs a macOS/Xcode "
                "host, which this machine is not: it is Swift against "
                "expo-modules-core's Swift API, which is a different surface "
                "from the Kotlin one that was proven.",
        "why_it_bites": "Two distinct ways this bites, and neither shows up "
                        "on Windows. (a) The first iOS/EAS build after this "
                        "lands is where an SDK 56->57 Swift API change would "
                        "surface - as a pod compile error in a cloud build, "
                        "far from this commit. (b) `npx expo install --check` "
                        "will keep flagging the pin as off-SDK forever, and "
                        "the tempting one-word fix (bump to whatever is "
                        "newest) silently discards the compile evidence this "
                        "pin represents.",
        "trigger": "the next iOS/EAS build; OR expo-speech-recognition "
                   "publishes a 57.x; OR `expo install --check` flags it",
        "fix": "When a 57.x ships, `npx expo install "
               "expo-speech-recognition` and re-run the same proof that "
               "bought the current pin: `cd surfaces/app/android && ./gradlew "
               ":expo-speech-recognition:compileReleaseKotlin`, then a full "
               "APK + emulator smoke. Do NOT bump on the version number "
               "alone. For iOS, the honest close is one EAS build - it is "
               "the only compiler that can answer, and it answers in ~20 "
               "minutes. If that build fails, the fallback is unchanged and "
               "still stands: the repo already owns a native Android "
               "SpeechRecognizer (app/plugins/glassvoice/"
               "GlassVoiceService.kt) that could be promoted into a local "
               "Expo module, and iOS SFSpeechRecognizer written beside it.",
        "order": 40,
    },
    {
        "id": "voice-stream-cost",
        "title": "Streaming speech costs one edge-tts round trip PER SENTENCE, and the client's tail wait is bounded",
        "status": "open",
        "what": "voice_stream.py renders Henry's reply sentence by sentence so "
                "voice mode starts talking about a second into a turn instead "
                "of after it (ops/docs/voice-interaction-design.md SS8d). Two "
                "load-bearing shortcuts come with that. (a) A turn that used "
                "to be ONE edge-tts render is now typically 2-4, so the "
                "unmeasured quota question in SS9.5 got sharper rather than "
                "softer - and edge-tts is an unofficial client of a Microsoft "
                "service with no published limit. (b) chat.tsx's post-turn "
                "drain waits at most 40x250ms = 10s for chunks still "
                "rendering; past that the last sentence is silently dropped.",
        "why_it_bites": "Both fail in the same shape: quietly, and only under "
                        "load or a slow network - the two conditions under "
                        "which nobody is watching a test. A rate-limit would "
                        "show up as voice mode going mute mid-answer (the "
                        "render fails soft, so there is no error to see), and "
                        "the drain cap shows up as replies that lose their "
                        "final sentence only sometimes. Neither is visible "
                        "from the code, and neither is caught by "
                        "e2e_voice_stream.py, whose render is stubbed by "
                        "design.",
        "trigger": "voice mode goes silent partway through answers; OR a reply "
                   "reproducibly loses its last sentence; OR the first real "
                   "day of heavy voice use",
        "fix": "Measure before tuning. Count renders per turn and log "
               "edge-tts failures in voice.py (today a failure returns None "
               "and vanishes) - a soft failure that leaves no trace is the "
               "actual bug here. Then: raise BATCH_CHARS if the count is the "
               "problem, and reuse the existing content-hash cache harder "
               "(short sentences like 'Soll ich anfangen?' repeat across "
               "turns and should never re-render). For the drain, replace the "
               "fixed cap with a wait keyed on voice_pending going false, "
               "with the cap only as a backstop, and say so on screen when it "
               "is hit rather than dropping the sentence in silence.",
        "order": 41,
    },
    {
        "id": "voice-barge-in",
        "title": "Barge-in is a TAP everywhere - and on two of three surfaces that is now a library gap, not a law",
        "status": "open",
        "what": "Interrupting Henry means tapping the orb. The reason on "
                "record was 'HFP and A2DP are mutually exclusive', which is "
                "true but was read far too broadly - it is about the GLASSES' "
                "microphone over Bluetooth Classic, and AOSP names the "
                "trigger as MODE_IN_COMMUNICATION, a mode GlassVoiceService "
                "asks for itself (ops/docs/glasses-reference.md SS3.1, corrected). "
                "With that scope fixed, three surfaces have three different "
                "blockers. GLASSES: solved this commit - "
                "ACTION_LISTEN_PHONE_MIC listens on the phone and leaves A2DP "
                "up; UNVERIFIED, no device here. PHONE: Android exposes both "
                "AcousticEchoCanceler and the AEC-documented "
                "VOICE_COMMUNICATION source, but expo-speech-recognition "
                "reaches neither - ExpoAudioRecorder.kt hardcodes "
                "MediaRecorder.AudioSource.VOICE_RECOGNITION and its "
                "`audioSource` option takes a FILE URI, not an input device - "
                "so the phone would transcribe its own answer. DESKTOP/WEB: "
                "Chromium opens a RAW capture for the Web Speech API "
                "(speech_recognizer_impl.cc wires no AudioProcessingSettings "
                "and no echo reference), so our own playback is not cancelled.",
        "why_it_bites": "It is the last thing separating this from the "
                        "ChatGPT/Gemini feel the owner asked for, and it now "
                        "looks closer than it is: the constraint that "
                        "justified giving up turned out to be mis-scoped, "
                        "which makes it tempting to assume the rest is "
                        "cheap. It is not. The measurement that matters says "
                        "NO: Chromium 150 (Electron 43, the newest shell "
                        "there is) neither advertises `echoCancellationMode` "
                        "in getSupportedConstraints() nor returns it from "
                        "track.getSettings() when asked for it explicitly - "
                        "so the documented full-duplex path does not exist in "
                        "a runtime we could ship, whatever the Chrome 141 "
                        "release note says. Bumping Electron for this would "
                        "have bought nothing.",
        "trigger": "`py -3.12 ops/tools/probe_duplex.js` (via electron) reports "
                   "DUPLEX AVAILABLE; OR expo-speech-recognition exposes an "
                   "input-device / audio-source option; OR the glasses turn "
                   "out to speak LE Audio",
        "fix": "Per surface, cheapest first. DESKTOP: re-run "
               "ops/tools/probe_duplex.js after any Electron bump; when "
               "echoCancellationMode comes back true, add the duplex branch "
               "to listenWeb (getUserMedia({echoCancellationMode:'all'}) -> "
               "recognition.start(track)) and flip caps().duplex's consumer "
               "on - the probe is already wired, so nothing else changes. "
               "Trap: the 'speech-recognition' content hint defaults "
               "echoCancellation/AGC/NS to FALSE, which is backwards here. "
               "PHONE: needs a device. Either upstream an input-source option "
               "to expo-speech-recognition, or attach "
               "AcousticEchoCanceler.create(sessionId) in a fork - and "
               "measure with a real acoustic loop, which no emulator has. "
               "GLASSES: ACTION_LISTEN_PHONE_MIC needs one real-device test; "
               "the notification now reports which microphone actually "
               "opened, so the answer is readable off the lens. Longer term "
               "LE Audio removes the whole question on Android 13+, but "
               "whether the Ray-Ban Display supports it is unanswered, and "
               "for iOS no primary Apple source on LE Audio exists at all.",
        "order": 42,
    },
    {
        "id": "glasses-native-uncompiled",
        "title": "The DAT CAMERA half of the glasses native seam is compiled and "
                 "shipped but UNREACHABLE - nothing calls capture()",
        "status": "open",
        "what": "Owner decision 2026-08-21 ('write source here'), taken knowingly: "
                "an APK cannot be built from a card worktree (DEPLOY.md 2, NDK "
                "path length), so the native half of the Meta companion work was "
                "written here to be built later from C:\\hd\\app. What is "
                "UNCOMPILED: app/plugins/metadat/GlassCameraService.kt (the DAT "
                "0.9.0 camera session + capturePhoto), app/plugins/glassvoice/"
                "GlassesRadio.kt (the mic/camera mutual-exclusion arbiter), and "
                "the GlassVoiceService.kt edits that claim and release it. What "
                "IS verified: the Gradle/manifest wiring in "
                "app/plugins/withMetaDat.js, covered by 54 assertions in "
                "ops/tests/test_meta_dat_plugin.js (dialect correctness, "
                "idempotency, the service/class name agreeing with the package "
                "path, no literal token ever written). The DAT API itself was "
                "NOT guessed - every symbol was read out of the real 0.9.0 AARs "
                "with javap after fetching them from GitHub Packages, so the "
                "signatures are measured even though the code is unbuilt.",
        "why_it_bites": "⚠ UPDATED 2026-09-04 - THE 'UNCOMPILED' PREMISE IS DEAD "
                        "and the entry is retitled to what actually remains. "
                        "MEASURED against the shipped artifact, not reasoned: all "
                        "four classes - app/helmdeck/voice/GlassVoiceService, "
                        "app/helmdeck/glasses/GlassesRadio, app/helmdeck/glasses/"
                        "GlassCameraService and app/helmdeck/glassesbridge/"
                        "GlassesBridgeModule - are present in the dex of "
                        "android/app/build/outputs/apk/release/app-release.apk "
                        "(versionCode 88, native ship fe8a204 2026-09-03). The "
                        "old residue (i) 'NO UI CALLS ANY OF IT' has SPLIT: the "
                        "VOICE half is now called (surfaces/app/src/app/chat.tsx:"
                        "659-669 reads settings.glance_origin + glance_token, "
                        "calls configure() then listen(true) behind an owner-only "
                        "header toggle) and its daemon path is proven end-to-end "
                        "through the live worker. The CAMERA half is NOT: "
                        "capture() and stopCamera() in surfaces/app/src/data/"
                        "glasses.ts have ZERO callers anywhere in surfaces/app/"
                        "src, so GlassCameraService and POST /glance/photo are "
                        "compiled, shipped, allowlisted in the worker - and "
                        "unreachable. That, plus 'has never run on real glasses', "
                        "is the whole of what is still open here. Also still "
                        "unreachable: listen(false) / ACTION_LISTEN_PHONE_MIC, "
                        "because chat.tsx:669 hardcodes listen(true), so every "
                        "turn collapses glasses audio to 8 kHz HFP even when the "
                        "phone is in hand - the per-turn A2DP trade the seam was "
                        "explicitly designed to offer has no control. "
                        "Kept below, the 2026-08-21 history: "
                        "(1) is PAID and the "
                        "other two stand. (1) COMPILES: the owner granted "
                        "access to C:\\hd, and :app:compileReleaseKotlin is "
                        "BUILD SUCCESSFUL against the real mwdat 0.9.0 - the "
                        "addCamera/removeCamera extension imports resolve and "
                        "the sealed PhotoData `when` is exhaustive. Four real "
                        "defects were found and fixed getting there (minSdk 29 "
                        "vs the app's 24, ALREADY_INITIALIZED being a failure "
                        "result, the repository not belonging in settings.gradle "
                        "in this template, and build_apk.sh never applying the "
                        "plugin at all). (2) STILL OPEN: the daemon endpoint "
                        "GlassCameraService POSTs to, /glance/photo, DOES NOT "
                        "EXIST - the contract is named in the Kotlin but nothing "
                        "serves it, so a successful capture ends in a 404. "
                        "(2) NOW PAID: POST /glance/photo exists, gated by its "
                        "OWN settings.glance_photo (a camera is a different "
                        "consent from reading the board), requires a card id "
                        "rather than guessing from presence, and is carried by "
                        "the glance Worker with a per-route 12 MB cap. "
                        "(3) NOW PAID: modules/glasses is a local Expo module "
                        "that starts both services by explicit ComponentName "
                        "and writes their base_url/glance_token prefs - "
                        "autolinking finds it and it COMPILES (364 tasks vs "
                        "350). surfaces/app/src/data/glasses.ts is the typed seam, "
                        "capability-probed like data/voice.ts so an OTA bundle "
                        "landing on an older APK degrades instead of white-"
                        "screening. WHAT IS STILL NOT DONE, and it is the "
                        "honest remainder: (i) NO UI CALLS ANY OF IT - the seam "
                        "exists and nothing invokes capture()/listen() yet, and "
                        "configure() is not called at pairing because the right "
                        "base_url is the public glance Worker origin, which the "
                        "app has no setting for (NOT useConfig.baseUrl - in "
                        "relay mode there is no direct daemon URL and the LAN "
                        "address is meaningless off the LAN); (ii) none of it "
                        "has run on glasses. So the code is PROVEN TO BUILD and "
                        "is NOT yet proven to work.",
        "trigger": "ACCEPTING THIS CARD. ops/deploy/ship.sh fingerprints app.json's "
                   "expo block, and adding ./plugins/withMetaDat moved it "
                   "(verified: 28dff14 -> dd88362), so the accept takes the "
                   "NATIVE branch - bump runtimeVersion, ops/deploy/build_apk.sh "
                   "(npm ci + gradle assembleRelease + emulator smoke + relay "
                   "distribute), then a matching OTA. That build runs from the "
                   "live repo root, which is a short path, so the NDK "
                   "path-length failure that blocks a worktree build does not "
                   "apply. It is ALSO the first time any of this Kotlin is "
                   "type-checked, so expect the first accept to be where the "
                   "compile errors surface.",
        "fix": "PARTLY WIRED 2026-08-21: ops/deploy/build_apk.sh now applies "
               "withMetaDat.js alongside the other three plugins. It did NOT, "
               "and that was the highest-consequence gap in this whole feature "
               "- surfaces/app/android is git-ignored and hand-managed, so an unapplied "
               "plugin contributes nothing, and gradle would have produced a "
               "green APK with the entire camera feature absent and exit 0. "
               "ops/tests/test_meta_dat_plugin.js now asserts generically that "
               "EVERY local plugin in app.json is applied by build_apk.sh, so "
               "the next plugin cannot repeat it. REMAINING: (a) run the build "
               "and fix whatever kotlinc says, starting with the DAT extension "
               "imports; (b) add "
               "POST /glance/photo to the daemon (token-gated exactly like "
               "/glance/talk, one glance_token, NO ticket registry - "
               "glasses-reference 11.1) and attach the frame to a card; (c) add "
               "the JS->native seam that starts both services and writes their "
               "base_url/glance_token prefs at pairing time; (d) verify the "
               "radio arbiter on real hardware - the mic and the camera must "
               "refuse each other, which is the one behaviour that cannot be "
               "checked without glasses. Until (c) lands, this feature is "
               "invisible to the owner and must not be described as shipped.",
        "order": 43,
    },
    {
        "id": "load-admission-scope",
        "title": "Load-aware admission covers gate/build starts and the box's "
                 "TOTAL CPU only - not a running worker turn, not per-process "
                 "attribution",
        "status": "open",
        "what": "ops/docs/backlog/load-aware-admission shipped: spine.ops.resources "
                "samples whole-box CPU (ctypes GetSystemTimes delta, no psutil - "
                "not vendored for the daemon's actual py -3.12 interpreter, "
                "measured) and free RAM; lanemachine._admit_heavy defers the "
                "START of a heavy op (gate run / preview or deploy hook - the "
                "deploy hook IS the APK/Gradle build + emulator boot, run as one "
                "opaque shell command, so admission cannot see 'gradle' vs "
                "'emulator' as separate phases and both are labelled 'build') "
                "while policy.load_admission.cpu_max_pct is exceeded, queuing "
                "with a NAMED holder note (spine.git.locks._heavy_holders) "
                "and admitting anyway past wait_s. _gate() also gained a per-tree "
                "singleton lock (locks._gate_lock_for) and moved from a fixed "
                "600s wall-clock subprocess.run timeout to the same SILENCE-"
                "bounded Popen+pump _repo_hook already used (now shared as "
                "lanemachine._run_streamed).",
        "why_it_bites": "Three edges, each real but out of this card's measured "
                        "scope. (1) A LIVE WORKER TURN's own 900s CLI idle_timeout "
                        "(drivers.py) is UNCHANGED - if a concurrently admitted "
                        "gate/build genuinely starves a running worker's CLI "
                        "output for >900s, that turn can still kill itself as "
                        "'stalled', mis-blaming the worker for load it didn't "
                        "cause (the exact failure class named in the backlog "
                        "README's 'why this is a real class' section, half-closed: "
                        "the gate/build side is fixed, the worker-turn side is "
                        "not). (2) The gate singleton SERIALIZES (a second gate "
                        "on the same tree blocks, then runs its own fresh check) "
                        "rather than JOINING the first's result the way Paseo's "
                        "replaceAgentRun shares one outcome - correct enough to "
                        "kill the measured double-gate-starvation bug, but a "
                        "queued second gate still re-runs the full suite instead "
                        "of reading the first's verdict. (3) The holder registry "
                        "only knows HelmDeck's OWN heavy ops - real load from "
                        "rustdesk, a hand-run gradle build, or any process this "
                        "daemon didn't start is named 'unbekannt/extern', which "
                        "is honest but not actionable; and a synchronous 0.2s "
                        "delta CPU read can itself be skewed by severe scheduler "
                        "contention on the same box it is measuring.",
        "trigger": "a worker turn timing out mid-build with nothing but silence "
                   "in its own log while a gate/deploy hook was concurrently "
                   "admitted; a second gate queued behind a slow first one "
                   "wanting to reuse rather than repeat the wait; an owner asking "
                   "why the wait note says 'unbekannt/extern' while rustdesk is "
                   "visibly pegging the CPU",
        "fix": "(1) Thread admission state into drivers.py's idle-watchdog: while "
               "policy.load_admission holders are non-empty, extend (never "
               "shrink) a live turn's idle deadline by the contention window - "
               "same 'bound by silence, not by someone else's noise' reasoning "
               "already applied to the hook/gate side. (2) Give the gate "
               "singleton a shareable future (store the running gate's (ok, "
               "problems) keyed by tree + a content hash of helmdeck.gate + "
               "HEAD, and have a second concurrent request AWAIT that result "
               "instead of re-running) - real 'join', not just no-stacking. "
               "(3) PARTLY ADDRESSED via the Henry route (the Paseo "
               "diagnostics pattern: measurement in the snapshot, judgement "
               "in the reader): a give-up past wait_s now emits a deduped "
               "'load-contention' escalation, and Henry's system snapshot "
               "carries _box_load_line (the SAME resources seam the admission "
               "decides by, plus the named holders) next to the tasklist "
               "process counts that can name an external hog - so 'unbekannt/"
               "extern' cases reach a judge who sees WHO instead of a code "
               "path that cannot. A per-process CPU breakdown (EnumProcesses "
               "+ GetProcessTimes) stays the escalation path only if Henry's "
               "coarse process picture proves insufficient in practice.",
        "order": 44,
    },
    {
        "id": "card-feed-is-claude-private-jsonl",
        "title": "The card feed is a re-parse of Claude Code's own private session file",
        "status": "paid",
        "what": "claude_sessions.py (~660 lines) + claude_transcript_fmt.py build "
                "every card's chat feed by globbing and parsing "
                "~/.claude/projects/<cwd>/<uuid>.jsonl - a file Claude Code owns "
                "and writes for ITS OWN purposes (record schema, envelope "
                "sniffing for strings like '<task-notification>', "
                "'[Request interrupted by user', tool-name whitelists). "
                "transcript_version()'s long-poll change token is literally the "
                "byte count of that foreign file.",
        "why_it_bites": "This is a heuristic reconstruction of first-class state "
                        "from another program's artifacts, at the OPPOSITE end "
                        "of the spectrum from the law CLAUDE.md states for this "
                        "repo (NO MONKEY PATCHES: derived and verified from the "
                        "runtime's own signals, folded in at event time - never "
                        "reconstructed by re-scanning artifacts). It has held "
                        "only because there has been exactly one engine, ever. "
                        "A second card driver (OpenCode/Codex/any ACP agent) "
                        "produces no such file, so it produces NO CARD FEED AT "
                        "ALL - not a degraded one, an absent one.",
        "trigger": "ops/docs/multi-engine-support.md (2026-08-24 analysis) + "
                   "ops/docs/multi-engine-build-plan.md Card 2 (E3, sized L/3-5d): "
                   "the pump that already runs every claude turn "
                   "(drivers.py _ClaudeSession._pump/_on_event) folds each "
                   "normalized event into a persisted per-card timeline store "
                   "at EVENT TIME - the same fold discipline already proven by "
                   "_scan_bg (drivers.py:634-680). /transcript reads THAT. "
                   "Dual-write against the old reader first, cut over only "
                   "after N clean live turns diff empty; the old reader stays "
                   "for adopting foreign claude sessions and pre-cutover "
                   "session_chain history. Worth doing even if multi-engine "
                   "support is never built further - it is closing a live law "
                   "violation, not just an enabler.",
        "fix": "PAID (spine/agent/timeline_store.py, drivers.py's "
               "_ClaudeSession._fold_timeline, claude_sessions."
               "read_transcript_store/transcript_store_version): the driver's "
               "own pump folds every stream event into a per-card append-only "
               "JSONL store at the moment each block completes - the SAME "
               "TStep shape read_transcript already produced, just derived "
               "from the LIVE stream instead of a re-parsed .jsonl. /transcript, "
               "/transcript/live and the SSE tick (routes_tracks.py, "
               "routes_track_actions.py) now all read the store; the old "
               ".jsonl reader is kept ONLY for what genuinely still needs it - "
               "session_chain history from before the store existed (adopted "
               "or rotated-away foreign sessions) and the in-progress "
               "live_partial.txt streaming block, exactly as the build plan's "
               "own design called for. Dual-write verified before cutover with "
               "ops/tools/compare_timeline.py against real dispatched turns "
               "(text, tool calls through all 4 states, todos, usage, a "
               "harness-injected question note, and a real mid-turn Stop/"
               "cancel) - PASS on every category. Two real bugs surfaced and "
               "fixed during that verification, not assumed away: (1) the "
               "human's own steer text never arrives on the OUTPUT stream at "
               "all (Claude Code does not echo stdin back) - now folded at "
               "the moment the driver WRITES it, including re-attributing "
               "HelmDeck's own harness-injected prompts to a system note "
               "exactly like the old reader did; (2) a message's LIVE "
               "usage.output_tokens can read far below its own settled value "
               "in the persisted file (measured: 2 live vs. 152 in the file, "
               "same message id, no later live frame ever corrects it) - "
               "documented as a permanent, harmless approximation (ctx, the "
               "only usage field econ.py's context meter reads, is unaffected "
               "and proven identical live vs. file). Not yet screenshotted in "
               "the app: zero frontend code changed (the TStep wire contract "
               "is byte-identical) and this worktree has no surfaces/app/node_modules "
               "set up - the owner should still spot-check a real card's feed "
               "once after accepting.",
        "order": 45,
    },
    {
        "id": "auto-model-routing-is-claude-ids-only",
        "title": "Auto model routing picks a claude-* id even when the driver isn't claude",
        "status": "open",
        "what": "turnrunner._turn resolves policy.auto to a concrete model id via "
                "turnopts.resolve_model, whose whole manifest (turnopts.CLAUDE_MODELS: "
                "claude-opus-5/claude-sonnet-5/claude-haiku-4-5) is Anthropic-specific. "
                "That resolved id is then handed to WHATEVER driver the card uses, "
                "unchanged - for the native omp driver it lands in build_argv's "
                "--model flag verbatim.",
        "why_it_bites": "Measured live 2026-08-24 (ops/docs/multi-engine-build-plan.md "
                        "Card 8): a card with driver=omp and no explicit model got "
                        "auto-routed to the literal string 'claude-sonnet-5', which "
                        "omp's own --model fuzzy-matcher happened to resolve to a real "
                        "sonnet-tier model (the turn completed correctly, cost "
                        "$0.147614 vs. an explicit 'haiku' dispatch's $0.11). It WORKED "
                        "by fuzzy-match coincidence, not by design - a future omp "
                        "catalog change, or a THIRD engine with a stricter --model "
                        "parser, could silently fail or silently pick the wrong model "
                        "with no error surfaced.",
        "trigger": "any auto-routed card on a non-claude driver in production. "
                   "Workaround today: pass an explicit model on card creation "
                   "(bypasses turnrunner's auto-resolve branch entirely - proven, "
                   "the second omp test dispatch used model='haiku' explicitly and "
                   "got exactly that model, confirmed via the track's own `models` "
                   "field). Real fix is Card 9's territory (econ/UI generalisation) "
                   "or its own small card: either give resolve_model an engine "
                   "parameter with a per-engine manifest, or have each native driver "
                   "translate the generic auto tier (cheap/balanced/strong) into its "
                   "own model id instead of receiving a claude-shaped string.",
        "fix": "OPEN.",
        "order": 46,
    },
    {
        "id": "codex-opencode-pi-drivers-unverified",
        "title": "Three native engine drivers exist as code with zero live turns run",
        "status": "open",
        "what": "spine/agent/{codex,opencode,pi}_driver.py (build plan "
                "Cards 6/7/8-pi) were built 2026-08-24 per owner decree ('do all "
                "like Paseo, test accounts later') - complete modules, unit-"
                "tested against synthetic frames built from Paseo's real "
                "TS source, gate green, wired into drivers.run() dispatch. NONE "
                "has been run against a real codex/opencode/pi CLI - no account "
                "exists for any of the three on this box. This is qualitatively "
                "different from the omp half of Card 8, which IS live-verified "
                "(real turns, real tool calls, real cost, real cancel, through "
                "the actual daemon and HTTP route).",
        "why_it_bites": "omp_driver.py's own history is the reason this matters: "
                        "reading Paseo's real TS source got the request/response "
                        "SHAPES right but missed a real behavioral surprise (one "
                        "prompt can produce multiple internal turn_end events, the "
                        "first carrying narration not the answer) that only live "
                        "testing caught - and a second one (a cancelled tool call "
                        "reports isError:true, which a naive mapping would have "
                        "shown as a tool FAILURE, not a cancel). Codex/OpenCode/Pi "
                        "each carry their own version of this risk, unmeasured. "
                        "Confidence is NOT uniform across the three: Codex has the "
                        "full JSON-RPC method+param shapes read from source (only "
                        "the account is missing); Pi rides on omp's proven event-"
                        "handling logic but pi's own stream has never been seen; "
                        "OpenCode is the highest-risk of the three because its "
                        "REST endpoint PATHS are INFERRED from SDK method names "
                        "(@opencode-ai/sdk is not vendored in this checkout), not "
                        "read from source at all - the one part of that module "
                        "most likely to need correction, not just confirmation.",
        "trigger": "an owner account for codex, opencode, or pi. Each card's own "
                   "'Verify (STILL OPEN)' line in ops/docs/multi-engine-build-plan.md "
                   "is the concrete checklist - run it the same way omp's Card 8 "
                   "verify ran (real dispatched turns through the live daemon and "
                   "the actual /tracks/.../transcript HTTP route, not just the "
                   "unit tests), and expect to find and fix at least one real "
                   "surprise per engine the same way omp did. Do not accept any "
                   "of these three cards as 'done' on unit-test-green alone.",
        "fix": "OPEN. Three independent verification passes, one per engine, "
               "each its own small follow-up card once that engine's account "
               "exists. OpenCode's pass should start by confirming the REST "
               "paths resolve at all before testing anything else.",
        "order": 47,
    },
    {
        "id": "android-build-arm64-only",
        "title": "Release APK narrowed to arm64-v8a - armeabi-v7a/x86/x86_64 unbuildable",
        "status": "open",
        "what": "ops/deploy/build_apk.sh's gradlew assembleRelease now passes "
                "-PreactNativeArchitectures=arm64-v8a (owner decree 2026-08-25), "
                "so every ship.sh/release.sh android build produces an APK for "
                "arm64-v8a only, down from the gradle.properties default of "
                "armeabi-v7a,arm64-v8a,x86,x86_64.",
        "why_it_bites": "react-native-reanimated's CMake/Ninja step for "
                        "armeabi-v7a fails deterministically: `ninja: error: "
                        "manifest 'build.ninja' still dirty after 100 tries`, "
                        "reproduced 3/3 on this checkout including after "
                        "clearing its .cxx cache (so not stale-cache corruption "
                        "- a persistent, environmental failure). Suspected but "
                        "UNCONFIRMED root cause: this checkout's Windows path "
                        "contains a space (C:\\Users\\Tien Duy Vo\\...), a known "
                        "class of CMake/Ninja fragility. Narrowing to arm64-v8a "
                        "sidesteps it rather than fixing it - real devices are "
                        "almost all arm64 now, but any 32-bit-ARM/x86/x86_64 "
                        "device (old phones, some emulators/Chromebooks) can no "
                        "longer install a build shipped from this pipeline.",
        "trigger": "someone needs an APK for a non-arm64 device, OR someone "
                   "has time to actually diagnose the ninja regen loop (try a "
                   "checkout path with no spaces first - cheapest test of the "
                   "leading theory - before patching reanimated's CMakeLists).",
        "fix": "OPEN. Confirm/refute the path-with-spaces theory, then either "
               "patch-package react-native-reanimated's CMake config or move "
               "the checkout, and restore the full ABI list in "
               "ops/deploy/build_apk.sh + surfaces/app/android/gradle.properties.",
        "order": 48,
    },
    {
        "id": "ship-decision-not-wired",
        "title": "Ship advisor exists but the automatic deploy path still uses the hash",
        "status": "paid",
        "what": "The ship decision was inverted from a fixed heuristic to an "
                "agent judgement (owner decree 2026-08-30): ops/tools/ship_facts.py "
                "reports evidence and decides nothing, ops/harness/agents/"
                "ship-advisor.md is the deciding brief, and ops/deploy/ship.sh "
                "now EXECUTES a decision passed as SHIP_KIND=none|ota|native. "
                "But the automatic path - lanemachine._repo_hook running the "
                "repo_hooks 'deploy' command post-accept - still invokes "
                "`bash ops/deploy/ship.sh` with no SHIP_KIND, so it falls back "
                "to the legacy stored-fingerprint hash. The seam is built and "
                "verified; nothing calls it yet on the automatic path.",
        "why_it_bites": "Every failure mode of the hash is still live for "
                        "automatic ships - including the one the advisor exists "
                        "to fix: the hash cannot answer 'do not ship', so a "
                        "docs-only or daemon-only accept still pushes an OTA. "
                        "Measured on this very tree 2026-08-30: only .py and "
                        "ops/ files had changed and the hash fallback still "
                        "resolved to 'OTA only'. Worse, the fallback is SILENT "
                        "in the sense that matters - it announces itself in the "
                        "hook log, but nobody reads a green deploy log, so the "
                        "advisor can look adopted while never running.",
        "trigger": "before trusting the advisor for real ships, or the first "
                   "time an automatic ship makes a decision a human disagrees "
                   "with.",
        "fix": "PAID 2026-09-01 (owner decree, after a day lost to colliding "
               "mechanical ships): NO automatic path fires the deploy hook "
               "anymore. Every landing (accept, fast-track, fast-track-direct, "
               "umzug, direct-accept) calls lanemachine.request_ship_decision, "
               "which emits a ship-decision escalation; Henry judges it with "
               "his live snapshot (locks, box load, build processes) and "
               "answers the `ship` verb with kind none|ota|native - the broker "
               "executes ota/native through _repo_hook with SHIP_KIND at event "
               "time, never via a file. Advisor-unavailable behaviour is "
               "DEFINED as: nothing ships (the escalation stays open and "
               "visible; give-up after the attempt cap notifies the owner) - "
               "the silent hash fallback is unreachable from any automatic "
               "path. ship.sh keeps the fallback for a bare manual run only.",
        "order": 50,
    },
    {
        "id": "android-build-lock-advisory",
        "title": "Android build mutex is advisory - a raw ./gradlew still bypasses it",
        "status": "open",
        "what": "ops/deploy/build_lock.sh serializes Android builds through a "
                "machine-global mkdir+live-PID lock, and every SCRIPT entrypoint "
                "takes it (ops/deploy/build_apk.sh, ops/deploy/build_wear_apk.sh, "
                "ops/tools/release.sh build_android). The lock is COOPERATIVE: it "
                "binds only callers that source it. Someone who runs `cd "
                "surfaces/app/android && ./gradlew :app:assembleRelease` by hand - "
                "or an agent that finds android/ and invokes gradlew directly "
                "instead of following DEPLOY.md - takes no lock and can still "
                "land on top of a running build.",
        "why_it_bites": "This is exactly the 2026-08-30 failure shape (two "
                        "Android builds blocking each other), just reachable by a "
                        "narrower path. The damage is not merely slow: "
                        "build_apk.sh opens with `./gradlew --stop`, which stops "
                        "EVERY Gradle daemon of this user, so an unlocked build "
                        "starting mid-flight kills a locked one that is ten "
                        "minutes into assembleRelease - the lock protects a "
                        "holder from other LOCKED builds, not from an unlocked "
                        "one. Second, narrower gap: a holder that is alive but "
                        "WEDGED is waited on indefinitely by design (only a DEAD "
                        "holder is taken over), so a hung gradle parks the queue "
                        "until someone kills it.",
        "trigger": "a build collision recurs despite the lock (read Henry's "
                   "`android-build.lock:` snapshot line first - it names the "
                   "holder), or someone adds a NEW gradle entrypoint.",
        "fix": "OPEN. Cheapest real closure is to move the lock UNDER gradle "
               "rather than beside it - a gradlew wrapper in "
               "surfaces/app/android that acquires before delegating, so the "
               "bypass path stops existing instead of being documented. Note "
               "surfaces/app/android is git-ignored and regenerated by `expo "
               "prebuild --clean`, so such a wrapper must be re-applied by one "
               "of build_apk.sh's with*.js plugin steps or it vanishes on the "
               "very next build. Until then this stays advisory.",
        "order": 49,
    },
    {
        "id": "alt-driver-by-wiring",
        "title": "omp/codex drivers accept `by` (sender identity) but don't fold it",
        "status": "open",
        "what": "The team-chat identity fold (spine/agent/drivers.py's "
                "_ClaudeSession._run_turn_locked) threads the steering human's "
                "name (`by`) into the timeline step it writes for every "
                "human-submitted prompt, so the transcript can show WHO sent "
                "a message. spine/agent/omp_driver.py and codex_driver.py's "
                "run() now accept the same `by` kwarg for signature parity "
                "with drivers.run, but neither folds it into their own "
                "transcript - only the default Claude driver path does.",
        "why_it_bites": "A card running on the omp or codex driver (opt-in, "
                        "not the default) will render its human steer text "
                        "with no sender attribution - same as before this "
                        "change, not a regression, just unfinished parity. "
                        "Both drivers are themselves unverified against real "
                        "accounts already (see the omp/codex driver docstrings).",
        "trigger": "the omp or codex driver moves from 'not live-verified' to "
                   "actually used against a real account.",
        "fix": "OPEN. Mirror the fold drivers.py's _run_turn_locked does "
               "(role/kind/text + by/byKind:human/to:worker) at each "
               "driver's own equivalent submission point, once each has a "
               "real account to verify against.",
        "order": 49,
    },
    {
        "id": "card-henry-reply-not-streamed",
        "title": "Card-scoped Henry replies arrive whole, not token-streamed",
        "status": "paid",
        "what": "cells/copilot/copilot.py's chat() already streams its prose "
                "into a live_partial.txt for the global board chat "
                "(surfaces/app/src/app/chat.tsx polls /chat/live), but a "
                "card-scoped turn (card= set, the team-chat @Henry path from "
                "surfaces/app/src/app/card/[id].tsx) only folds the finished "
                "reply into the card's timeline once the whole turn returns.",
        "why_it_bites": "A slow Henry turn on a card shows nothing but the "
                        "local 'Henry denkt…' indicator (card.chat.henryThinking) "
                        "for the full duration, instead of live text appearing "
                        "the way a card Worker's own turn streams.",
        "trigger": "a card-scoped Henry turn regularly takes long enough "
                   "(multi-tool-call turns, slow model) that the no-feedback "
                   "wait becomes annoying.",
        "fix": "PAID. The prose was always streamed - chat() writes "
               "live_partial.txt for EVERY turn, card-scoped ones included - so "
               "the missing half was purely the reader plus a way to know the "
               "stream belonged here. /chat/live now reports the running turn's "
               "card (copilot._running_card, a satellite of _running with the "
               "same lifetime, set and cleared at the same two places), and "
               "card/[id].tsx polls it while its own Henry send is in flight, "
               "rendering the growing text as a `streaming` step in the SAME "
               "card_transcript bubble the worker's stream uses. Scoped twice "
               "(our send is running AND r.card === this card) because the feed "
               "is per user and turns are serialised - an unscoped read would "
               "have painted a board-chat answer into a card's timeline. The "
               "'Henry denkt…' row now stands down as soon as prose arrives.",
        "order": 50,
    },
    {
        "id": "repo-template-card-kind-unwired",
        "title": "A repo template declares its card kind, and nothing reads it",
        "status": "paid",
        "what": "ops/harness/templates/*.md carry `card_kind` (new_track for "
                "software-dev, new_direct_task for documents) and "
                "spine/ops/projects.resolve() surfaces it, but no caller "
                "consumes it: cells/engineer/dispatch.py still decides worktree "
                "vs live-tree per CARD (new_track / new_direct_task are separate "
                "entry points), never per repo.",
        "why_it_bites": "It is the exact failure the templates were written to "
                        "avoid, and spine/registry/templates.py's own docstring "
                        "names it: a value that reports success and changes "
                        "nothing. The PRD's tables (§3.1/§3.2) mark the card "
                        "kind 'neu als Repo-Default', so this half was always "
                        "future work - but until it is wired the documents "
                        "template CANNOT promise 'direkt im Ordner'. Its prose "
                        "and summary were reworded to stop claiming it. If "
                        "someone re-adds that claim before wiring this, the "
                        "screen starts lying again.",
        "trigger": "the owner onboards a real document repo and expects its "
                   "cards to skip the worktree without saying so per card.",
        "fix": "PAID 2026-08-31. dispatch.new_track - the one place a card is "
               "born, every other entry point wraps it - now resolves the kind "
               "BEFORE the branch name, because they are the same decision: a "
               "live-tree card carries DIRECT_BRANCH and never gets a worktree. "
               "Precedence: an explicit card_kind= argument, else the branch "
               "sentinel a wrapper already passed, else _repo_default_kind() - "
               "THE single reader of projects.resolve(repo)['card_kind']. So the "
               "repo default only ever fills a silence and can never override a "
               "caller. The three direct-card fields (machine/direct/worktree) "
               "moved into new_track too, so a card born from the repo default "
               "and one born from new_direct_task are the same object, not two "
               "lookalikes. The guards are NOT bypassable: policy.machine.enabled, "
               "a real .git, machine_root_ok are re-checked on the default path, "
               "and a repo that fails them falls back to the worktree LOUDLY "
               "rather than silently doing something other than the template "
               "promised. A direct card also adopts policy.machine's perm, or it "
               "would stall on its own git write. Pinned: "
               "ops/tests/test_repo_template_wiring.py.",
        "order": 51,
    },
    {
        "id": "repo-template-policy-presets-still-global",
        "title": "A repo template's policy.* presets are workspace-wide, so two "
                 "repos still overwrite each other",
        "status": "paid",
        "what": "spine/ops/projects.apply_template() writes the per-repo half "
                "(repo_hooks.<repo>.deploy) truly per repo, but its `settings` "
                "presets (policy.auto_accept_green, policy.auto_dispatch_modes) "
                "go through events.save_settings into the ONE global "
                "settings.json blob. Applying a template to repo B therefore "
                "moves those values for repo A too.",
        "why_it_bites": "The decree is 'pro Repo', and for the stations, the "
                        "deploy hook and the record itself that now holds - for "
                        "these two policy keys it does not. It is NOT silent: "
                        "the deviation is recorded in projects.overrides/applied "
                        "at write time and the pipeline card renders 'vom "
                        "Standard abgewichen' (PRD §5 accepted exactly this "
                        "trade). But visible drift is still drift, and the "
                        "measured proof is in ops/docs/shots/harness/"
                        "repo-code.png: the code repo shows two deviations it "
                        "never asked for, caused by the document repo's template "
                        "being applied after it.",
        "trigger": "the owner runs two repos of different types side by side "
                   "and cares that autonomy differs between them.",
        "fix": "PAID 2026-08-31, via the first of the two options. "
               "apply_template() no longer fans the template's `settings` out "
               "through events.save_settings; it records them in the project "
               "record's `applied`, which was ALREADY the snapshot of what the "
               "template set and simply had no reader. projects.policy_for(repo, "
               "key, default) is that reader: overrides -> applied -> the global "
               "settings value, three recorded facts in falling order, total on "
               "a missing/unreadable record. Both real read sites consume it - "
               "processes.sync() and processes._autopilot() no longer hoist the "
               "values above their loops (the hoist was WHY they had to be "
               "global) and resolve at the decision point, where the card's repo "
               "is in hand. capacity.wip_limit deliberately stays workspace-wide: "
               "it is a property of the machine, per PRD §9 A. resolve()'s "
               "deviation check compares per-repo now, or every repo whose "
               "template legitimately differs from the workspace default would "
               "show a permanent phantom deviation. A repo with no template is "
               "unchanged: it falls through to the same global value as before. "
               "Pinned: ops/tests/test_repo_template_wiring.py, including that an "
               "override on repo B leaks neither to repo A nor into the "
               "workspace default.",
        "order": 52,
    },
    {
        "id": "e2e-harnesses-stale-after-split",
        "title": "Test/camera harnesses died silently at the four-folder split - "
                 "mostly recovered, six assertions still stale",
        "status": "open",
        "what": "The move to spine/cells/surfaces/ops left 12 scripts under "
                "ops/tests/ and ops/docs/shots/ importing the pre-split flat "
                "layout (sys.path.insert(<repo>/daemon) + `import auth, db, "
                "events`). Resolved so far: verify_plan_share.py (19 checks) and "
                "verify_billing_auto.py (24 checks) are repaired and GREEN; "
                "e2e_lane_http.py runs again but is still red for its own "
                "reasons (its stubs name sessions._gate etc., machinery that "
                "moved into lanemachine.py in the same split, so they bind "
                "nothing); and four USED-UP JIGS were deleted rather than "
                "repaired - uifix_harness.py, uifix_p3_harness.py and "
                "uifix_question_harness.py each seeded the card states of ONE "
                "branch that has long since shipped (gating, the transcript "
                "model, the question channel are all normal product now), and "
                "loopmap_sandbox.py seeded nothing at all, so it was pure "
                "boilerplate that repo_pipeline_sandbox.py now does. Still dead: "
                "e2e_cancel_resume.py, e2e_question_live.py, glance_shoot.py, "
                "smoke/pair_link.py, smoke/setup_sandbox.py - these five are "
                "STANDING tools (a behaviour proof, a glasses camera, the "
                "APK-against-a-tunnel recipe), not scaffolding, which is why "
                "they were not deleted with the rest. FINAL STATE: the whole "
                "ops/tests/unit/ pytest suite turned out to be dead too - its "
                "shared conftest.py pointed at <repo>/daemon, so all three "
                "tests died at COLLECTION and the count was 15 files, not 12. "
                "conftest + imports + the moved seams (proctable._PIDFILE, the "
                "package-attribute fakes) are fixed and the suite runs: 29 pass, "
                "6 fail. glance_shoot.py and e2e_cancel_resume.py are repaired "
                "and GREEN (9/9). smoke/pair_link.py, smoke/setup_sandbox.py and "
                "e2e_question_live.py were DELETED - the first two bootstrapped "
                "one finished release-smoke card whose result is written down in "
                "ops/tests/smoke/EVIDENCE.md, and the third spent real tokens on "
                "a real worker.",
        "why_it_bites": "A dead script is worse than a missing one: it COMPILES, "
                        "so py_compile and the gate stay green and it looks "
                        "maintained right up until someone runs it and gets a "
                        "ModuleNotFoundError. Measured - it cost a card's worth "
                        "of time when the loop-map camera was needed for a UI "
                        "judgement and turned out not to exist any more. Worse, "
                        "reviving two of them recovered 43 real checks that had "
                        "been silently unenforced ever since, including the "
                        "proof behind the 'costs are plan-share, not EUR' "
                        "decree.",
        "trigger": "any UI judgement or e2e replay that reaches for one of the "
                   "eight remaining scripts.",
        "fix": "OPEN for exactly six assertions, all named in "
               "ops/tests/unit/conftest.py's STATUS block, none of them a "
               "product regression: five duplicate `order` values that piled up "
               "in THIS register while test_debt was dead (left red on purpose "
               "- renumbering entries other cards edit is a merge conflict "
               "waiting to happen), four test_drivers expectations that "
               "describe an older _ClaudeSession (meta legitimately grew a "
               "ctx_usage key), and one nightshift fake thinner than the code "
               "it stands in for. Each needs its FIXTURE updated to match "
               "evolved behaviour and then an actual RUN - a fix nobody "
               "executes is exactly how this rotted, and a dead script compiles "
               "green so nothing else will catch it. The triage rule that "
               "settled the deletions is the owner's and it generalises: is "
               "this a JIG for one branch, or a TOOL that gets used again? A "
               "jig whose branch shipped is used up.",
        "order": 53,
    },
    {
        "id": "board-scope-still-in-settings-json",
        "title": "policy.lane_labels is BADGED 'Board' but still STORED in settings.json",
        "status": "paid",
        "what": "accounts-boards-prd phase 4 gave every settings knob a `scope` "
                "tag, and the tag is load-bearing twice over: the hub badges "
                "the row from it AND routes the write from it "
                "(surfaces/app/src/data/settings_schema.ts writeTargetFor). "
                "For profile/workspace/device/system the tag and the store "
                "agree. `policy.lane_labels` is the one row where they do not: "
                "it is tagged scope 'board' and rendered in the Boards door, "
                "because naming the stations is a property of the board layer "
                "(PRD section 3) - but it is still a workspace key in "
                "settings.json, so writeTargetFor maps 'board' to POST "
                "/settings like everything else. PRD section 6 already plans "
                "the migration ('policy.lane_labels becomes the default "
                "board's column labels; the settings key stays readable one "
                "release as fallback, then retires') and asks for a debt entry "
                "if the shortcut ships first. It did.",
        "why_it_bites": "The badge is a PROMISE about blast radius, and this "
                        "one row breaks it: it says 'Board' while a save moves "
                        "the station names for every board and every account "
                        "on the daemon. That is the exact confusion the scope "
                        "badges were added to end, so a wrong badge is worse "
                        "here than no badge - it is trusted. It also 403s a "
                        "non-owner who has a personal board and would "
                        "reasonably expect to name its columns, since the "
                        "write lands on the owner-only POST /settings.",
        "trigger": "a second board-scoped knob appears, or a non-owner tries "
                   "to rename a station from the Boards door",
        "fix": "PAID, and by the OPPOSITE of what this entry proposed - the "
               "BADGE was wrong, not the store, and the live data settled it. "
               "boards._seed_columns already copied this key into the default "
               "board's columns at phase-2 boot, and data/boards.ts "
               "columnLabel() prefers a column's own label, so editing this "
               "knob had not moved a board column since that seed ran. What it "
               "still names is every station OUTSIDE a board (move menu, card "
               "detail, loop map) plus the fallback for a deliberately "
               "unlabelled column. So the key is tagged scope 'workspace' now "
               "and the badge tells the truth; the genuinely board-owned value "
               "- the per-COLUMN label, per column and variable in count, which "
               "no CONTROLS entry can render - keeps the board editor as its "
               "one edit surface and is SHOWN in the Boards door in place of "
               "the old column count. The plan above would have caused real "
               "damage: retiring the key reverts the move menu, the card detail "
               "and the loop map to the untranslated i18n default, losing "
               "renames the owner had made. writeTargetFor deliberately did NOT "
               "grow a third target - no shipped row carries scope 'board', so "
               "it would have been dead code; 'board' stays in SCOPES as a "
               "badge with no knob-shaped row, exactly like 'device'. Judged in "
               "a browser at both form factors (ops/tests/e2e_stored_config.py) "
               "against a sandbox seeded with every state "
               "(ops/tools/storedconfig_verify_daemon.py).",
        "order": 54,
    },
    {
        "id": "glasses-voice-unconfigured-origin",
        "title": "Glasses voice was dead for one EMPTY setting, and nothing in "
                 "any UI can set it",
        "status": "paid",
        "what": "settings.glance_origin (spine/storage/events.py:54) is the "
                "public origin of the glance Worker - the only address "
                "GlassVoiceService can reach, because it is a plain "
                "HttpURLConnection client OUTSIDE the E2EE relay. It defaults to "
                "\"\" and was EMPTY on the owner's live daemon while everything "
                "around it was ready: glance_token set, glance_talk on, the "
                "Worker deployed at https://glance.helmdeck.de, and all four "
                "native classes in the shipped APK (versionCode 88). "
                "surfaces/app/src/app/chat.tsx:660-668 reads it, finds it blank, "
                "shows chat.glassesUnconfigured and RETURNS - so configure() and "
                "listen() were never called and the microphone never opened. "
                "PAID 2026-09-04 by setting it to the deployed Worker origin via "
                "events.save_settings (audited, checkpointed). No code changed; "
                "no rebuild was needed, because nothing was ever unbuilt.",
        "why_it_bites": "PAID - kept because the FAILURE SHAPE is the lesson and "
                        "it is still latent. Two debts (glasses-native-"
                        "uncompiled, glass-listening-report-uncompiled) blamed an "
                        "uncompiled Kotlin half for a symptom that was one empty "
                        "string, and that misdiagnosis survived in the register "
                        "long enough to be quoted as the reason the mic did not "
                        "work. The register said 'build the APK'; the APK was "
                        "already built. VERIFY THE PREMISE AGAINST THE ARTIFACT - "
                        "a dex scan settled in one command what the prose had "
                        "asserted for two weeks. What is STILL open and is why "
                        "this stays listed: glance_origin has NO UI ANYWHERE. "
                        "Grepping surfaces/app/src and surfaces/desktop finds "
                        "only the reader at chat.tsx:660 and the i18n string that "
                        "tells the owner to go set it 'in daemon settings' - a "
                        "place with no field for it. It is settable only by "
                        "direct save_settings from the repo, so the moment it is "
                        "cleared or a fresh workspace is seeded, glasses voice "
                        "silently reverts to a chat bubble and the next reader "
                        "will again suspect the native build.",
        "trigger": "A fresh workspace, a settings reset, or any redeploy of the "
                   "glance Worker to a different origin. The daemon defaults it "
                   "to empty, so a new install starts BROKEN and the only "
                   "diagnosis surface is one German error bubble in the chat.",
        "fix": "Give glance_origin a real field on the glasses/connectors "
               "settings door, beside glance_token and the glance_talk switch - "
               "so the setting that decides whether the microphone exists is "
               "editable where the owner already goes to enable it. Better: have "
               "ops/deploy/push_glance.sh write the origin it just deployed to "
               "back into settings, so deploying the Worker configures the phone "
               "by construction rather than by the owner remembering.",
        "since": "2026-09-04",
        "order": 56,
    },
    {
        "id": "glass-listening-report-uncompiled",
        "title": "The lens's LISTENING indicator depends on a GlassVoiceService "
                 "edit that has never been compiled",
        "status": "paid",
        "what": "The glasses conversation surface itself is verified: GET "
                "/glance/chat, POST /glance/state and spine/ops/glassturn.py are "
                "covered by ops/tests/test_glance_conversation.py, and the "
                "rebuilt talk screen in surfaces/glasses/ by a real 600x600 "
                "Playwright drive against a real daemon "
                "(ops/tests/e2e_glance_conversation.py), screenshots judged. "
                "WHAT IS NOT COMPILED is the reporter feeding one of its five "
                "states: app/plugins/glassvoice/GlassVoiceService.kt gained a "
                "report() helper plus five call sites (mic opened; conversation "
                "ended on silence; ACTION_STOP; and the two early returns where "
                "no mic opens). An APK cannot be built from a card worktree "
                "(DEPLOY.md 2, NDK path length) - the same constraint that "
                "produced glasses-native-uncompiled.",
        "why_it_bites": "PAID 2026-09-04, and the premise was already stale when "
                        "it was read - this entry claimed 'never compiled' while "
                        "the class was sitting in the shipped APK. MEASURED, not "
                        "reasoned: app/helmdeck/voice/GlassVoiceService is present "
                        "in the dex of android/app/build/outputs/apk/release/"
                        "app-release.apk (versionCode 88, native ship fe8a204), "
                        "alongside GlassesRadio, GlassCameraService and "
                        "GlassesBridgeModule, and the built manifest carries "
                        "RECORD_AUDIO / MODIFY_AUDIO_SETTINGS / BLUETOOTH_CONNECT "
                        "/ FOREGROUND_SERVICE_MICROPHONE, the typed <service> and "
                        "the RecognitionService <queries>. So report() compiles - "
                        "that was the entry's one genuinely open question. The "
                        "INDICATOR PATH is now proven end-to-end through the "
                        "public worker: POST /glance/state {state:listening,"
                        "mic:glasses} -> 200, and GET /glance/chat immediately "
                        "reports turn {state:'listening', mic:'glasses'}; idle "
                        "clears it the same way. What blocked the indicator was "
                        "never the Kotlin - it was settings.glance_origin being "
                        "EMPTY, so the phone never called configure()/listen() at "
                        "all (see glasses-voice-unconfigured-origin). STILL "
                        "UNPROVEN and only observable on hardware: whether "
                        "micWire() reports 'glasses' exclusively when the SCO "
                        "route was genuinely obtained, and whether the fire-and-"
                        "forget thread stays invisible to the recogniser. "
                        "Kept for the original reasoning: "
                        "it DEGRADES rather than breaks, and that was a design "
                        "constraint rather than luck. `listening` is the only "
                        "one of the five turn states that comes from the device; "
                        "heard/thinking/answered/failed are all set by the daemon "
                        "from its own handling of /glance/talk. So on an APK "
                        "predating this edit the lens still shows the transcript, "
                        "the thinking state, the answer and the options - it "
                        "simply never lights the mic indicator, and it NEVER "
                        "claims a mic is open when none is. The failure mode is a "
                        "missing signal, never a false one. Genuinely unproven: "
                        "whether report() compiles (it uses only "
                        "HttpURLConnection, JSONObject, Thread and the existing "
                        "safe{}/prefs members - all already imported and used by "
                        "ask() a few lines above, but unproven is unproven), and "
                        "whether the fire-and-forget thread is really invisible "
                        "to the recogniser on real hardware.",
        "trigger": "ACCEPTING THIS CARD. ops/deploy/ship.sh fingerprints "
                   "app.json's expo block and this card does not touch it, so the "
                   "accept takes the OTA branch and this Kotlin is NOT built: the "
                   "daemon and lens halves ship and work, and the mic indicator "
                   "stays dark until the next APK rebuild for any reason. That is "
                   "a safe outcome - it is only misleading if someone reads 'the "
                   "listening indicator shipped' without this note.",
        "fix": "Build the APK from a short real path (C:\\hd\\app, per DEPLOY.md "
               "2), fix whatever kotlinc says, then verify on real glasses that "
               "the indicator lights within a beat of the mic opening and clears "
               "on ACTION_STOP. The one thing no build can settle is whether "
               "micWire() reports 'glasses' only when the SCO route was genuinely "
               "obtained - it reads micInUse, which GlassVoiceService already "
               "derives from routeToGlasses()'s own return value rather than from "
               "the request, so the plumbing is right by construction and only "
               "the observation is missing.",
        "order": 55,
    },
    {
        "id": "configure-allowlist-not-derived",
        "title": "The configure allowlist is MEASURED against the enforced set, "
                 "not rendered from it - the design doc asked for rendered",
        "status": "paid",
        "what": "harness-config-ui phase 2 turned the twelve hand-counted keys "
                "in board-copilot.md:134-146 into a `fixed` slot "
                "(behavior.ALLOWLIST_PROSE) that harness.write_agent can no "
                "longer overwrite. The design doc section 5.5 asked for more: "
                "the paragraph should be RENDERED FROM THE SCHEMA so that "
                "'what the settings page can edit is what the chat can edit, by "
                "construction'. It is not. Rendering it truthfully would CHANGE "
                "WHAT HENRY IS TOLD, because the prose and the enforced set "
                "genuinely differ today: copilot_actions.ALLOWED_CONFIG "
                "contains `jira` and the brief has never mentioned it. So the "
                "drift is measured instead - behavior.allowlist_drift() "
                "computes it and ops/tests/test_behavior_rules.py fails on it, "
                "currently reporting exactly that one key.",
        "why_it_bites": "The two lists can still disagree; they just can no "
                        "longer disagree SILENTLY, which is the smaller half of "
                        "the goal. Concretely: Henry may write settings.jira "
                        "through `configure` and nothing in his brief says so, "
                        "so the capability is reachable but undocumented to the "
                        "agent that holds it. The reverse direction is now safe "
                        "- a key added to the brief but not to ALLOWED_CONFIG is "
                        "refused by the server, which is the correct way round "
                        "for a permission surface to fail.",
        "trigger": "The failing check in ops/tests/test_behavior_rules.py "
                   "(test_allowlist_is_measured). It is red on purpose and "
                   "names the difference, so this cannot be missed by anyone "
                   "who runs the suite.",
        "fix": "PAID 2026-09-02 by owner decision: option (a) - `jira` was added "
               "to the brief's configure paragraph, so the prose now names "
               "exactly what ALLOWED_CONFIG enforces. The owner was shown that "
               "settings.jira holds an api_token and that door "
               "'Verbindungen' already has a real form for it, and chose to "
               "TELL Henry about the capability rather than withdraw it; the "
               "brief line therefore carries an explicit caution not to invent "
               "a token or echo one back in chat. Henry's brief genuinely "
               "changed, so the byte-identity pin for the `pm` surface moved in "
               "the same commit - only after the diff against 883c72f was shown "
               "to be exactly one added line. "
               "allowlist_drift() was KEPT rather than deleted as this entry "
               "originally planned: now that the two lists agree it IS the "
               "equality assertion, and inlining it into the test would have "
               "given the test its own second copy of the parser - the very "
               "duplication this item was about.",
        "order": 56,
    },
    {
        "id": "agent-may-swap-readonly",
        "title": "\"Henry darf Regeln selbst aendern\" ships READ-ONLY - the "
                 "design doc asked for a real switch",
        "status": "open",
        "what": "harness-config-ui section 6 lists `agentMaySwap` as an "
                "\"echter Schalter, Default aus\" in Henry's block \"Was er "
                "darf\". Phase 3 renders it as a LOCKED row instead. The reason "
                "is mechanical: the value that actually gates an agent-initiated "
                "swap is `policies.agentMaySwap` in the POLICY PLANE, written "
                "only by spine/auth/policy.py::swap. Every other behaviour rule "
                "stores under `rule.<key>.<surface>` in settings.json or the "
                "project table, and a rule row doing that for this key would "
                "save cleanly, badge itself \"gesetzt\", and gate nothing.",
        "why_it_bites": "The owner cannot grant or withdraw Henry's swap "
                        "authority from the screen that shows him every other "
                        "rule - he has to know this one value lives elsewhere. "
                        "That is a smaller version of the complaint the whole "
                        "card answers. The direction that shipped is the safe "
                        "one (it stays OFF, the seeded default), so this costs "
                        "reach, not safety.",
        "trigger": "Opening Henry > \"Was er selbst darf\" on the harness "
                   "screen: the row carries a lock and a why-sentence saying the "
                   "value lives in the policy plane, where the doc specifies a "
                   "switch.",
        "fix": "Give projectconfig.write_scoped a third branch for rules that "
               "declare a policy-plane path, routing them through policy.swap() "
               "- the shipped, tracked, human-authority writer - instead of the "
               "settings/project writers. This is an OWNER DECISION rather than "
               "a refactor, because it puts 'may the agent change the rules' on "
               "a screen the agent can also drive through `configure`: the same "
               "swap() would become reachable from the chat, and whether that is "
               "wanted is precisely the question agentMaySwap asks. Shipping a "
               "dummy toggle meanwhile was REFUSED - a dead switch on this row "
               "of all rows is the worst place in the app for one.",
        "order": 57,
    },
    {
        "id": "pm-copilot-merge",
        "title": "pm and copilot were two cells for one responsibility - merged",
        "status": "paid",
        "what": "Owner directive 2026-09-03 (\"pm und henry is eins\"), backed "
                "by evidence, not just preference: cells/pm/pm.py's start_loop "
                "was a plain ticker with no LLM identity, no chat surface and "
                "no memory - its _ask() spawned the SAME copilot.CLAUDE binary "
                "Henry's broker uses, one-shot, for report generation. PM's "
                "proactive notices already routed to Henry, never to the owner "
                "directly (owner decree 2026-08-30, pm.py's _to_henry). Two "
                "registered Cells, two enabled_keys (pmEnabled/copilotEnabled), "
                "two route groups, for one agent identity. Order 33 above (this "
                "same file) had already documented them as structurally "
                "identical/parallel without proposing a merge - this entry is "
                "that merge. FIXED: spine/registry/cells.py's standalone `pm` "
                "Cell deleted; its logic_files/route_modules/repo_files/"
                "ui_files/board folded into the `copilot` Cell. `Cell.start` "
                "generalised to accept a tuple of (module, func) pairs so one "
                "enabled_key can launch two independent loops "
                "(henry_broker.start_broker + pm.start_loop) - a failure in "
                "one does not take the other down. `pmEnabled` removed from "
                "daemon/policy_seed.json, surfaces/app/src/boot/policies.ts and "
                "kernel/keys.ts's PolicySet. Every useCellEnabled(\"pm\") call "
                "site (index.tsx's PMStatusPanel gate, settings.tsx's "
                "PMControls gate) repointed to useCellEnabled(\"copilot\") - "
                "confirmed trap: the hook fails OPEN for an unknown cell id, so "
                "leaving these unchanged would have made both panels "
                "permanently visible the moment the pm cell id left the /cells "
                "manifest, regardless of the Henry toggle. apimeta._cell_tracks "
                "(the pipeline's per-cell band, added the same day) now joins "
                "copilot's rule-derived segments with its declared `board` "
                "entry when both fire at the same station (backlog), so the "
                "merge does not silently drop PM's verb from the picture.",
        "why_it_bites": "RESOLVED. This was a deliberate BEHAVIOR NARROWING, "
                        "decided explicitly by the owner rather than left as a "
                        "side effect: before the merge, PM's backlog-planning "
                        "loop ran independent of copilotEnabled (only specific "
                        "escalation notices routed through Henry); after the "
                        "merge, turning Henry off also stops backlog planning. "
                        "ops/tests/test_server_routes.py's cell-registry-gate "
                        "block rewritten accordingly: /cells now lists five "
                        "agentic systems (not six), and disabling copilotEnabled "
                        "404s /pm/plan alongside /chat/history in the same "
                        "policy.swap round trip.",
        "trigger": "adding a cell that only ever speaks through another cell's "
                   "voice; a UI toggle that never independently controlled "
                   "anything the owner could observe",
        "fix": "PAID. See 'what' above for the full file list.",
        "order": 58,
    },
    {
        "id": "process-connectors-engineer-merge",
        "title": "process and connectors were the engineer's own build process "
                 "wearing two extra cells - merged",
        "status": "paid",
        "what": "Owner directive 2026-09-03, completing the two-agent model "
                "the pm-copilot merge (order 58) began: \"Du hast ein "
                "engineer, der seinen Bau Prozess hat, dazu gehoert alle drei "
                "folder. Henry ist der Koordinator.\" Evidence matched the "
                "directive: a chain's steps dispatch INTO engineer cards, "
                "process's proposer is a stateless one-shot LLM call (the "
                "same shape pm's planner was), and connectors are BUILT by "
                "engineer cards and installed only after the same gate. "
                "FIXED: cells.py's process + connectors Cell entries deleted; "
                "their paths/prefixes/logic_files/route_modules/ui folded "
                "into engineer; Cell.start's tuple form (built for order 58) "
                "now launches three loops under one flag (engineer lifecycle "
                "+ chain poller + connector scheduler); processes.py, "
                "connectors.py, routes_connectors.py physically moved to "
                "cells/engineer/ (ui plugins renamed processes.tsx/"
                "connectors.tsx to dodge the surface.tsx collision); "
                "processEnabled/connectorsEnabled removed from the seeds and "
                "the app kernel. NEW MECHANISM this merge needed that order "
                "58 did not: Cell.surfaces (extra surface ids beyond the "
                "primary) + manifest \"surfaces\" list, because tab-hiding "
                "(_layout.tsx useDisabledCellSurfaces) keyed off ONE surface "
                "per cell and the absorbed systems have REAL tabs - engineer "
                "now declares surfaces.processes + surfaces.connectors, so "
                "one switch hides all three tabs.",
        "why_it_bites": "RESOLVED - and it CAUGHT TWO LATENT BUGS from order "
                        "58 while sweeping for the fail-open trap: pm.py and "
                        "pm_triangle.py still guarded their loops with "
                        "enabled_id(\"pm\"), an id that stopped existing at "
                        "the pm merge - enabled_id returns True for unknown "
                        "ids, so both guards had silently become no-ops. "
                        "Repointed to \"copilot\" alongside processes.py's "
                        "clear_step_stamps guard (\"process\" -> "
                        "\"engineer\"). The trap is now named in the guard "
                        "comments themselves; any future cell rename must "
                        "grep enabled_id( and useCellEnabled( first.",
        "trigger": "a cell whose outputs only ever become another cell's "
                   "inputs through that cell's own gate; renaming/merging a "
                   "cell id (grep enabled_id/useCellEnabled for fail-open "
                   "guards)",
        "fix": "PAID. See 'what' above for the full file list.",
        "order": 59,
    },
    {
        "id": "config-consolidation-and-canonical-cell-structure",
        "title": "harness config scattered across 5 planes (no export); "
                 "cells/engineer and cells/copilot each a flat pile of .py "
                 "with no shared shape",
        "status": "paid",
        "what": "Owner directive 2026-09-03: \"alles was harness config ist "
                "gehoert ins db... einer stellt seine harness ein und kann "
                "dieses exportieren\" + \"die Idee von einer Zelle ist dass "
                "es gemeinsame Struktur gibt... warum hat man eine "
                "Zellen-Form wenn man am Ende alles unterschiedlich laesst\". "
                "8-phase plan, both tracks landed: (1-4) settings.json, "
                "policy_live.json and the wipLimit/users/henry_* duplicate "
                "keys folded into db tables (workspace_config, policy_doc) "
                "behind the EXISTING events.settings()/save_settings() and "
                "policy.load()/swap() call sites - zero call-site churn, "
                "one-time migration via db._migrate() archiving the legacy "
                "files (.imported, never deleted); (5) Henry's memory folds "
                "db-ward at event time right after the spawned turn's own "
                "file write (fold-at-event-time, not a stored flag); (6) GET/"
                "POST /harness/export|import replay exclusively through "
                "those same tracked writers; (7) daemon/ split into state/ "
                "(runtime bookkeeping) + content/ (feature dirs) + backups/ "
                "(.imported chaff), board_directives.json deliberately left "
                "flat (it is TRACKED repo data, not runtime state); (8) both "
                "cells physically reorganized onto ONE shape - __init__.py + "
                "routes/ + domain folders + ui/, no loose .py at cell root "
                "(cells/engineer: cards/ chains/ connectors/; cells/copilot: "
                "chat/ broker/ planning/) - enforced going forward by "
                "ops/tests/test_cell_structure.py rather than left to "
                "discipline. spine/registry/cells.py's logic_files/"
                "route_modules now store the subpath (e.g. "
                "\"planning/pm.py\", \"routes.routes_pm\") instead of a bare "
                "leaf name; Cell.allowed_files()/daemon_roots()/"
                "_import_by_bare_name() and permissions.py's _import_module "
                "candidates updated to resolve through the new subpackages.",
        "why_it_bites": "RESOLVED for the moved files (~170 files touched "
                        "across two mechanical import-rewrite passes: "
                        "fully-dotted references, then `from cells.X import "
                        "Y` style) - full suite run afterward showed ZERO "
                        "ImportError, confirming the move was structurally "
                        "sound. Remaining test failures found in the same "
                        "sweep (steer's `by=` actor kwarg missing from "
                        "several fake_turn test doubles, a FakeEvents "
                        "double missing .settings(), a stale pm.md brief "
                        "hash) are PRE-EXISTING behavioral drift unrelated "
                        "to this move - none involve a missing module - and "
                        "are left as separate, un-registered debt for "
                        "whoever owns that feature next; flagging here so "
                        "they are not mistaken for a Phase 8 regression.",
        "trigger": "a config value the owner sets once and never sees again "
                   "when standing up a second workspace; a cell folder that "
                   "grew past ~5 files with no shared shape",
        "fix": "PAID (config track + structure track). See 'what' above.",
        "order": 60,
    },
    {
        "id": "buildloop-was-a-rule-not-a-cell",
        "title": "the settings UI rendered 3 cells while cells/ holds 2 "
                 "folders - buildloop was a registry entry with no tree",
        "status": "paid",
        "what": "Owner decree 2026-09-03 (spotted on the phone's Zellen "
                "door): \"wenn 2 folder da sind dann gibt es 2 Zellen und "
                "nicht mehr - die Registry ist nicht Taktgeber sondern "
                "code\". buildloop (registered 2026-08-18 as 'Cell #6', see "
                "cell-6-buildloop-self-governing) never had routes, an "
                "owned surface, a daemon lifecycle, or a cells/<id>/ "
                "folder - it is a HARNESS RULE: buildLoopEnabled silences "
                "the interactive agent's Stop hook. FIXED: the buildloop "
                "Cell removed from CELLS (a dated comment marks the spot); "
                "the flag lives on unchanged in the policy plane "
                "(loop_state.py's direct read untouched) and is now "
                "rendered as a toggle in the app's REGELN section "
                "(cells_catalog.tsx, next to gateBeforeReview) instead of "
                "the cell list; test_cell_structure.py gained the "
                "bidirectional mirror check (CELLS ids == cells/ folders, "
                "both directions) so a phantom cell or an unregistered "
                "folder fails the contract instead of drifting silently; "
                "e2e_cell_tab_gating/e2e_cell_rules/shot_modules_i18n "
                "repointed (engineer is the rule-less cell now - all 23 "
                "rule bindings resolve to copilot).",
        "why_it_bites": "RESOLVED. The root cause was one-directional "
                        "enforcement: the phase-8 contract test verified "
                        "'every registered cell WITH a folder has the "
                        "canonical form' and silently skipped entries "
                        "without one - the exact skip that let the UI show "
                        "an architecture the disk did not have.",
        "trigger": "a Cell registered without a cells/<id>/ folder; a UI "
                   "count that disagrees with `ls cells/`",
        "fix": "PAID. See 'what' above.",
        "order": 61,
    },
    {
        "id": "process-template-config-split",
        "title": "a process's reusable step SHAPE and its per-run state "
                 "were one JSON blob - the shape never accumulated anywhere "
                 "exportable",
        "status": "open",
        "what": "Owner decree 2026-09-03, spotted looking at the Prozesse "
                "screen: \"das ist auch config, wie trennen\". The "
                "`processes` table's rows mixed RUN fields (request/client/"
                "due/status/cost, steps carrying done/lane/track/state/"
                "ready/auto_dispatched) with the step DEFINITION (title/"
                "desc/mode/days) that is actually reusable across clients - "
                "'Schritte vorschlagen' re-invented the same shape from "
                "scratch every time via an LLM call, and it never landed "
                "anywhere an owner could export it. FIXED: new "
                "process_template db table (closed doc: name/description/"
                "steps, steps restricted to title/desc/mode/days by "
                "cells/engineer/chains/processes.py's _clean_template_step, "
                "the one writer - a runtime field can never enter this "
                "table by any path, including template_from_process lifting "
                "a step off a REAL run). create() gained template_id: a "
                "template-sourced process skips the LLM proposer entirely "
                "(deterministic, free). /harness/export gained the "
                "process_templates plane; import replays through "
                "save_template only. New caps templates.view (team-visible, "
                "same tier as projects.view) / templates.manage (owner-only, "
                "since this is config now) - a process RUN itself stays open "
                "to every role, unchanged.",
        "why_it_bites": "RESOLVED for the backend + export/import track "
                        "(ops/tests/test_process_templates.py, full round "
                        "trip through the real HTTP handlers). The UI split "
                        "(a Vorlagen list above the Läufe list on the "
                        "Prozesse screen, 'als Vorlage speichern' action) "
                        "is NOT yet built - the API is there "
                        "(GET /process_templates, POST /process_templates/"
                        "save|from_process|<id>/delete, POST /processes/new "
                        "with template_id) but cells/engineer/ui/"
                        "processes.tsx still only shows the freetext "
                        "'Schritte vorschlagen' form. Trigger for whoever "
                        "picks this up: the backend is done, this is a UI-"
                        "only remaining task.",
        "trigger": "config data (a reusable shape/definition) stored only "
                   "as a byproduct of one run's instance data, with no "
                   "separate identity or export path",
        "fix": "PAID (backend). OPEN: cells/engineer/ui/processes.tsx UI "
               "for template list/save/start-from/delete.",
        "order": 62,
    },
]

def list_debt():
    return sorted(DEBT, key=lambda d: (d["status"] == "paid", d["order"]))

def fix_task(item):
    return ("STRUCTURAL DEBT FIX [%s]: %s\n\nWhat: %s\nWhy it bites: %s\n"
            "Planned fix: %s\n\nWhen done and verified, set this item's status "
            "to 'paid' in daemon/debt.py (keep it listed) and note the commit."
            % (item["id"], item["title"], item["what"], item["why_it_bites"],
               item["fix"]))
