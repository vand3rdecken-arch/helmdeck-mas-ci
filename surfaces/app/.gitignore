# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

example

# generated native folders
/ios
/android
google-services.json
# dated safety copies made before editing the Firebase config (e.g.
# google-services.json.bak-20260829) - same secret-bearing content, same rule
google-services.json.bak*
.claude/settings.json
.vscode/settings.json
dist-ota/
# ANY dist-* export dir, not just dist-ota - a bare `expo export` for a
# verification screenshot (dist-check2, dist-verify, ...) got scooped into
# a commit by the Henry auto-snapshot hook (2026-09-03, 16548 lines of
# built HTML) because only the literal dist-ota/ name was excluded.
dist-*/
