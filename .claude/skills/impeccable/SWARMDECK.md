# SwarmDeck overrides (read AFTER SKILL.md - these win on conflict)

- Register: PRODUCT (app UI/dashboard). Never brand mode inside the app.
- The design system is committed and wins: Plane OKLCH tokens + liquid glass
  in web/app/globals.css. Do not introduce new palettes/fonts; palette.mjs is
  not needed here.
- Laws (from CLAUDE.md): data colors stay semantic (status/priority/lane);
  client hues come from the deterministic hash only; recording/audit UI is
  never hidden or de-emphasized.
- Verification: the loop's TEST state requires screenshots JUDGED (readability,
  centering, theming, collisions) - impeccable guides the writing, the loop
  gates the shipping.
